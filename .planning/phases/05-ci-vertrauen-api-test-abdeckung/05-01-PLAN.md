---
phase: 05-ci-vertrauen-api-test-abdeckung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-ci-vertrauen-api-test-abdeckung/05-CI-AUDIT.md
autonomous: true
requirements:
  - CI-01

must_haves:
  truths:
    - "A document exists that lists every active CI workflow with per-job status (stable/flaky/slow/skipped/failing)"
    - "The document identifies which CI gates are currently blocking vs. non-blocking"
    - "The document records the known typecheck failure root cause and the lint debt count"
    - "The document is placed at the path specified and is complete enough to serve as the Phase 5 Ist-Zustand baseline"
  artifacts:
    - path: ".planning/phases/05-ci-vertrauen-api-test-abdeckung/05-CI-AUDIT.md"
      provides: "CI-01 requirement artifact — per-job status matrix for all 4 workflows"
      contains: "ci.yml, test.yml, release.yml, security.yml"
  key_links:
    - from: "05-CI-AUDIT.md"
      to: ".github/workflows/ci.yml"
      via: "documents current state of Lint step (continue-on-error: true)"
      pattern: "continue-on-error"
    - from: "05-CI-AUDIT.md"
      to: "apps/matrix-bot/src/index.ts:44"
      via: "documents TS2322 root cause"
      pattern: "TS2322"
---

<objective>
Create the CI-01 requirement artifact: a complete, accurate CI audit document that captures the current (pre-fix) state of all GitHub Actions workflows, per-job status, and known failures.

Purpose: CI-01 requires an objective written record of the CI Ist-Zustand before any changes are made. This document serves as the baseline that the rest of Phase 5 works against, and gives future Andi a clear before/after comparison.

Output: `.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-CI-AUDIT.md` — a structured markdown document covering all 4 workflows with per-job status.
</objective>

<execution_context>
@/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-RESEARCH.md section "CI Workflow Audit"
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/andreas/workspace/guildora/platform/.planning/PROJECT.md
@/home/andreas/workspace/guildora/platform/.planning/ROADMAP.md
@/home/andreas/workspace/guildora/platform/.planning/STATE.md
@/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-CONTEXT.md
@/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-RESEARCH.md

<interfaces>
<!-- Current CI workflow file contents — executor verified these directly -->

From .github/workflows/ci.yml:
```yaml
name: CI
on:
  push: { branches: [main, dev] }
  pull_request: { branches: [main, dev] }
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4 (node-version: 22, cache: pnpm)
      - run: pnpm install --frozen-lockfile
      - name: Lint
        run: pnpm lint
        continue-on-error: true        # ← FAILS (257 hub errors), non-blocking
      - run: pnpm typecheck            # ← FAILS (matrix-bot TS2322), blocking
      - run: pnpm build                # ← unknown (blocked by typecheck), blocking
```

From .github/workflows/test.yml:
```yaml
jobs:
  hub-unit-tests:
    - run: pnpm --filter @guildora/hub test   # STABLE — 36 files, 291 tests, ~2s
```

From .github/workflows/release.yml (trigger: push to v* tags):
```yaml
jobs:
  validate:
    - run: pnpm lint
      continue-on-error: true                 # ← FAILS, non-blocking
    - run: pnpm typecheck                     # ← FAILS (same TS2322), blocking
    - run: pnpm build                         # ← unknown, blocking
    - run: pnpm --filter @guildora/hub test   # STABLE
    - name: Security audit
      run: pnpm audit --audit-level=high
      continue-on-error: true                 # intentionally non-blocking
```

From .github/workflows/security.yml (trigger: daily cron 04:00 UTC + workflow_dispatch):
```yaml
jobs:
  security-scan:
    - uses: anthropics/claude-code-base-action@beta   # INDEPENDENT scan
    - run: pnpm audit                                  # informational
    # Notification: Linear issue + Discord alert on findings
    # Requires: CLAUDE_CODE_OAUTH_TOKEN, LINEAR_API_KEY, LINEAR_TEAM_ID, DISCORD_SECURITY_WEBHOOK
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CI audit document</name>
  <files>/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-CI-AUDIT.md</files>
  <read_first>
    - /home/andreas/workspace/guildora/platform/.github/workflows/ci.yml (current state of every step)
    - /home/andreas/workspace/guildora/platform/.github/workflows/test.yml
    - /home/andreas/workspace/guildora/platform/.github/workflows/release.yml
    - /home/andreas/workspace/guildora/platform/.github/workflows/security.yml
    - /home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-RESEARCH.md (section "CI Workflow Audit" — pre-verified data)
  </read_first>
  <action>
Create `/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-CI-AUDIT.md` with the following exact structure and content (derived from verified research data):

```markdown
# CI Audit — Phase 5 Baseline

**Audited:** 2026-04-18
**Purpose:** Ist-Zustand der CI vor Phase-5-Fixes. Dient als Baseline für CI-01.
**Scope:** `platform/` — alle GitHub Actions Workflows unter `.github/workflows/`

---

## Workflow Inventory

| Workflow | Datei | Trigger | Zweck |
|----------|-------|---------|-------|
| CI | `ci.yml` | push/PR auf main, dev | Basis-Gates: Lint, Typecheck, Build |
| Test | `test.yml` | push/PR auf main, dev | Hub Unit-Tests |
| Release | `release.yml` | push auf `v*`-Tags | Validate + GitHub Release + Docker-Publish |
| Security | `security.yml` | Täglich 04:00 UTC + workflow_dispatch | Scheduled Security Scan (unabhängig) |

---

## `.github/workflows/ci.yml` — Push/PR auf main, dev

| Job | Step | Aktueller Status | Gate? | Anmerkung |
|-----|------|-----------------|-------|-----------|
| `ci` | `pnpm install --frozen-lockfile` | STABLE | Ja — blocking | |
| `ci` | Lint (`pnpm lint`) | FAILING — 257 hub-Errors | **NEIN** — `continue-on-error: true` | Ziel von Phase 5: fixing und blocking machen |
| `ci` | Typecheck (`pnpm typecheck`) | FAILING — `@guildora/matrix-bot` TS2322 | Ja — blocking | Einziger Fehler: `src/index.ts:44` |
| `ci` | Build (`pnpm build`) | UNBEKANNT | Ja — blocking | Nicht auswertbar, da Typecheck davor abbricht |

**Bekannte Fehler:**

1. **Lint** (257 Errors, 75 Warnings in `@guildora/hub`):
   - 149× `@typescript-eslint/no-explicit-any` — Test-Mocks und Prod-Code
   - 51× `@typescript-eslint/no-unused-vars` — Ungenutzte Imports/Variablen
   - 24× `@typescript-eslint/unified-signatures` — Emit-Overloads in Flow-Builder-Komponenten
   - 16× `import/no-duplicates` — Doppelte Import-Statements aus gleichem Modul
   - 5× `@typescript-eslint/no-dynamic-delete` — `delete obj[key]`-Ausdrücke
   - 1× `vue/no-mutating-props` — `SimpleFormField.vue:108`
   - 1× `vue/no-v-text-v-html-on-component` — `LandingPreview.vue:74`
   - `@guildora/web`: 0 Errors, 12 Warnings (exit 0 — schon grün)

2. **Typecheck** (1 Error in `@guildora/matrix-bot`):
   - Datei: `apps/matrix-bot/src/index.ts:44`
   - Fehler: `TS2322 — Type 'string | undefined' is not assignable to type 'string'`
   - Ursache: `BOT_INTERNAL_TOKEN` (Typ `string | undefined` aus `process.env`) wird an `startInternalSyncServer({ token: string })` übergeben. Guard auf Zeilen 21–26 beendet den Prozess bei falsy-Token, aber TypeScript's Narrowing greift nicht über die Funktionsgrenze.
   - Fix: `token: BOT_INTERNAL_TOKEN!` (Non-Null-Assertion nach dem Guard) — 1 Zeile.

---

## `.github/workflows/test.yml` — Push/PR auf main, dev

| Job | Step | Aktueller Status | Gate? | Anmerkung |
|-----|------|-----------------|-------|-----------|
| `hub-unit-tests` | `pnpm --filter @guildora/hub test` | STABLE — 36 Dateien, 291 Tests, ~2s | Ja — blocking | Kein Flaky-Test gefunden |

**Details:**
- Test-Run dauert ~2 Sekunden (deterministisch)
- Kein `it.skip`, kein `xit`, kein `// TODO: flaky` in der gesamten Testsuite
- 36 Spec-Dateien unter `apps/hub/`
- Alle 291 Tests grün (verifiziert 2026-04-18)

---

## `.github/workflows/release.yml` — Push auf `v*`-Tags

| Job | Step | Aktueller Status | Gate? | Anmerkung |
|-----|------|-----------------|-------|-----------|
| `validate` | `pnpm install --frozen-lockfile` | STABLE | Ja | |
| `validate` | Lint (`pnpm lint`) | FAILING | **NEIN** — `continue-on-error: true` | Gleicher Grund wie ci.yml |
| `validate` | Typecheck (`pnpm typecheck`) | FAILING | Ja — blocking | Gleicher matrix-bot TS2322-Fehler |
| `validate` | Build (`pnpm build`) | UNBEKANNT | Ja — blocking | Nicht auswertbar |
| `validate` | Hub-Tests (`pnpm --filter @guildora/hub test`) | STABLE | Ja — blocking | |
| `validate` | Security audit (`pnpm audit --audit-level=high`) | UNBEKANNT | **NEIN** — `continue-on-error: true` | Absichtlich non-blocking (Phase 4 OK) |
| `create-release` | GitHub Release erstellen | — | Braucht `validate` | |
| `docker-publish` | Docker Build + Push (hub, web, bot) | — | Braucht `create-release` | |

---

## `.github/workflows/security.yml` — Täglich 04:00 UTC + manuell

| Job | Step | Aktueller Status | Gate? | Anmerkung |
|-----|------|-----------------|-------|-----------|
| `security-scan` | Claude Code Security Scan (`anthropics/claude-code-base-action@beta`) | INDEPENDENT | Nein | Läuft unabhängig vom Push-CI |
| `security-scan` | `pnpm audit` | Informational | Nein | Teil des Scans |
| Notification | Linear Issue + Discord Alert bei Findings | Conditional | Nein | |

**Benötigte Secrets:** `CLAUDE_CODE_OAUTH_TOKEN`, `LINEAR_API_KEY`, `LINEAR_TEAM_ID`, `DISCORD_SECURITY_WEBHOOK`

---

## Zusammenfassung: Gate-Status

| Gate | Blocking? | Aktuell grün? |
|------|-----------|--------------|
| Lint (ci.yml) | NEIN (`continue-on-error`) | NEIN — 257 Errors |
| Lint (release.yml) | NEIN (`continue-on-error`) | NEIN — 257 Errors |
| Typecheck | JA | NEIN — TS2322 in matrix-bot |
| Build | JA | UNBEKANNT (Typecheck-Fehler vorher) |
| Hub Unit-Tests | JA | JA — 291/291 grün |
| Security Audit | NEIN (`continue-on-error`) | (Phase 4 OK) |
| Scheduled Security Scan | NEIN (unabhängig) | Informational |

---

## Phase-5-Ziele (was sich ändern soll)

1. **Typecheck blocking & grün**: matrix-bot TS2322 fixen → `pnpm typecheck` exit 0
2. **Lint grün**: 257 Hub-Errors fixen → `pnpm --filter @guildora/hub lint` exit 0
3. **ESLint-Security-Plugins**: `eslint-plugin-security` + `eslint-plugin-no-unsanitized` in hub integrieren (D-02)
4. **Lint wieder blocking**: `continue-on-error: true` aus `ci.yml` + `release.yml` entfernen — erst nach Punkt 2+3
5. **API-Test-Abdeckung**: 4 neue Spec-Files für Auth, Mod, Admin-Settings, Community-Settings (QA-01, D-04)

---

*Audit erstellt: 2026-04-18*
*Nächstes Review: nach Phase-5-Abschluss*
```
  </action>
  <acceptance_criteria>
    - File exists at exact path: `/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-CI-AUDIT.md`
    - File contains the string "ci.yml" (workflow table row)
    - File contains the string "test.yml" (workflow table row)
    - File contains the string "release.yml" (workflow table row)
    - File contains the string "security.yml" (workflow table row)
    - File contains the string "continue-on-error" (non-blocking gate documentation)
    - File contains the string "TS2322" (typecheck root cause)
    - File contains the string "257" (lint error count)
    - File contains the string "291" (test count)
    - File contains the string "STABLE" (at least one stable job)
    - File contains the string "NEIN" (at least one non-blocking gate identified)
  </acceptance_criteria>
  <verify>
    <automated>
      test -f /home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-CI-AUDIT.md && grep -q "TS2322" /home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-CI-AUDIT.md && grep -q "continue-on-error" /home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-CI-AUDIT.md && echo "AUDIT DOC OK"
    </automated>
  </verify>
  <done>05-CI-AUDIT.md exists, covers all 4 workflows with per-job status, documents the two known failures (TS2322, 257 lint errors), and is committed. CI-01 artifact complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document authorship | Planner/executor writes the audit document; no external input crosses a trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | 05-CI-AUDIT.md | accept | Document is in `.planning/` (tracked in repo); contains no secrets, only workflow structure and error counts. Low-value target. |
</threat_model>

<verification>
- `test -f .planning/phases/05-ci-vertrauen-api-test-abdeckung/05-CI-AUDIT.md` → exits 0
- `grep -q "ci.yml\|test.yml\|release.yml\|security.yml" 05-CI-AUDIT.md` → all four workflow names present
- `grep -q "continue-on-error" 05-CI-AUDIT.md` → non-blocking gates documented
- `grep -q "TS2322" 05-CI-AUDIT.md` → typecheck root cause documented
</verification>

<success_criteria>
- 05-CI-AUDIT.md created with accurate per-job status for all 4 workflows
- Both non-blocking lint gates (ci.yml, release.yml) explicitly called out
- Matrix-bot TS2322 root cause documented with file path and line number
- Hub lint debt count (257 errors) and dominant categories documented
- Hub test suite status (291 tests, ~2s, stable) documented
- Document committed to git
</success_criteria>

<output>
After completion, create `/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-01-SUMMARY.md`
</output>
