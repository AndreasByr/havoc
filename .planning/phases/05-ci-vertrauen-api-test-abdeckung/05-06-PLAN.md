---
phase: 05-ci-vertrauen-api-test-abdeckung
plan: 06
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - apps/hub/server/api/__tests__/admin-settings.spec.ts
  - apps/hub/server/api/__tests__/community-settings.spec.ts
autonomous: true
requirements:
  - QA-01

must_haves:
  truths:
    - "Admin settings routes (community-settings, theme, membership-settings, moderation-rights, discord-roles) have spec tests verifying requireAdminSession enforcement"
    - "Community-settings route (display-name-template) has spec tests verifying requireSession enforcement"
    - "Both new spec files follow the established pattern from admin-platforms.spec.ts"
    - "All new tests pass when run with `pnpm --filter @guildora/hub test`"
  artifacts:
    - path: "apps/hub/server/api/__tests__/admin-settings.spec.ts"
      provides: "Tests for admin settings routes with requireAdminSession auth checks"
      exports: ["describe('GET /api/admin/community-settings')", "describe('GET /api/admin/theme')"]
    - path: "apps/hub/server/api/__tests__/community-settings.spec.ts"
      provides: "Tests for community-settings display-name-template and apps routes"
      exports: ["describe('GET /api/community-settings/display-name-template')"]
  key_links:
    - from: "apps/hub/server/api/__tests__/admin-settings.spec.ts"
      to: "apps/hub/server/utils/auth.ts"
      via: "vi.mock('../../utils/auth', () => ({ requireAdminSession: vi.fn() }))"
      pattern: "requireAdminSession"
    - from: "apps/hub/server/api/__tests__/community-settings.spec.ts"
      to: "apps/hub/server/utils/auth.ts"
      via: "vi.mock('../../utils/auth', () => ({ requireSession: vi.fn() }))"
      pattern: "requireSession"
---

<objective>
Create two new spec files covering Admin Settings routes (5 route pairs) and Community Settings routes (display-name-template + apps endpoints). These complete the D-04 priority list alongside Plan 05.

Purpose: QA-01 requires all D-04 priority routes to have spec files. Admin settings and community-settings are ranked MEDIUM priority and complete the required coverage. Per D-06, success is measured by spec file existence, not coverage percentage.

Output: Two new spec files following the `vi.mock("../../utils/auth")` + dynamic `importHandler()` pattern established in existing admin spec files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/andreas/workspace/guildora/platform/.planning/STATE.md
@/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-RESEARCH.md
@/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-PATTERNS.md

<interfaces>
<!-- Handler import paths for admin-settings.spec.ts (relative to server/api/__tests__/) -->
../admin/community-settings.get
../admin/community-settings.put
../admin/theme.get
../admin/theme.put
../admin/membership-settings.get
../admin/membership-settings.put
../admin/moderation-rights.get
../admin/moderation-rights.put
../admin/discord-roles.get

<!-- Handler import paths for community-settings.spec.ts (relative to server/api/__tests__/) -->
../community-settings/display-name-template.get
../apps/index.get
../apps/navigation.get
../apps/[appId]/activate.post
../apps/[appId]/deactivate.post

<!-- Auth guards used -->
admin-settings.spec.ts: requireAdminSession (named import from ../../utils/auth)
community-settings.spec.ts: requireSession (named import from ../../utils/auth)

<!-- DB chain mock helper — typed version (post plan 03 lint fixes) -->
```typescript
function mockDbChain(returnValue: unknown = []) {
  const chain: Record<string, unknown> = {};
  chain["select"] = vi.fn().mockReturnValue(chain);
  chain["from"] = vi.fn().mockReturnValue(chain);
  chain["where"] = vi.fn().mockReturnValue(chain);
  chain["limit"] = vi.fn().mockReturnValue(chain);
  chain["offset"] = vi.fn().mockReturnValue(chain);
  chain["orderBy"] = vi.fn().mockReturnValue(chain);
  chain["innerJoin"] = vi.fn().mockReturnValue(chain);
  chain["insert"] = vi.fn().mockReturnValue(chain);
  chain["values"] = vi.fn().mockReturnValue(chain);
  chain["returning"] = vi.fn().mockReturnValue(chain);
  chain["update"] = vi.fn().mockReturnValue(chain);
  chain["set"] = vi.fn().mockReturnValue(chain);
  chain["delete"] = vi.fn().mockReturnValue(chain);
  chain["then"] = (resolve: (v: unknown) => unknown) => resolve(returnValue);
  return chain;
}
```

<!-- Admin-settings specific module mocks (from PATTERNS.md) -->
```typescript
vi.mock("../../utils/auth", () => ({
  requireAdminSession: vi.fn(),
}));

vi.mock("../../utils/db", () => ({
  getDb: vi.fn(),
}));

vi.mock("../../utils/community-settings", () => ({
  COMMUNITY_SETTINGS_SINGLETON_ID: "singleton",
  loadCommunitySettingsLocale: vi.fn().mockResolvedValue("en"),
}));

vi.mock("@guildora/shared", () => ({
  communitySettings: {
    id: "id",
    communityName: "community_name",
    discordInviteCode: "discord_invite_code",
    defaultLocale: "default_locale",
    displayNameTemplate: "display_name_template",
  },
  theme: { id: "id", primaryColor: "primary_color" },
  membershipSettings: { id: "id" },
  moderationRights: { id: "id" },
}));
```

<!-- Community-settings specific module mocks (from PATTERNS.md) -->
```typescript
vi.mock("../../utils/auth", () => ({
  requireSession: vi.fn(),
}));

vi.mock("../../utils/db", () => ({
  getDb: vi.fn(),
}));

vi.mock("../../utils/community-settings", () => ({
  loadDisplayNameTemplate: vi.fn().mockResolvedValue([]),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create admin-settings.spec.ts — 5 admin settings route groups</name>
  <files>apps/hub/server/api/__tests__/admin-settings.spec.ts</files>
  <read_first>
    - /home/andreas/workspace/guildora/platform/apps/hub/server/api/admin/community-settings.get.ts (verify exact @guildora/shared symbols used, return shape, any extra utilities imported)
    - /home/andreas/workspace/guildora/platform/apps/hub/server/api/admin/theme.get.ts (verify imports)
    - /home/andreas/workspace/guildora/platform/apps/hub/server/api/admin/membership-settings.get.ts (verify imports)
    - /home/andreas/workspace/guildora/platform/apps/hub/server/api/__tests__/admin-platforms.spec.ts (copy exact beforeEach/afterEach structure)
  </read_first>
  <behavior>
    - Every admin settings route rejects non-admin requests (403)
    - Every admin settings route rejects unauthenticated requests (401)
    - GET /api/admin/community-settings returns an object with communityName and defaultLocale for admin (200)
    - GET /api/admin/theme returns theme data for admin (200)
    - GET /api/admin/membership-settings returns membership settings for admin (200)
    - GET /api/admin/moderation-rights returns moderation rights for admin (200)
    - GET /api/admin/discord-roles returns discord roles list for admin (200)
  </behavior>
  <action>
Create `/home/andreas/workspace/guildora/platform/apps/hub/server/api/__tests__/admin-settings.spec.ts`.

Before writing, read `apps/hub/server/api/admin/community-settings.get.ts` to identify:
1. Exact `@guildora/shared` symbols imported (for vi.mock)
2. Extra utility imports (`community-settings`, `locale-preference`, etc.)
3. Return shape (what properties the response has)

Then read `apps/hub/server/api/admin/theme.get.ts`, `membership-settings.get.ts`, `moderation-rights.get.ts`, `discord-roles.get.ts` to identify additional shared symbols or utilities used.

Structure the spec file as follows (adjust vi.mock contents based on what the handlers actually import):

```typescript
import { describe, expect, it, vi, beforeEach, afterEach } from "vitest";
import {
  buildSession,
  createMockEvent,
  stubNuxtAutoImports,
  cleanupAutoImportStubs,
} from "../../utils/__tests__/test-helpers";

let mocks: ReturnType<typeof stubNuxtAutoImports>;

// ─── Module mocks ────────────────────────────────────────────────────────────

vi.mock("../../utils/auth", () => ({
  requireAdminSession: vi.fn(),
}));

vi.mock("../../utils/db", () => ({
  getDb: vi.fn(),
}));

// Add mocks for each utility the handlers import (determined by reading handlers in read_first)
// Example stubs (update based on actual imports found):
vi.mock("../../utils/community-settings", () => ({
  COMMUNITY_SETTINGS_SINGLETON_ID: "singleton",
  loadCommunitySettingsLocale: vi.fn().mockResolvedValue("en"),
}));

vi.mock("@guildora/shared", () => ({
  communitySettings: { id: "id", communityName: "community_name", defaultLocale: "default_locale", discordInviteCode: "discord_invite_code", displayNameTemplate: "display_name_template" },
  // Add other table symbols as needed based on handler imports
}));

// ─── DB chain helper ─────────────────────────────────────────────────────────

function mockDbChain(returnValue: unknown = []) {
  const chain: Record<string, unknown> = {};
  chain["select"] = vi.fn().mockReturnValue(chain);
  chain["from"] = vi.fn().mockReturnValue(chain);
  chain["where"] = vi.fn().mockReturnValue(chain);
  chain["limit"] = vi.fn().mockReturnValue(chain);
  chain["offset"] = vi.fn().mockReturnValue(chain);
  chain["orderBy"] = vi.fn().mockReturnValue(chain);
  chain["insert"] = vi.fn().mockReturnValue(chain);
  chain["values"] = vi.fn().mockReturnValue(chain);
  chain["returning"] = vi.fn().mockReturnValue(chain);
  chain["update"] = vi.fn().mockReturnValue(chain);
  chain["set"] = vi.fn().mockReturnValue(chain);
  chain["then"] = (resolve: (v: unknown) => unknown) => resolve(returnValue);
  return chain;
}

// ─── Auth rejection helpers ───────────────────────────────────────────────────

async function mockRequire401() {
  const { requireAdminSession } = await import("../../utils/auth");
  vi.mocked(requireAdminSession).mockRejectedValue(
    Object.assign(new Error("Authentication required."), { statusCode: 401 })
  );
}

async function mockRequire403() {
  const { requireAdminSession } = await import("../../utils/auth");
  vi.mocked(requireAdminSession).mockRejectedValue(
    Object.assign(new Error("Forbidden."), { statusCode: 403 })
  );
}

async function mockRequireOk() {
  const { requireAdminSession } = await import("../../utils/auth");
  vi.mocked(requireAdminSession).mockResolvedValue(
    buildSession("admin") as ReturnType<typeof buildSession>
  );
}

// ─── Setup ────────────────────────────────────────────────────────────────────

beforeEach(() => {
  mocks = stubNuxtAutoImports();
  vi.stubGlobal("getQuery", vi.fn().mockReturnValue({}));
  vi.stubGlobal("readBody", vi.fn());
});

afterEach(() => {
  cleanupAutoImportStubs();
  vi.resetModules();
  vi.clearAllMocks();
});

// ─── GET /api/admin/community-settings ────────────────────────────────────────

describe("GET /api/admin/community-settings", () => {
  async function importHandler() {
    return (await import("../admin/community-settings.get")).default;
  }

  it("rejects unauthenticated requests (401)", async () => {
    await mockRequire401();
    const handler = await importHandler();
    await expect(handler(createMockEvent({ method: "GET" }))).rejects.toThrow();
  });

  it("requires admin role (403 for non-admin)", async () => {
    await mockRequire403();
    const handler = await importHandler();
    await expect(handler(createMockEvent({ method: "GET" }))).rejects.toThrow();
  });

  it("returns community settings for admin (200)", async () => {
    await mockRequireOk();
    const { getDb } = await import("../../utils/db");
    vi.mocked(getDb).mockReturnValue(mockDbChain([{
      communityName: "Test Guild",
      discordInviteCode: "abc123",
      defaultLocale: "en",
      displayNameTemplate: [],
    }]) as ReturnType<typeof getDb>);
    const handler = await importHandler();
    const result = await handler(createMockEvent({ method: "GET" }));
    expect(result).toBeDefined();
  });
});

// ─── GET /api/admin/theme ────────────────────────────────────────────────────

describe("GET /api/admin/theme", () => {
  async function importHandler() {
    return (await import("../admin/theme.get")).default;
  }

  it("rejects unauthenticated requests (401)", async () => {
    await mockRequire401();
    const handler = await importHandler();
    await expect(handler(createMockEvent({ method: "GET" }))).rejects.toThrow();
  });

  it("requires admin role (403 for non-admin)", async () => {
    await mockRequire403();
    const handler = await importHandler();
    await expect(handler(createMockEvent({ method: "GET" }))).rejects.toThrow();
  });
});

// ─── GET /api/admin/membership-settings ──────────────────────────────────────

describe("GET /api/admin/membership-settings", () => {
  async function importHandler() {
    return (await import("../admin/membership-settings.get")).default;
  }

  it("rejects unauthenticated requests (401)", async () => {
    await mockRequire401();
    const handler = await importHandler();
    await expect(handler(createMockEvent({ method: "GET" }))).rejects.toThrow();
  });

  it("requires admin role (403 for non-admin)", async () => {
    await mockRequire403();
    const handler = await importHandler();
    await expect(handler(createMockEvent({ method: "GET" }))).rejects.toThrow();
  });
});

// ─── GET /api/admin/moderation-rights ────────────────────────────────────────

describe("GET /api/admin/moderation-rights", () => {
  async function importHandler() {
    return (await import("../admin/moderation-rights.get")).default;
  }

  it("rejects unauthenticated requests (401)", async () => {
    await mockRequire401();
    const handler = await importHandler();
    await expect(handler(createMockEvent({ method: "GET" }))).rejects.toThrow();
  });

  it("requires admin role (403 for non-admin)", async () => {
    await mockRequire403();
    const handler = await importHandler();
    await expect(handler(createMockEvent({ method: "GET" }))).rejects.toThrow();
  });
});

// ─── GET /api/admin/discord-roles ────────────────────────────────────────────

describe("GET /api/admin/discord-roles", () => {
  async function importHandler() {
    return (await import("../admin/discord-roles.get")).default;
  }

  it("rejects unauthenticated requests (401)", async () => {
    await mockRequire401();
    const handler = await importHandler();
    await expect(handler(createMockEvent({ method: "GET" }))).rejects.toThrow();
  });

  it("requires admin role (403 for non-admin)", async () => {
    await mockRequire403();
    const handler = await importHandler();
    await expect(handler(createMockEvent({ method: "GET" }))).rejects.toThrow();
  });
});
```

If the PUT routes have a different response shape or additional body parsing, add describe blocks for them following the same 401/403 pattern. The GET routes are the minimum required by D-05 (auth-check + happy-path).

Adjust the vi.mock for @guildora/shared based on what you find in the handler files — only mock symbols that are actually imported by the handlers being tested.
  </action>
  <acceptance_criteria>
    - File exists at `/home/andreas/workspace/guildora/platform/apps/hub/server/api/__tests__/admin-settings.spec.ts`
    - File contains `vi.mock("../../utils/auth", () => ({ requireAdminSession: vi.fn() }))`
    - File contains at least 5 `describe(` blocks (one per admin settings route group)
    - File contains `vi.resetModules()` in `afterEach`
    - `cd /home/andreas/workspace/guildora/platform && pnpm --filter @guildora/hub test server/api/__tests__/admin-settings.spec.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <verify>
    <automated>
      cd /home/andreas/workspace/guildora/platform && pnpm --filter @guildora/hub test server/api/__tests__/admin-settings.spec.ts 2>&1 | tail -10
    </automated>
  </verify>
  <done>admin-settings.spec.ts created with describe blocks for all 5 admin settings route groups. 401 and 403 tests pass. community-settings GET also has a 200 happy-path test. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create community-settings.spec.ts — display-name-template and apps routes</name>
  <files>apps/hub/server/api/__tests__/community-settings.spec.ts</files>
  <read_first>
    - /home/andreas/workspace/guildora/platform/apps/hub/server/api/community-settings/display-name-template.get.ts (verify guard, imports, return shape)
    - /home/andreas/workspace/guildora/platform/apps/hub/server/api/apps/index.get.ts (verify guard, imports)
    - /home/andreas/workspace/guildora/platform/apps/hub/server/api/apps/navigation.get.ts (verify guard, imports)
    - /home/andreas/workspace/guildora/platform/apps/hub/server/api/__tests__/member-profile.spec.ts (role-match analog — requireSession pattern)
  </read_first>
  <behavior>
    - GET /api/community-settings/display-name-template rejects unauthenticated (401)
    - GET /api/community-settings/display-name-template returns display name template array for authenticated user (200)
    - GET /api/apps rejects unauthenticated requests (401), returns apps for authenticated (200)
    - GET /api/apps/navigation rejects unauthenticated requests (401), returns navigation for authenticated (200)
    - POST /api/apps/[appId]/activate rejects unauthenticated (401) and non-admin (403)
    - POST /api/apps/[appId]/deactivate rejects unauthenticated (401) and non-admin (403)
  </behavior>
  <action>
Create `/home/andreas/workspace/guildora/platform/apps/hub/server/api/__tests__/community-settings.spec.ts`.

Before writing, read `apps/hub/server/api/community-settings/display-name-template.get.ts` to determine:
1. Which guard it uses (requireSession vs requireUserSession)
2. Which `community-settings` utilities it calls
3. What the return shape is

Read `apps/hub/server/api/apps/index.get.ts` and `apps/navigation.get.ts` to determine their guards.
Read `apps/hub/server/api/apps/[appId]/activate.post.ts` and `deactivate.post.ts` to determine their guards (may require admin session, not just session).

Structure based on what you find, following this pattern for display-name-template:

```typescript
import { describe, expect, it, vi, beforeEach, afterEach } from "vitest";
import {
  buildSession,
  createMockEvent,
  stubNuxtAutoImports,
  cleanupAutoImportStubs,
} from "../../utils/__tests__/test-helpers";

let mocks: ReturnType<typeof stubNuxtAutoImports>;

// ─── Module mocks ─────────────────────────────────────────────────────────────
// Adjust based on what you find in the handler files

vi.mock("../../utils/auth", () => ({
  requireSession: vi.fn(),
  requireAdminSession: vi.fn(),  // for activate/deactivate if they use admin guard
}));

vi.mock("../../utils/db", () => ({
  getDb: vi.fn(),
}));

vi.mock("../../utils/community-settings", () => ({
  loadDisplayNameTemplate: vi.fn().mockResolvedValue([]),
}));

// ─── DB chain helper ──────────────────────────────────────────────────────────

function mockDbChain(returnValue: unknown = []) {
  const chain: Record<string, unknown> = {};
  chain["select"] = vi.fn().mockReturnValue(chain);
  chain["from"] = vi.fn().mockReturnValue(chain);
  chain["where"] = vi.fn().mockReturnValue(chain);
  chain["then"] = (resolve: (v: unknown) => unknown) => resolve(returnValue);
  return chain;
}

// ─── Setup ────────────────────────────────────────────────────────────────────

beforeEach(() => {
  mocks = stubNuxtAutoImports();
  vi.stubGlobal("getRouterParam", vi.fn());
});

afterEach(() => {
  cleanupAutoImportStubs();
  vi.resetModules();
  vi.clearAllMocks();
});

// ─── GET /api/community-settings/display-name-template ───────────────────────

describe("GET /api/community-settings/display-name-template", () => {
  async function importHandler() {
    return (await import("../community-settings/display-name-template.get")).default;
  }

  it("rejects unauthenticated requests (401)", async () => {
    const { requireSession } = await import("../../utils/auth");
    vi.mocked(requireSession).mockRejectedValue(
      Object.assign(new Error("Authentication required."), { statusCode: 401 })
    );
    const handler = await importHandler();
    await expect(handler(createMockEvent({ method: "GET" }))).rejects.toThrow();
  });

  it("returns display name template for authenticated user (200)", async () => {
    const { requireSession } = await import("../../utils/auth");
    vi.mocked(requireSession).mockResolvedValue(
      buildSession("user") as ReturnType<typeof buildSession>
    );
    const { getDb } = await import("../../utils/db");
    vi.mocked(getDb).mockReturnValue(mockDbChain([]) as ReturnType<typeof getDb>);
    const handler = await importHandler();
    const result = await handler(createMockEvent({ method: "GET" }));
    expect(result).toHaveProperty("displayNameTemplate");
  });
});
```

Continue with describe blocks for:
- `GET /api/apps` — 401 test + 200 happy path
- `GET /api/apps/navigation` — 401 test
- `POST /api/apps/[appId]/activate` — 401 test + 403 test (if uses admin guard)
- `POST /api/apps/[appId]/deactivate` — 401 test + 403 test

For activate/deactivate: set `vi.stubGlobal("getRouterParam", vi.fn().mockReturnValue("test-app-id"))` in beforeEach if they use `getRouterParam`.

If the apps/index.get or apps/navigation.get handlers use different guards (check the actual files), update the vi.mock accordingly. For `apps/[appId]/activate.post` and `deactivate.post`, if they use requireAdminSession, add it to the vi.mock and use the 403 pattern.
  </action>
  <acceptance_criteria>
    - File exists at `/home/andreas/workspace/guildora/platform/apps/hub/server/api/__tests__/community-settings.spec.ts`
    - File contains `describe("GET /api/community-settings/display-name-template"`
    - File contains `vi.resetModules()` in `afterEach`
    - `cd /home/andreas/workspace/guildora/platform && pnpm --filter @guildora/hub test server/api/__tests__/community-settings.spec.ts` exits 0 with all tests passing
    - The display-name-template route has a 401 test AND a 200 happy-path test
  </acceptance_criteria>
  <verify>
    <automated>
      cd /home/andreas/workspace/guildora/platform && pnpm --filter @guildora/hub test server/api/__tests__/community-settings.spec.ts 2>&1 | tail -10
    </automated>
  </verify>
  <done>community-settings.spec.ts created covering display-name-template (401 + 200), apps index (401), apps navigation (401), activate (401/403), deactivate (401/403). All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated request → admin settings routes | Tests verify requireAdminSession blocks access |
| authenticated non-admin → admin settings routes | Tests verify 403 for insufficient role |
| unauthenticated request → community-settings route | Tests verify requireSession blocks access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Elevation of Privilege | All 5 admin settings route groups (`GET/PUT /api/admin/community-settings`, theme, membership-settings, moderation-rights, discord-roles) | mitigate | Tests verify that `requireAdminSession` is called and rejects non-admin (403) and unauthenticated (401) requests. If any auth test fails, the route is unguarded — must be fixed before merge. |
| T-05-13 | Elevation of Privilege | `GET /api/community-settings/display-name-template` | mitigate | Tests verify `requireSession` (any authenticated user) blocks unauthenticated access. This is intentionally a lower gate than admin — any logged-in member can see display name template. |
| T-05-14 | Elevation of Privilege | `POST /api/apps/[appId]/activate` and `deactivate` | mitigate | If activate/deactivate use `requireAdminSession`, the 403 test confirms non-admins cannot toggle app state. Verify guard level by reading the handler before writing the test. |
</threat_model>

<verification>
```bash
cd /home/andreas/workspace/guildora/platform
pnpm --filter @guildora/hub test server/api/__tests__/admin-settings.spec.ts      # all pass
pnpm --filter @guildora/hub test server/api/__tests__/community-settings.spec.ts  # all pass
pnpm --filter @guildora/hub test                                                    # full suite 291+ tests
```
</verification>

<success_criteria>
- `admin-settings.spec.ts` created: 5+ describe blocks covering admin settings routes, 401/403/200 tests
- `community-settings.spec.ts` created: display-name-template 401+200, apps route 401, activate/deactivate 401/403
- Both files follow `vi.mock("../../utils/auth")` + `vi.resetModules()` in afterEach pattern
- `pnpm --filter @guildora/hub test` exits 0 with all tests passing (4 new spec files + original 291 tests)
- Files committed to git
</success_criteria>

<output>
After completion, create `/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-06-SUMMARY.md`
</output>
