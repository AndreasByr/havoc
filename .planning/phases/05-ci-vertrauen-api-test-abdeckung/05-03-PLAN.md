---
phase: 05-ci-vertrauen-api-test-abdeckung
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - apps/hub/app/components/applications/flow-builder/SimpleFormField.vue
  - apps/hub/app/pages/landing/editor.vue
  - apps/hub/app/pages/landing/footer.vue
  - apps/hub/app/pages/landing/settings.vue
  - apps/hub/server/api/**/*.ts
  - apps/hub/server/utils/**/*.ts
  - apps/hub/app/composables/**/*.ts
  - apps/hub/server/api/__tests__/**/*.ts
autonomous: true
requirements:
  - CI-02

must_haves:
  truths:
    - "`pnpm --filter @guildora/hub lint` exits 0 (zero errors)"
    - "All 257 lint errors are fixed in source — no file-level eslint-disable comments added"
    - "The 75 html-self-closing warnings are auto-fixed or suppressed at line level"
    - "All existing 291 hub unit tests still pass after the lint fixes"
  artifacts:
    - path: "apps/hub/server/api/"
      provides: "Lint-clean production API routes"
      min_lines: 0
    - path: "apps/hub/app/components/applications/flow-builder/SimpleFormField.vue"
      provides: "Fixed vue/no-mutating-props — local ref replaces direct prop mutation"
      contains: "const localValue = ref("
    - path: "apps/hub/app/pages/landing/editor.vue"
      provides: "Fixed vue/no-v-text-v-html-on-component — v-html on native element"
      contains: ""
  key_links:
    - from: "apps/hub/server/api/"
      to: "pnpm --filter @guildora/hub lint"
      via: "lint must exit 0 before Plan 04 (ESLint security plugins) can run"
      pattern: "exit 0"
---

<objective>
Fix all 257 existing ESLint errors in `@guildora/hub` so that `pnpm --filter @guildora/hub lint` exits 0. This clears the lint debt accumulated before Phase 5, enabling Lint to become a blocking CI gate in Plan 07.

Purpose: Per D-01 — all errors are fixed (not suppressed with eslint-disable). This is a prerequisite for Plan 04 (security plugins) and Plan 07 (remove continue-on-error from CI). Per D-03 — no file-level `/* eslint-disable */` comments may be added.

Output: All 82 affected files in `apps/hub/` lint-clean. `pnpm --filter @guildora/hub lint` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/andreas/workspace/guildora/platform/.planning/STATE.md
@/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-RESEARCH.md
@/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-PATTERNS.md

<interfaces>
<!-- Error categories and fix strategies — from verified lint run -->

| Rule | Count | Fix Strategy |
|------|-------|-------------|
| `@typescript-eslint/no-explicit-any` | 149 | Production files: add explicit types. Test files: replace `any` with `Record<string, unknown>` or typed generics; `Function` → `(...args: unknown[]) => unknown` |
| `@typescript-eslint/no-unused-vars` | 51 | Remove unused imports and variables |
| `@typescript-eslint/unified-signatures` | 24 | Collapse Vue emit overloads into union: `emit(event: 'a' | 'b', payload: PayloadA | PayloadB)` |
| `import/no-duplicates` | 16 | Merge two import statements from the same module into one |
| `@typescript-eslint/no-dynamic-delete` | 5 | Replace `delete obj[key]` with object rest: `const { [key]: _, ...rest } = obj; obj = rest` |
| `vue/no-mutating-props` | 1 | `SimpleFormField.vue:108` — extract prop to local `ref`: `const localValue = ref(props.value); watch(() => props.value, v => localValue.value = v)` |
| `vue/no-v-text-v-html-on-component` | 1 | `LandingPreview.vue:74` — move v-html from component to a native `<div>` or `<span>` wrapper inside the component's template |

DB chain mock helper in test files — `chain.then = (resolve: Function) => resolve(returnValue)` must become `chain.then = (resolve: (v: unknown) => unknown) => resolve(returnValue)`.

<!-- Self-closing warnings: 75× vue/html-self-closing — auto-fixable -->
Run: `pnpm --filter @guildora/hub lint --fix` to auto-fix all 75 self-closing warnings first, then address the 257 errors manually.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run auto-fix for self-closing warnings and triage remaining errors</name>
  <files>apps/hub/app/**/*.vue</files>
  <read_first>
    - Run `cd /home/andreas/workspace/guildora/platform && pnpm --filter @guildora/hub lint 2>&1 | head -100` to see the current first 100 error lines and identify the exact files
    - Run `cd /home/andreas/workspace/guildora/platform && pnpm --filter @guildora/hub lint 2>&1 | grep "error" | head -50` to get a clear list of error locations
  </read_first>
  <action>
Step 1 — Auto-fix the 75 `vue/html-self-closing` warnings:
```bash
cd /home/andreas/workspace/guildora/platform
pnpm --filter @guildora/hub lint --fix
```
This fixes self-closing tags automatically. Re-run `pnpm --filter @guildora/hub lint 2>&1 | grep "error"` to confirm warnings are gone and get the remaining error list.

Step 2 — Fix all remaining errors by category. Work through the error output systematically:

**`@typescript-eslint/no-explicit-any` (149 errors):**
- In test spec files (`*.spec.ts`): Find the `chain.then = (resolve: Function) =>` pattern in mock chain builders and replace with `chain.then = (resolve: (v: unknown) => unknown) =>`. For other `any` in test mocks, use `Record<string, unknown>` for objects or `unknown[]` for arrays.
- In `test-helpers.ts`: The `createError: vi.fn((opts: { statusCode: number; statusMessage: string }) => {...})` callback typing may have `any` — replace with proper types.
- In production server files: Add proper TypeScript types to function parameters and return values.
- Do NOT add `// eslint-disable-next-line @typescript-eslint/no-explicit-any` — fix the type (D-01).

**`@typescript-eslint/no-unused-vars` (51 errors):**
- Remove unused import statements.
- Remove unused local variables.
- If a variable is genuinely needed but flagged (e.g., destructuring for side effects), prefix with `_`: `const [_x, y] = arr`.
- Do NOT add eslint-disable comments — remove the dead code.

**`@typescript-eslint/unified-signatures` (24 errors):**
- Located in flow-builder Vue components with multiple emit overloads.
- Combine: `emit(event: 'update', value: string): void; emit(event: 'close'): void;` → `emit(event: 'update' | 'close', value?: string): void`
- Or use defineEmits generic form in Vue 3: `const emit = defineEmits<{ update: [value: string]; close: [] }>()`

**`import/no-duplicates` (16 errors):**
- Find files with two separate `import { X } from '@guildora/shared'` statements.
- Merge them into one: `import { X, Y, Z } from '@guildora/shared'`
- Files known to have this: `application-archive.ts`, `application-flows.ts`, `membership-sync.ts` (and others — identify from lint output)

**`@typescript-eslint/no-dynamic-delete` (5 errors):**
- Replace `delete obj[key]` with destructuring rest:
  ```typescript
  // Before:
  delete obj[key]
  // After (for object reference):
  const { [key]: _, ...rest } = obj
  Object.assign(obj, rest) // if mutation needed, or reassign the variable
  ```
  Or use a Map if the data structure allows it.

**`vue/no-mutating-props` (1 error — `SimpleFormField.vue:108`):**
- Read `SimpleFormField.vue` to see what prop is mutated.
- Replace direct prop mutation with a local ref:
  ```typescript
  const localValue = ref(props.value)
  watch(() => props.value, (v) => { localValue.value = v })
  ```
  Then use `localValue` instead of `props.value` in the template and handlers.

**`vue/no-v-text-v-html-on-component` (1 error — `LandingPreview.vue:74`):**
- Read `LandingPreview.vue` to see which component has `v-html`.
- Move `v-html` from the component to a native HTML element inside the component, or wrap in a `<div v-html="...">` where the component was.

Step 3 — After all fixes, verify:
```bash
cd /home/andreas/workspace/guildora/platform
pnpm --filter @guildora/hub lint
```
Must exit 0. If errors remain, fix them before marking this task done.

Step 4 — Verify tests still pass:
```bash
cd /home/andreas/workspace/guildora/platform
pnpm --filter @guildora/hub test
```
Must exit 0 with all 291 tests passing.
  </action>
  <acceptance_criteria>
    - `cd /home/andreas/workspace/guildora/platform && pnpm --filter @guildora/hub lint` exits 0
    - `pnpm --filter @guildora/hub lint 2>&1 | grep " error "` returns no output (zero errors)
    - `grep -r "/\* eslint-disable \*/" /home/andreas/workspace/guildora/platform/apps/hub/` returns no output (no file-level disables added)
    - `pnpm --filter @guildora/hub test` exits 0 with all 291 tests passing
    - `grep -n "BOT_INTERNAL_TOKEN!" /home/andreas/workspace/guildora/platform/apps/matrix-bot/src/index.ts` still present (prior plan's fix not broken)
  </acceptance_criteria>
  <verify>
    <automated>
      cd /home/andreas/workspace/guildora/platform && pnpm --filter @guildora/hub lint 2>&1 | tail -5 && pnpm --filter @guildora/hub test 2>&1 | tail -5
    </automated>
  </verify>
  <done>`pnpm --filter @guildora/hub lint` exits 0. `pnpm --filter @guildora/hub test` exits 0 with 291+ tests passing. No file-level eslint-disable comments were added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lint rules → production code | ESLint enforces type safety and correctness; fixing errors improves security posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | `vue/no-dynamic-delete` fixes in production code | mitigate | Replace `delete obj[key]` with object rest spread — avoids prototype pollution via computed property deletion. Each fix must be verified with the lint rule passing. |
| T-05-04 | Tampering | `vue/no-mutating-props` fix in `SimpleFormField.vue` | mitigate | Prop mutation can cause unpredictable state in Vue's reactivity system. Local ref pattern is the correct isolation. Test that the component still emits updates correctly after fix. |
| T-05-05 | Tampering | `vue/no-v-text-v-html-on-component` fix in `LandingPreview.vue` | mitigate | Moving `v-html` to a native element does not change the XSS risk, but the fix must preserve the `isomorphic-dompurify` sanitization that already wraps the value (verify the sanitization call is still in the data path). |
</threat_model>

<verification>
```bash
cd /home/andreas/workspace/guildora/platform
pnpm --filter @guildora/hub lint          # exit 0, zero errors
pnpm --filter @guildora/hub test          # exit 0, 291+ tests
pnpm --filter @guildora/hub lint 2>&1 | grep " error "   # no output
grep -r "/\* eslint-disable \*/" apps/hub/  # no output
```
</verification>

<success_criteria>
- `pnpm --filter @guildora/hub lint` exits 0 (zero errors, any remaining warnings are acceptable)
- All 257 pre-existing lint errors fixed in source code (not suppressed with disable comments)
- `pnpm --filter @guildora/hub test` exits 0 with all 291 tests still passing
- No file-level `/* eslint-disable */` comments introduced
- Changes committed to git
</success_criteria>

<output>
After completion, create `/home/andreas/workspace/guildora/platform/.planning/phases/05-ci-vertrauen-api-test-abdeckung/05-03-SUMMARY.md`
</output>
