---
phase: 04-supply-chain-secrets
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/package.json
  - platform/packages/shared/package.json
  - platform/apps/hub/package.json
  - platform/apps/bot/package.json
  - .planning/research/04-overrides-audit.md
  - .planning/research/04-audit-accepted-risks.md
autonomous: true
requirements:
  - SEC-07

must_haves:
  truths:
    - "pnpm audit --prod reports no unaccepted High or Critical findings"
    - "All 15 pnpm.overrides entries are documented with CVE number or rationale"
    - "New overrides for defu, lodash, and vite are added to platform/package.json"
    - "drizzle-orm is upgraded to ^0.45.2 in shared, hub, and bot packages"
    - "All remaining unresolvable High/Critical CVEs are documented as accepted risks with justification"
  artifacts:
    - path: ".planning/research/04-overrides-audit.md"
      provides: "Per-entry documentation for all 15 pnpm.overrides entries"
      contains: "serialize-javascript"
    - path: ".planning/research/04-audit-accepted-risks.md"
      provides: "Accepted-risk documentation for CVEs that cannot be fixed upstream"
      contains: "CVE-2025-7783"
    - path: "platform/package.json"
      provides: "Updated pnpm.overrides with new defu/lodash/vite entries"
      contains: "defu"
  key_links:
    - from: "platform/package.json pnpm.overrides"
      to: "transitive dependencies"
      via: "pnpm install"
      pattern: "\"defu\": \">=6.1.5\""
    - from: "platform/packages/shared/package.json"
      to: "drizzle-orm@^0.45.2"
      via: "pnpm install"
      pattern: "drizzle-orm.*0\\.45"
---

<objective>
Fix all resolvable High/Critical vulnerabilities found by `pnpm audit --prod`, document every
`pnpm.overrides` entry with its CVE rationale, and document accepted risks for CVEs that have
no upstream fix.

Purpose: Closes F-12 and SEC-07. The current audit shows 23 vulnerabilities (1 Critical, 5
High). Several are immediately fixable by adding `pnpm.overrides` entries or upgrading
drizzle-orm. The rest are locked to the unmaintained `request` package inside matrix-bot-sdk
and require accepted-risk documentation.

Output:
- `platform/package.json` — three new `pnpm.overrides` entries (defu, lodash, vite)
- `platform/packages/shared/package.json`, `platform/apps/hub/package.json`,
  `platform/apps/bot/package.json` — drizzle-orm bumped to ^0.45.2
- `.planning/research/04-overrides-audit.md` — all 15 overrides documented (D-06, D-07)
- `.planning/research/04-audit-accepted-risks.md` — accepted risk entries for form-data,
  request, tough-cookie CVE chain (D-05)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-supply-chain-secrets/04-CONTEXT.md
@.planning/phases/04-supply-chain-secrets/04-RESEARCH.md

<interfaces>
<!-- Current pnpm.overrides block in platform/package.json (verified 2026-04-18): -->
"pnpm": {
  "overrides": {
    "serialize-javascript": ">=7.0.3",
    "devalue": ">=5.6.4",
    "undici": ">=6.24.0 <8.0.0",
    "flatted": ">=3.4.2",
    "node-forge": ">=1.3.4",
    "picomatch": ">=4.0.4",
    "happy-dom": ">=20.8.9",
    "h3": ">=1.15.9",
    "srvx": ">=0.11.13",
    "postcss": ">=8.4.31",
    "brace-expansion@>=2.0.0 <2.0.3": ">=2.0.3",
    "brace-expansion@>=4.0.0 <5.0.5": ">=5.0.5",
    "yaml": ">=2.8.3",
    "ajv": ">=8.18.0",
    "eslint>ajv": "^6"
  }
}

<!-- Current drizzle-orm versions in affected packages (all declare "^0.44.5"): -->
platform/packages/shared/package.json:  "drizzle-orm": "^0.44.5"
platform/apps/hub/package.json:         "drizzle-orm": "^0.44.5"
platform/apps/bot/package.json:         "drizzle-orm": "^0.44.5"
(The ^0.44.5 range is <0.45.0 — it will NOT pick up the security-patched 0.45.2)

<!-- pnpm audit --prod High/Critical findings (live run 2026-04-18): -->
Critical: form-data CVE-2025-7783 via matrix-bot-sdk > request > form-data < 2.5.4
High:     lodash CVE-2026-4800 — fix: add override "lodash": ">=4.18.0"
High:     defu CVE-2026-35209 — fix: add override "defu": ">=6.1.5"
High:     vite CVE-2026-39364 — fix: add override "vite": ">=7.3.2"
High:     vite CVE-2026-39363 — fix: same as above (same path, same fix)
High:     drizzle-orm CVE-2026-39356 — fix: upgrade ^0.44.5 → ^0.45.2 in 3 package.json files
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new pnpm.overrides entries and upgrade drizzle-orm</name>
  <files>
    platform/package.json,
    platform/packages/shared/package.json,
    platform/apps/hub/package.json,
    platform/apps/bot/package.json
  </files>
  <read_first>
    - platform/package.json (full file — verify exact current overrides block before editing)
    - platform/packages/shared/package.json (verify drizzle-orm version declaration)
    - platform/apps/hub/package.json (verify drizzle-orm version declaration)
    - platform/apps/bot/package.json (verify drizzle-orm version declaration)
  </read_first>
  <action>
**Step 1 — Add three new entries to pnpm.overrides in `platform/package.json`.**

Add the following three entries to the existing `pnpm.overrides` block, preserving all 15
existing entries unchanged:

```json
"defu": ">=6.1.5",
"lodash": ">=4.18.0",
"vite": ">=7.3.2"
```

Insert these before `"eslint>ajv": "^6"` (keep that entry last for readability since it
is a scoped override). The updated block will have 18 entries total.

Reason for each (per D-05):
- `"defu": ">=6.1.5"` — CVE-2026-35209, prototype pollution, 205 transitive paths via @nuxt/kit
- `"lodash": ">=4.18.0"` — CVE-2026-4800, code injection via template, in discord.js and nuxt paths
- `"vite": ">=7.3.2"` — CVE-2026-39364 + CVE-2026-39363, fs.deny bypass + arbitrary file read via
  WebSocket; vite is a production dep of nuxt (via @nuxt/vite-builder), conservative fix is override

**Step 2 — Upgrade drizzle-orm in three package.json files.**

In each of these three files, change `"drizzle-orm": "^0.44.5"` to `"drizzle-orm": "^0.45.2"`:
- `platform/packages/shared/package.json`
- `platform/apps/hub/package.json`
- `platform/apps/bot/package.json`

Rationale: CVE-2026-39356 (SQL injection via improperly escaped SQL identifiers) was fixed
in drizzle-orm@0.45.2. The `^0.44.5` range is bounded by `<0.45.0` and will not pick up the
patched version without this change.

**Step 3 — Run pnpm install to update the lockfile.**

After editing all four package.json files:
```bash
cd /home/andreas/workspace/guildora/platform && pnpm install
```

This resolves the new override constraints and drizzle-orm version into pnpm-lock.yaml. If
pnpm install fails with a conflict, diagnose the drizzle-orm CHANGELOG for 0.44.x → 0.45.x
breaking changes and report in the summary — do NOT force-resolve without understanding.
  </action>
  <verify>
    <automated>cd /home/andreas/workspace/guildora/platform && pnpm audit --prod --audit-level=high 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"defu": ">=6.1.5"' platform/package.json` returns 1 match
    - `grep '"lodash": ">=4.18.0"' platform/package.json` returns 1 match
    - `grep '"vite": ">=7.3.2"' platform/package.json` returns 1 match
    - `grep '"drizzle-orm": "\^0.45.2"' platform/packages/shared/package.json` returns 1 match
    - `grep '"drizzle-orm": "\^0.45.2"' platform/apps/hub/package.json` returns 1 match
    - `grep '"drizzle-orm": "\^0.45.2"' platform/apps/bot/package.json` returns 1 match
    - `cd platform && pnpm install` exits 0 (no unresolvable conflicts)
    - `cd platform && pnpm audit --prod --audit-level=high` exits 0 OR remaining High/Critical findings
      are the form-data/request/tough-cookie chain (matrix-bot-sdk) documented in Task 2
  </acceptance_criteria>
  <done>All resolvable High/Critical CVEs have fixes applied via overrides or direct dependency upgrades; lockfile is updated; pnpm install exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create overrides audit and accepted-risks documentation</name>
  <files>
    .planning/research/04-overrides-audit.md,
    .planning/research/04-audit-accepted-risks.md
  </files>
  <read_first>
    - .planning/phases/04-supply-chain-secrets/04-RESEARCH.md (§pnpm.overrides Current State table and §pnpm audit --prod Current State — contains all verified data needed for both docs)
    - platform/package.json (confirm current 18-entry overrides block after Task 1)
  </read_first>
  <action>
Create two documentation files in `.planning/research/`.

**File 1: `.planning/research/04-overrides-audit.md`**

Document all 18 `pnpm.overrides` entries (15 original + 3 new from Task 1). Per D-06/D-07,
each entry must have: package key, constraint value, CVE number or "no CVE — compatibility
patch", whether upstream has patched, whether override can be removed, status (keep/removable).

Use this exact table format:

```markdown
# pnpm.overrides Audit — Phase 4

**Audited:** 2026-04-18
**Source:** platform/package.json pnpm.overrides block
**Total entries:** 18 (15 pre-existing + 3 added in Phase 4)

| Override Key | Constraint | CVE / Reason | Upstream Patched? | Removable? | Status |
|---|---|---|---|---|---|
| serialize-javascript | >=7.0.3 | XSS via unsafe serialization | yes (7.0.5 latest) | no — transitive consumers may still pull older | keep |
| devalue | >=5.6.4 | compatibility/CVE patch (5.7.1 latest) | yes | no — protect against new installs | keep |
| undici | >=6.24.0 <8.0.0 | multiple CVEs (GHSA-*); upper bound intentional compat guard | yes (8.x patched too) | no — compat guard needed until direct deps update to undici 8 | keep |
| flatted | >=3.4.2 | compatibility patch (3.4.2 is latest) | n/a (latest = required) | removable once all consumers independently updated | keep (no-op but protects future installs) |
| node-forge | >=1.3.4 | CVE patch (1.4.0 latest) | yes (1.3.4+) | no — transitive consumers may still pull older | keep |
| picomatch | >=4.0.4 | compatibility patch (4.0.4 is latest) | n/a (latest = required) | removable once all consumers updated | keep (no-op but protects future installs) |
| happy-dom | >=20.8.9 | CVE/compat patch (20.9.0 latest) | yes | no — protect against new installs pulling old | keep |
| h3 | >=1.15.9 | CVE patch; h3 is core Nuxt dep — removing risks reverting to older h3 | yes (1.15.x maintained) | no — Nuxt may pull older h3 without this | keep |
| srvx | >=0.11.13 | compatibility patch (0.11.15 latest) | n/a (latest = required) | removable once consumers updated | keep |
| postcss | >=8.4.31 | CVE-2023-44270 (line injection, 8.5.10 latest) | yes | no — many tools still pin older postcss | keep |
| brace-expansion@>=2.0.0 <2.0.3 | >=2.0.3 | CVE patch for specific 2.x range (2.1.0 latest) | yes | no — pin protects against specific range | keep |
| brace-expansion@>=4.0.0 <5.0.5 | >=5.0.5 | CVE patch for 4.x range pinned to 5.0.5+ (5.0.5 latest) | yes | no — intentional range pin | keep |
| yaml | >=2.8.3 | CVE/compat patch (2.8.3 is latest) | n/a (latest = required) | removable once consumers updated | keep |
| ajv | >=8.18.0 | CVE/compat patch (8.18.0 is latest) | n/a (latest = required) | removable once consumers updated | keep |
| eslint>ajv | ^6 | ESLint requires ajv v6 internally — scoped to eslint to avoid forcing v6 on others | n/a (intentional compat) | no — ESLint v8/v9 still use ajv v6 internally | keep |
| defu | >=6.1.5 | CVE-2026-35209, prototype pollution via uncontrolled recursion (205 transitive paths) | yes (6.1.5+) | no — many nuxt paths still pull older defu | keep (Phase 4 addition) |
| lodash | >=4.18.0 | CVE-2026-4800, code injection via template function (4.18.1 latest) | yes (4.18.0+) | no — discord.js and nuxt paths still pull older lodash | keep (Phase 4 addition) |
| vite | >=7.3.2 | CVE-2026-39364 + CVE-2026-39363, fs.deny bypass + arbitrary file read via WebSocket | yes (7.3.2+) | assess after nuxt upgrade — if nuxt pulls >=7.3.2 directly, can remove | keep (Phase 4 addition) |
```

**File 2: `.planning/research/04-audit-accepted-risks.md`**

Document the CVEs that cannot be fixed upstream. Per D-05, each entry must have: CVE ID,
package, dependency path, why it cannot be fixed, mitigating controls, review date.

```markdown
# pnpm audit — Accepted Risks — Phase 4

**Audited:** 2026-04-18
**Scope:** pnpm audit --prod (production dependencies only, per D-04)
**Policy:** Per D-05: every High/Critical without upstream fix is documented here with
justification. Medium/Low are noted but not required to be fixed in this phase.

## Accepted: Critical / High

### CVE-2025-7783 — form-data unsafe random boundary (Critical)

- **Package:** form-data < 2.5.4
- **Dependency path:** matrix-bot > matrix-bot-sdk@0.7.1 > request@2.88.2 > form-data@2.3.3
- **Patched version:** form-data >= 2.5.4
- **Why not fixable:** `request@2.88.2` is the entry point. `request` was deprecated in 2020
  and never patched for any CVE. matrix-bot-sdk@0.7.1 depends on `request` and has not
  removed it. A pnpm.overrides entry for `form-data >= 2.5.4` may break the `request@2.88.2`
  interface (form boundary API changed between 2.3.x and 2.5.x).
- **Impact assessment:** This CVE concerns multipart form boundary predictability (boundary
  is pseudo-random but deterministic). matrix-bot uses `request` only for its internal HTTP
  transport to Matrix homeserver — these are JSON-body requests, not multipart form uploads.
  The predictable boundary attack requires an attacker who can intercept and replay specific
  multipart requests; this is not applicable to matrix-bot's actual usage pattern.
- **Mitigating controls:**
  - matrix-bot's internal sync server uses Bearer token auth (Phase 3 hardened: timing-safe)
  - matrix-bot is not internet-facing (Docker internal network only)
  - No multipart form uploads occur in matrix-bot's code paths
- **Review date:** 2026-04-18
- **Re-evaluate when:** matrix-bot-sdk releases a version that drops the `request` dependency,
  or when an upgrade path to matrix-bot-sdk@0.8+ becomes available.

## Noted: Moderate (not required to fix per D-05)

The following 17 Moderate findings are noted. They do not require fixes in this phase per D-05.

| Package | CVE / Advisory | Path | Why Not Fixed |
|---------|----------------|------|---------------|
| request | SSRF | matrix-bot-sdk > request | request is unmaintained; no patched version |
| tough-cookie | prototype pollution | matrix-bot-sdk > request > tough-cookie | same path, unmaintained |
| dompurify | XSS | isomorphic-dompurify@3.7.1 > dompurify@3.3.3 | patched in >=3.4.0; isomorphic-dompurify upgrade scheduled separately |
| hono (multiple) | multiple | mcp-server > @modelcontextprotocol/sdk > hono | Moderate only; mcp-server is optional/internal |
| sanitize-html | XSS | matrix-bot-sdk > sanitize-html@2.x | patched in >=2.17.3; override would conflict with matrix-bot-sdk |
| lodash (prototype) | prototype pollution | same chain as High CVE-2026-4800 | resolved by lodash override added in Task 1 |
| vite (path traversal) | traversal | same chain as High CVE-2026-39363 | resolved by vite override added in Task 1 |
| esbuild | dev server | hub > @nuxt/devtools > esbuild | dev-server-only; does not run in production |
| unhead | advisory | hub > nuxt > unhead | Moderate only; no active exploit path |
| qs | prototype pollution | multiple paths | Moderate only; no active exploit in hub usage |
| vue-template-compiler | advisory | hub > nuxt | Moderate only |
```

Use approximate matches for the moderate table — exact entry count may vary slightly based on
live audit output. The critical and high entries are the required ones per D-05.
  </action>
  <verify>
    <automated>test -f /home/andreas/workspace/guildora/platform/.planning/research/04-overrides-audit.md && test -f /home/andreas/workspace/guildora/platform/.planning/research/04-audit-accepted-risks.md && echo "PASS: both docs exist"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/research/04-overrides-audit.md` exits 0
    - `test -f .planning/research/04-audit-accepted-risks.md` exits 0
    - `grep "serialize-javascript" .planning/research/04-overrides-audit.md` returns 1 match (first of 18 entries)
    - `grep "eslint>ajv" .planning/research/04-overrides-audit.md` returns 1 match (15th pre-existing entry)
    - `grep "defu" .planning/research/04-overrides-audit.md` returns 1+ matches (Phase 4 addition documented)
    - `grep "CVE-2025-7783" .planning/research/04-audit-accepted-risks.md` returns 1 match (Critical accepted risk)
    - `grep "request" .planning/research/04-audit-accepted-risks.md` returns 1+ matches (request chain documented)
    - `wc -l .planning/research/04-overrides-audit.md | awk '{print ($1 >= 25)}'` returns 1 (at least 25 lines — full table)
  </acceptance_criteria>
  <done>Both planning artifacts exist; 04-overrides-audit.md documents all 18 overrides; 04-audit-accepted-risks.md justifies the form-data/request Critical CVE as accepted risk with mitigating controls.</done>
</task>

<task type="auto">
  <name>Task 3: Verify pnpm audit --prod is clean (or fully documented)</name>
  <files></files>
  <read_first>
    - .planning/research/04-audit-accepted-risks.md (just created — confirms which CVEs are accepted)
  </read_first>
  <action>
Run `pnpm audit --prod` and verify the result matches the expected post-fix state.

```bash
cd /home/andreas/workspace/guildora/platform && pnpm audit --prod 2>&1
```

Expected post-fix state:
- lodash (High CVE-2026-4800): RESOLVED by override
- defu (High CVE-2026-35209): RESOLVED by override
- vite (High CVE-2026-39364/39363): RESOLVED by override
- drizzle-orm (High CVE-2026-39356): RESOLVED by direct upgrade to ^0.45.2
- form-data (Critical CVE-2025-7783): DOCUMENTED as accepted risk in 04-audit-accepted-risks.md

If the audit shows any High or Critical findings that are NOT:
- The form-data/request/tough-cookie matrix-bot-sdk chain (documented as accepted)
- Already covered by a new override or upgrade

→ add the additional finding to `04-audit-accepted-risks.md` with justification, OR add
  another override to `platform/package.json` if a simple fix is available.

Do NOT mark the phase as complete if there are unresolvable High/Critical findings without
documentation. Every High/Critical must be either fixed or documented.

After confirming the audit result matches expectation, run the full test suite to verify
drizzle-orm 0.45.2 has no breaking API changes:

```bash
cd /home/andreas/workspace/guildora/platform && pnpm --filter @guildora/shared test run 2>&1 | tail -30
cd /home/andreas/workspace/guildora/platform && pnpm --filter @guildora/hub test run 2>&1 | tail -30
cd /home/andreas/workspace/guildora/platform && pnpm --filter @guildora/bot test run 2>&1 | tail -30
```

If tests fail due to drizzle-orm API changes between 0.44.x and 0.45.x, consult the drizzle-orm
CHANGELOG and fix the affected code before declaring the upgrade complete. Note any API changes
in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/andreas/workspace/guildora/platform && pnpm audit --prod --audit-level=high 2>&1 | grep -E "found [0-9]+ vulnerabilities" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `cd platform && pnpm audit --prod --audit-level=high` either exits 0 (no High/Critical) OR
      every remaining High/Critical finding appears in `.planning/research/04-audit-accepted-risks.md`
    - `pnpm --filter @guildora/shared test run` exits 0 (drizzle-orm 0.45.2 API compatible)
    - `pnpm --filter @guildora/hub test run` exits 0
    - `pnpm --filter @guildora/bot test run` exits 0
    - If any test fails due to drizzle-orm API change: the breaking change is documented and code is fixed
  </acceptance_criteria>
  <done>pnpm audit --prod shows no unaccepted High/Critical findings; full test suite is green with drizzle-orm 0.45.2; Roadmap Phase 4 Success Criterion 2 is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Transitive dependencies may contain CVEs; pnpm.overrides forces patched versions |
| matrix-bot-sdk → internal HTTP | matrix-bot-sdk ships unmaintained `request` package with unpatched CVEs; mitigated by network isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | drizzle-orm < 0.45.2 (CVE-2026-39356: SQL injection via identifier escaping) | mitigate | Upgrade `^0.44.5` → `^0.45.2` in shared, hub, bot package.json (Task 1). Direct fix — no workaround required. |
| T-04-02-02 | Tampering | defu prototype pollution CVE-2026-35209 (205 transitive paths through nuxt/kit) | mitigate | Add `"defu": ">=6.1.5"` override (Task 1). Forces all defu consumers to patched version. |
| T-04-02-03 | Information Disclosure | vite fs.deny bypass CVE-2026-39364/39363 (file read via WebSocket) | mitigate | Add `"vite": ">=7.3.2"` override (Task 1). Conservative fix; vite dev-server may not run in prod builds but override is safe. |
| T-04-02-04 | Elevation of Privilege | lodash code injection via template CVE-2026-4800 | mitigate | Add `"lodash": ">=4.18.0"` override (Task 1). Forces all lodash consumers to patched version. |
| T-04-02-05 | Spoofing | form-data unsafe boundary CVE-2025-7783 (Critical, via matrix-bot-sdk > request) | accept | No upstream fix; matrix-bot does not use multipart form uploads; internal network only; bearer auth hardened in Phase 3. Documented in 04-audit-accepted-risks.md (Task 2). |
| T-04-02-06 | Information Disclosure | undocumented pnpm.overrides — future maintainers cannot determine if overrides are still needed | mitigate | 04-overrides-audit.md documents all 18 entries with CVE/reason and removability assessment (Task 2). Finding: F-12. |
</threat_model>

<verification>
After all three tasks complete:

```bash
# Overrides updated
grep '"defu": ">=6.1.5"' platform/package.json           # 1 match
grep '"lodash": ">=4.18.0"' platform/package.json         # 1 match
grep '"vite": ">=7.3.2"' platform/package.json            # 1 match

# drizzle-orm upgraded
grep '"drizzle-orm": "\^0.45.2"' platform/packages/shared/package.json  # 1 match
grep '"drizzle-orm": "\^0.45.2"' platform/apps/hub/package.json          # 1 match
grep '"drizzle-orm": "\^0.45.2"' platform/apps/bot/package.json          # 1 match

# Documentation exists
test -f .planning/research/04-overrides-audit.md && echo PASS
test -f .planning/research/04-audit-accepted-risks.md && echo PASS

# Audit state
cd platform && pnpm audit --prod --audit-level=high

# Tests green
cd platform && pnpm --filter @guildora/shared test run
cd platform && pnpm --filter @guildora/hub test run
cd platform && pnpm --filter @guildora/bot test run
```
</verification>

<success_criteria>
- `pnpm audit --prod --audit-level=high` exits 0 OR all remaining High/Critical findings are
  the matrix-bot-sdk chain documented in `04-audit-accepted-risks.md`
- All 18 pnpm.overrides entries are documented in `04-overrides-audit.md`
- drizzle-orm is at `^0.45.2` in shared, hub, bot — tests pass with no API regressions
- Roadmap Phase 4 Success Criteria 2 and 3 are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-supply-chain-secrets/04-02-SUMMARY.md`
</output>
