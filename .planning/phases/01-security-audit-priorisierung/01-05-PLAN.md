---
phase: 01-security-audit-priorisierung
plan: 05
type: execute
wave: 5
depends_on:
  - 04
files_modified:
  - .planning/research/01-security-audit.md
  - .planning/phases/01-security-audit-priorisierung/validation.sh
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - SEC-01
tags:
  - security
  - audit
  - finalize
must_haves:
  truths:
    - "Executive-Summary in §1 ist mit echten Finding-Counts pro Severity und Phase gefuellt."
    - "Traceability-Tabelle §10 ist mit Finding-IDs pro SEC-Requirement gefuellt - alle 6 SEC-02..07 sind gedeckt."
    - "Commit-SHA im Header ist mit echtem HEAD-SHA ersetzt (Anti-Staleness)."
    - "validation.sh existiert und deckt alle 13 Checks aus 01-VALIDATION.md ab."
    - "validation.sh laeuft ohne Fehler (Exit 0) gegen das finale Audit-Doc."
    - "REQUIREMENTS.md SEC-01-Zeile ist von Pending auf Done geflippt."
    - "STATE.md ist aktualisiert: completed_phases=1, percent=12, last_activity auf heute, Phase-1-Eintrag in By-Phase-Tabelle."
    - "Git-Tag phase-1-audit-baseline zeigt auf den Commit mit dem finalen Audit."
  artifacts:
    - path: ".planning/research/01-security-audit.md"
      provides: "Vollstaendiges Audit mit Exec-Summary, Traceability-Tabelle ausgefuellt, Commit-SHA im Header"
      contains: "Gesamt-Findings-Zaehlung, Phase-Aufteilung, SEC-02..07 Finding-ID-Listen"
    - path: ".planning/phases/01-security-audit-priorisierung/validation.sh"
      provides: "Bash-Script mit allen 13 Nyquist-Validation-Checks"
      contains: "#!/bin/bash, SC1 Audit-Datum, SC2a-e Pflicht-Felder, SC3a Severity-Order, SC3b Critical/High-Target-Phase, SC4 Deferred, D-13 Kopf-Review, D-15 Operational, TRACE SEC-Requirements, VOL 15-25"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SEC-01-Status-Flip"
      contains: "| SEC-01 | Phase 1 | Done |"
    - path: ".planning/STATE.md"
      provides: "STATE-Aktualisierung nach Phase-1-Abschluss"
      contains: "completed_phases: 1, last_activity Phase 1 completion"
  key_links:
    - from: "validation.sh"
      to: "01-VALIDATION.md Per-Task-Verification-Map (13 Checks)"
      via: "Jeder der 13 Checks hat eine shell-Umsetzung im Script"
      pattern: "check \""
    - from: ".planning/research/01-security-audit.md §10"
      to: ".planning/REQUIREMENTS.md SEC-02..SEC-07"
      via: "Traceability-Tabelle mapped jedes Requirement auf mindestens ein Finding"
      pattern: "^\\| SEC-0[2-7] "
    - from: "git tag phase-1-audit-baseline"
      to: "Audit-Doc header Codebase-Stand"
      via: "SHA im Header == Commit-SHA den der Tag referenziert"
      pattern: "git rev-parse phase-1-audit-baseline"
---

<objective>
Finalisierung der Phase: Exec-Summary und Traceability-Tabelle fuellen, validation.sh erzeugen, full-suite-run, REQUIREMENTS.md SEC-01 auf Done flippen, STATE.md aktualisieren, git-Tag setzen. Diese Wave macht aus dem konsolidierten Audit ein abgeschlossenes, phase-abschluss-reifes Artefakt.

Purpose: Roadmap Phase-1 Success Criteria #3 und #4 brauchen noch die formalen Artefakte (Exec-Summary + Traceability-Tabelle + Deferred-Section validiert + SEC-01 geflippt). Wave 5 liefert diese Artefakte und den Nyquist-Validation-Rahmen aus 01-VALIDATION.md (13 Checks als `validation.sh`).

Output: Audit-Doc ist komplett; validation.sh laeuft gruen; REQUIREMENTS-Traceability-Zeile ist `Done`; STATE.md spiegelt Phase-1-Abschluss; git-Tag markiert Baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-audit-priorisierung/01-CONTEXT.md
@.planning/phases/01-security-audit-priorisierung/01-RESEARCH.md
@.planning/phases/01-security-audit-priorisierung/01-PATTERNS.md
@.planning/phases/01-security-audit-priorisierung/01-VALIDATION.md
@.planning/research/01-security-audit.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
Validation.sh-Skelett (aus 01-PATTERNS.md §Pattern H + §"Content-Mapping", Zeilen ~254-359):

```bash
#!/bin/bash
# Validate .planning/research/01-security-audit.md structural integrity
# Usage: bash .planning/phases/01-security-audit-priorisierung/validation.sh
#
# Exit 0 = all checks pass; Exit 1 = at least one check failed.
# Referenced specs:
#   - .planning/ROADMAP.md §"Phase 1" Success Criteria
#   - .planning/phases/01-security-audit-priorisierung/01-CONTEXT.md §D-06, D-08, D-15, D-16
#   - .planning/phases/01-security-audit-priorisierung/01-VALIDATION.md Per-Task-Verification-Map

DIR="$(cd "$(dirname "$0")/../../.." && pwd)"
cd "$DIR"

AUDIT=".planning/research/01-security-audit.md"
KOPF=".planning/phases/01-security-audit-priorisierung/kopf-review.md"

FAIL=0

check() {
  local label="$1"
  local status="$2"
  if [ "$status" = "PASS" ]; then
    echo "PASS $label"
  else
    echo "FAIL $label"
    FAIL=1
  fi
}

# --- Check 1: SC1 Audit-Datum header present ---
if rg -q '^\*\*Audit-Datum:\*\*' "$AUDIT" 2>/dev/null; then
  check "SC1 Audit-Datum header" PASS
else
  check "SC1 Audit-Datum header" FAIL
fi

# --- Check 2-6: SC2a-e All 5 mandatory meta-fields per finding ---
FINDING_COUNT=$(rg -c '^### \[F-' "$AUDIT" 2>/dev/null || echo 0)
for FIELD_PATTERN in "Severity" "Datei-Pfad\\\(e\\\)" "Current Mitigation" "Fix-Ansatz" "Target Phase"; do
  FIELD_COUNT=$(rg -c "^- \*\*${FIELD_PATTERN}:\*\*" "$AUDIT" 2>/dev/null || echo 0)
  if [ "$FIELD_COUNT" -ge "$FINDING_COUNT" ]; then
    check "SC2 All findings have ${FIELD_PATTERN}" PASS
  else
    check "SC2 All findings have ${FIELD_PATTERN} (found $FIELD_COUNT vs expected >= $FINDING_COUNT)" FAIL
  fi
done

# --- Check 7: SC3a Severity order Critical < High < Medium < Low ---
L4=$(rg -n '^## 4\. Findings' "$AUDIT" 2>/dev/null | head -1 | cut -d: -f1)
L5=$(rg -n '^## 5\. Findings' "$AUDIT" 2>/dev/null | head -1 | cut -d: -f1)
L6=$(rg -n '^## 6\. Findings' "$AUDIT" 2>/dev/null | head -1 | cut -d: -f1)
L7=$(rg -n '^## 7\. Findings' "$AUDIT" 2>/dev/null | head -1 | cut -d: -f1)
if [ -n "$L4" ] && [ -n "$L5" ] && [ -n "$L6" ] && [ -n "$L7" ] && [ "$L4" -lt "$L5" ] && [ "$L5" -lt "$L6" ] && [ "$L6" -lt "$L7" ]; then
  check "SC3a Severity header order" PASS
else
  check "SC3a Severity header order (lines: 4=$L4 5=$L5 6=$L6 7=$L7)" FAIL
fi

# --- Check 8: SC3b No Critical/High with Target Phase v2 or out-of-scope ---
VIOL=$(awk '
  /^### \[F-/ { capture=1; sev=""; tp="" }
  capture && /^- \*\*Severity:\*\*/ { sev=$0 }
  capture && /^- \*\*Target Phase:\*\*/ {
    tp=$0
    if (sev ~ /(Critical|High)/ && tp ~ /(v2|out-of-scope)/) print "FAIL: " sev " -> " tp
    capture=0
  }
' "$AUDIT" 2>/dev/null)
if [ -z "$VIOL" ]; then
  check "SC3b No Critical/High on v2/out-of-scope" PASS
else
  check "SC3b Critical/High found on v2/out-of-scope: $VIOL" FAIL
fi

# --- Check 9: SC4 Deferred section + Warum nicht jetzt per item ---
DEFERRED_COUNT=$(rg -c '^### \[D-' "$AUDIT" 2>/dev/null || echo 0)
WARUM_COUNT=$(rg -c '^- \*\*Warum nicht jetzt:\*\*' "$AUDIT" 2>/dev/null || echo 0)
if rg -q '^## 9\. Deferred / Accepted Risks' "$AUDIT" 2>/dev/null && [ "$WARUM_COUNT" -ge "$DEFERRED_COUNT" ] && [ "$DEFERRED_COUNT" -ge 1 ]; then
  check "SC4 Deferred section + Warum nicht jetzt" PASS
else
  check "SC4 Deferred section (deferred=$DEFERRED_COUNT warum=$WARUM_COUNT)" FAIL
fi

# --- Check 10: D-13 Kopf-Review protocol exists ---
if [ -f "$KOPF" ] || rg -q '^## .*Appendix.*Kopf-Review' "$AUDIT" 2>/dev/null; then
  check "D-13 Kopf-Review protocol present" PASS
else
  check "D-13 Kopf-Review protocol missing" FAIL
fi

# --- Check 11: D-15 Operational findings tagged Class: Operational ---
OP_COUNT=$(rg -c '^### \[OP-' "$AUDIT" 2>/dev/null || echo 0)
OP_CLASS_COUNT=$(rg -A2 '^### \[OP-' "$AUDIT" 2>/dev/null | grep -c '^- \*\*Class:\*\* Operational' || echo 0)
if [ "$OP_COUNT" -ge 1 ] && [ "$OP_CLASS_COUNT" -eq "$OP_COUNT" ]; then
  check "D-15 Operational findings tagged Class: Operational" PASS
else
  check "D-15 Operational tagging (OP=$OP_COUNT ClassTag=$OP_CLASS_COUNT)" FAIL
fi

# --- Check 12: TRACE SEC-02..07 covered ---
TRACE_MISS=""
for req in SEC-02 SEC-03 SEC-04 SEC-05 SEC-06 SEC-07; do
  if ! rg -q "^\| $req " "$AUDIT" 2>/dev/null; then
    TRACE_MISS="$TRACE_MISS $req"
  fi
done
if [ -z "$TRACE_MISS" ]; then
  check "TRACE SEC-02..07 covered" PASS
else
  check "TRACE missing SEC requirements:$TRACE_MISS" FAIL
fi

# --- Check 13: VOL 15-25 F-findings ---
if [ "$FINDING_COUNT" -ge 15 ] && [ "$FINDING_COUNT" -le 25 ]; then
  check "VOL 15-25 findings (actual=$FINDING_COUNT)" PASS
else
  check "VOL 15-25 findings violated (actual=$FINDING_COUNT)" FAIL
fi

# --- Exit ---
if [ "$FAIL" = "0" ]; then
  echo ""
  echo "All 13 structural checks passed."
  exit 0
else
  echo ""
  echo "At least one structural check failed."
  exit 1
fi
```

Regeln (aus 01-PATTERNS.md §Pattern H):
- Shebang `#!/bin/bash` (nicht `#!/usr/bin/env bash`) - konsistent mit existierenden scripts/*.sh.
- Dir-Resolve 3-Ebenen-Hop: Script liegt in `.planning/phases/01-security-audit-priorisierung/`, Ziel ist Platform-Repo-Root.
- Kein `set -e` - wir wollen ALLE Checks sehen, FAIL-Accumulator stattdessen.
- Output: `PASS <label>` / `FAIL <label>` (keine Emojis erforderlich, schlichter Text).

Exec-Summary-Template (aus 01-RESEARCH.md §Audit Document Skeleton Zeilen ~529-540):

```markdown
## 1. Executive Summary

- **Gesamt-Findings:** N (davon X Critical, Y High, Z Medium, W Low + V Operational + U Deferred)
- **Phase-Aufteilung:**
  - Phase 2 (Apps-Plugin-Sandbox): N Findings (davon X Critical)
  - Phase 3 (Auth- & Session-Haertung): N Findings (davon Y High)
  - Phase 4 (Supply-Chain & Secrets): N Findings (davon Z Critical/High)
  - v2 / Deferred: N Findings
- **Sofort-Handlungsempfehlung:** {1 Satz, z.B. "Phase 2 zuerst - Plugin-Sandbox ist einziges Critical."}
- **Nicht abgedeckt (out-of-scope):** Infrastructure (Docker daemon, Cloudflare), Fremd-Repos.
```

STATE.md-Update-Struktur (aus 01-PATTERNS.md §Pattern J + K, Zeilen ~397-439):

Frontmatter:
- `status: planning` (bleibt - Phase 2 startet als naechstes)
- `stopped_at: Phase 1 audit completed`
- `last_updated: "<ISO-8601 UTC>"`
- `last_activity: <DATE> - Phase 1 abgeschlossen: Security-Audit veroeffentlicht (.planning/research/01-security-audit.md)`
- `progress.completed_phases: 1`
- `progress.total_plans: 5` (Phase 1 hatte 5 Plans)
- `progress.completed_plans: 5`
- `progress.percent: 12` (1/8 = 12.5 gerundet)

Body:
- §Current Position: `Phase: 2 of 8 (Apps-Plugin-Sandbox)`, `Status: Audit delivered; ready for Phase 2 planning`, Progress-Bar `[█░░░░░░░░░] 12%`.
- §Performance Metrics: By-Phase-Tabelle eine Zeile `| 1 | 5 | - | - |` (Duration leer wenn keine Daten).
- §Accumulated Context › Decisions: neuer Bullet `- <DATE>: Audit publiziert, N Critical/High Findings mapped an Phase 2/3/4`.
- §Session Continuity: `Stopped at: Phase 1 audit completed`, `Resume file: .planning/phases/02-*/02-CONTEXT.md` (oder leer lassen).

REQUIREMENTS.md-Edit (aus 01-PATTERNS.md §Pattern I):
- Zeile `| SEC-01 | Phase 1 | Pending |` -> `| SEC-01 | Phase 1 | Done |`
- Footer: `*Last updated: <DATE> after Phase 1 audit completion*`
- Keine anderen Zeilen anfassen.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit-Doc -> Phase-2/3/4 | Finale Traceability-Tabelle ist Join-Key fuer Folge-Phasen-Research. Fehler hier fuehren zu fehlgeleitetem Research. |
| validation.sh -> Nyquist Contract | Script ist der einzige automatisierte Qualitaets-Gate zwischen Wave 4 und Phase-Ende. Fehler im Script (False-Positive-Pass) verschleiern Audit-Luecken. |
| Git-Tag -> externe Referenz | `phase-1-audit-baseline` wird von Phase-2-Research als Checkout-Point genutzt. Falscher Tag = stale Code-Referenzen. |
| STATE.md / REQUIREMENTS.md -> GSD-Orchestrator | Diese Files werden vom Orchestrator fuer Fortschritts-Tracking gelesen. Inkonsistenz bricht Workflow-State. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20 | T (Tampering) | validation.sh-Check gibt PASS obwohl Datei fehlt (Script-Bug) | mitigate | Jeder rg-Call hat `2>/dev/null` + Count-Vergleich auf `>= EXPECTED` (nicht nur `> 0`); Test-Run MIT validation.sh als Task-3 vor Tag-Setzung. |
| T-01-21 | R (Repudiation) | git-Tag zeigt auf falschen Commit (Audit-Doc nicht darin) | mitigate | Tag-Setzung ERST nachdem Audit + validation.sh + REQUIREMENTS + STATE committed sind; `git tag phase-1-audit-baseline` ohne Argument nimmt HEAD - sofort `git log phase-1-audit-baseline -1` als Verify. |
| T-01-22 | I (Information Disclosure) | Vor Phase-2-Mitigation wird der Phase-Branch öffentlich gepusht | mitigate | Task 4 setzt nur lokalen Tag, pusht NICHT (kein `git push --tags`). Wenn Andi pushen will, muss er das explizit tun. |
| T-01-23 | S (Spoofing - Fake Completion) | SEC-01 auf Done geflippt aber Audit ist unvollstaendig | mitigate | validation.sh MUSS Exit 0 liefern BEVOR Task 4 (Flip + STATE) laeuft. Wenn validation.sh FAIL, Task 4 wird NICHT ausgefuehrt und Fehler zurueckgegeben. |
| T-01-24 | E (Elevation via Stale-Reference) | Phase-2-Research liest Audit zu Zeit T+N Tage, Code-Stelle hat sich verschoben, Finding-Reference ist broken | mitigate | Header-Zeile `**Codebase-Stand:** git-rev <SHA>` + Tag `phase-1-audit-baseline`. Phase-2-Research checkt tag und kann Drift detektieren. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Exec-Summary, Traceability-Tabelle und Header-SHA im Audit-Doc fuellen</name>
  <files>.planning/research/01-security-audit.md</files>
  <read_first>
    - .planning/research/01-security-audit.md (aktueller Zustand nach Wave 4 - Findings gefuellt, aber §1 und §10 noch Platzhalter).
    - .planning/phases/01-security-audit-priorisierung/01-RESEARCH.md §"Audit Document Skeleton" (Exec-Summary-Template) + §"Audit Document Skeleton" §10 (Traceability-Tabelle-Template).
    - .planning/phases/01-security-audit-priorisierung/01-PATTERNS.md §"Pattern F - Traceability-Tabelle" (Zielform mit Finding-IDs pro SEC-Req).
  </read_first>
  <action>
    1. **Finding-Counts extrahieren** (von Wave 4 konsolidiertem Audit):

       ```bash
       cd /home/andreas/workspace/guildora/platform
       AUDIT=.planning/research/01-security-audit.md

       TOTAL_F=$(rg -c '^### \[F-' $AUDIT)
       CRITICAL=$(rg -A1 '^### \[F-' $AUDIT | grep -c '^- \*\*Severity:\*\* Critical')
       HIGH=$(rg -A1 '^### \[F-' $AUDIT | grep -c '^- \*\*Severity:\*\* High')
       MEDIUM=$(rg -A1 '^### \[F-' $AUDIT | grep -c '^- \*\*Severity:\*\* Medium')
       LOW=$(rg -A1 '^### \[F-' $AUDIT | grep -c '^- \*\*Severity:\*\* Low')
       OP=$(rg -c '^### \[OP-' $AUDIT)
       DEF=$(rg -c '^### \[D-' $AUDIT)

       # Phase-Aufteilung
       P2=$(rg -A5 '^### \[F-' $AUDIT | grep -c '^- \*\*Target Phase:\*\* Phase 2')
       P3=$(rg -A5 '^### \[F-' $AUDIT | grep -c '^- \*\*Target Phase:\*\* Phase 3')
       P4=$(rg -A5 '^### \[F-' $AUDIT | grep -c '^- \*\*Target Phase:\*\* Phase 4')
       V2=$(rg -A5 '^### \[F-' $AUDIT | grep -c '^- \*\*Target Phase:\*\* v2')

       echo "Total F: $TOTAL_F (C:$CRITICAL H:$HIGH M:$MEDIUM L:$LOW) OP:$OP DEF:$DEF"
       echo "Phase 2: $P2, Phase 3: $P3, Phase 4: $P4, v2: $V2"
       ```

    2. **Exec-Summary in §1 schreiben** (Edit-Tool): Ersetze den Blockquote-Platzhalter (`> Wird in Wave 5 (Plan 05) ausgefuellt, sobald Finding-Anzahl pro Severity/Phase feststeht.` plus Bullet-Liste) durch die gefuellte Fassung:

       ```markdown
       ## 1. Executive Summary

       - **Gesamt-Findings:** <TOTAL_F> (davon <CRITICAL> Critical, <HIGH> High, <MEDIUM> Medium, <LOW> Low + <OP> Operational + <DEF> Deferred)
       - **Phase-Aufteilung:**
         - Phase 2 (Apps-Plugin-Sandbox): <P2> Findings (davon <critical-in-p2> Critical)
         - Phase 3 (Auth- & Session-Haertung): <P3> Findings (davon <high-in-p3> High)
         - Phase 4 (Supply-Chain & Secrets): <P4> Findings
         - v2 / Deferred: <V2+DEF> Findings
       - **Sofort-Handlungsempfehlung:** <1 Satz basierend auf Findings. Wenn 1 Critical vorhanden, typischerweise "Phase 2 zuerst - <X> ist einziges Critical.". Wenn mehr als 1 Critical, ordne nach Exploit-Wahrscheinlichkeit.>
       - **Nicht abgedeckt (out-of-scope):** Infrastructure (Docker daemon, Cloudflare-Tunnel, Host-OS), Fremd-Repos (marketplace/guildai/voice-rooms/app-template).
       ```

       WICHTIG: Konkrete Zahlen einsetzen (keine Platzhalter). Sofort-Handlungsempfehlung muss echten Phase-1-Audit-Inhalt reflektieren (nicht generisch).

    3. **Commit-SHA im Header fuellen:**

       ```bash
       SHA=$(git rev-parse HEAD)
       ```

       Edit: `**Codebase-Stand:** git-rev \`<fill-in-wave-5>\`` -> `**Codebase-Stand:** git-rev \`<SHA>\`` (40-Zeichen-hex).
       Zusaetzlich `letzte Aktualisierung: YYYY-MM-DD` -> `letzte Aktualisierung: <heutiges Datum ISO>`.

    4. **Traceability-Tabelle §10 fuellen:**

       Fuer jede Zeile (SEC-02..SEC-07), finde alle F-/D-Findings die `Target Phase: Phase N` haben und zur entsprechenden SEC-Req-Gruppe gehoeren. Mapping-Guide (aus 01-RESEARCH.md §Proposed Finding -> Phase Mapping Zeilen 717-746):
       - SEC-02 (Apps-Plugin Sandbox) -> Phase 2 -Findings (typisch: Plugin-Sandbox, Sideload-Integrity, code_bundle-Manipulation, createAppDb-Scope)
       - SEC-03 (timingSafeEqual) -> Phase 3 -Findings (typisch: Hub-Token-Comparison, Application-Tokens)
       - SEC-04 (Session-Middleware deny-by-default) -> Phase 3 -Findings (typisch: Session-Middleware-Finding)
       - SEC-05 (OAuth/Cookie/CSRF) -> Phase 3 -Findings (typisch: CSRF-Skip, Dev-Login-Bypass, Secure-Flag, Upload-Handling)
       - SEC-06 (Docker-Compose env-basiert) -> Phase 4 -Findings (typisch: Docker-Secrets, .env.example-Default, MCP-Token-Placeholder)
       - SEC-07 (pnpm-audit + overrides) -> Phase 4 / Deferred-Findings (typisch: D-01 pnpm-audit, D-N pnpm.overrides)

       Ersetze die `_Wave 5 fuellt_`-Platzhalter in der Tabelle durch konkrete Finding-ID-Listen:

       ```markdown
       | SEC-02  | Apps-Plugin Sandbox (CPU/Memory/Timeout + Whitelisted APIs) | F-01, F-XX, F-YY |
       | SEC-03  | timingSafeEqual fuer interne Tokens | F-ZZ, F-AA |
       ...
       ```

       Nach der Tabelle: `**Unabgedeckt / offen:**` fuellen:
       - Wenn alle 6 SEC-Reqs eine Finding-ID-Liste haben (keine leere Zelle) -> `**Unabgedeckt / offen:** Keine - alle 6 SEC-Requirements sind durch mindestens ein Finding gedeckt.`
       - Wenn ein SEC-Req keine Finding-Abdeckung hat -> `**Unabgedeckt / offen:** SEC-XX - kein Finding im Audit zeigt auf diese Requirement. Moeglicherweise redundant oder Audit-Luecke.` (Alarmierender Zustand; sollte in Wave 4 nicht passiert sein).

    5. **Verify per rg:**

       ```bash
       # Exec-Summary enthaelt Zahl statt N-Platzhalter:
       rg -q '^\*\*Gesamt-Findings:\*\* [0-9]+' $AUDIT
       # Traceability-Tabelle hat keine Wave-5-Platzhalter mehr:
       ! rg -q '_Wave 5 fuellt_' $AUDIT
       # SHA im Header ist gefuellt:
       rg -q 'Codebase-Stand:\*\* git-rev `[0-9a-f]{7,40}`' $AUDIT
       ```
  </action>
  <verify>
    <automated>rg -q '^\*\*Gesamt-Findings:\*\* [0-9]+' .planning/research/01-security-audit.md &amp;&amp; ! rg -q '_Wave 5 fuellt_' .planning/research/01-security-audit.md &amp;&amp; rg -q 'Codebase-Stand.*git-rev `[0-9a-f]{7,40}`' .planning/research/01-security-audit.md &amp;&amp; for req in SEC-02 SEC-03 SEC-04 SEC-05 SEC-06 SEC-07; do line=$(rg "^\| $req " .planning/research/01-security-audit.md); echo "$line" | rg -q '[FD]-[0-9]+' || { echo "MISSING finding-id for $req: $line"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - Exec-Summary hat echte Zahl nach "Gesamt-Findings:": `rg -q '^- \*\*Gesamt-Findings:\*\* [0-9]+' .planning/research/01-security-audit.md`.
    - Kein `_Wave 5 fuellt_`-Platzhalter mehr in der Datei: `! rg -q '_Wave 5 fuellt_' audit`.
    - Kein `<fill-in-wave-5>`-Platzhalter mehr: `! rg -q 'fill-in-wave-5' audit`.
    - SHA im Header ist 7+ hex chars: `rg -q 'Codebase-Stand.*git-rev `[0-9a-f]{7,40}`' audit`.
    - Jede SEC-Zeile in der Traceability-Tabelle enthaelt mindestens eine F- oder D-Finding-ID: Script-Check oben iteriert SEC-02..07.
    - Letzte-Aktualisierung-Datum ist nicht mehr YYYY-MM-DD: `! rg -q 'letzte Aktualisierung: YYYY-MM-DD' audit`.
  </acceptance_criteria>
  <done>
    §1 Exec-Summary enthaelt konkrete Gesamt-/Severity-/Phase-Counts + Sofort-Handlungsempfehlung. §10 Traceability-Tabelle hat alle 6 SEC-Reqs mit Finding-ID-Referenzen. Header-SHA und letzte-Aktualisierung-Datum sind real. Audit ist inhaltlich und strukturell fertig.
  </done>
</task>

<task type="auto">
  <name>Task 2: validation.sh generieren mit allen 13 Nyquist-Checks</name>
  <files>.planning/phases/01-security-audit-priorisierung/validation.sh</files>
  <read_first>
    - .planning/phases/01-security-audit-priorisierung/01-VALIDATION.md §"Per-Task Verification Map" (13 Checks mit Automated-Command-Spalte).
    - .planning/phases/01-security-audit-priorisierung/01-PATTERNS.md §"Pattern H - Skript-Skelett" (Zeilen ~254-359) und §"Content-Mapping" (Zeilen ~348-359).
    - platform/scripts/switch-env.sh (Vorbild-Stil: Shebang, Dir-Resolve, Exit-Pattern).
  </read_first>
  <action>
    Erzeuge `.planning/phases/01-security-audit-priorisierung/validation.sh` mit dem Inhalt aus dem `<interfaces>`-Block oben (komplettes Bash-Skelett).

    Nach dem Write:

    1. **Ausfuehrbar machen:**
       ```bash
       chmod +x /home/andreas/workspace/guildora/platform/.planning/phases/01-security-audit-priorisierung/validation.sh
       ```

    2. **Sanity-Run:** Script einmal ausfuehren und pruefen, dass es **ALLE 13 Checks ausgibt** (unabhaengig von Pass/Fail):
       ```bash
       bash /home/andreas/workspace/guildora/platform/.planning/phases/01-security-audit-priorisierung/validation.sh
       # Zaehl-Check: Output hat mindestens 13 PASS/FAIL-Zeilen
       ```

       Wenn Output weniger als 13 Checks hat, ist ein Check uebersprungen worden (Script-Bug) -> korrigieren.

    3. **Exit-Code-Interpretation:** Wenn `$?` 0 ist -> Audit ist strukturell korrekt, Phase kann abgeschlossen werden. Wenn `$?` 1 ist -> mindestens ein Check fehlgeschlagen; Fehlermeldungen analysieren, Audit-Doc nachbessern, dann erneut laufen lassen.

    4. **Falls Script FAIL meldet:** NICHT einfach den Check-Pattern loockern. Stattdessen:
       - Fuer SC1/SC3a/SC4/D-13/TRACE-Fehler: Audit-Doc korrigieren (Task 1 oder Wave 4 Task 1/2 nachhakt).
       - Fuer SC2a-e-Fehler: fehlende Pflicht-Meta-Felder im Audit ergaenzen.
       - Fuer SC3b-Fehler: Critical/High-Finding auf v2 oder out-of-scope -> Severity korrigieren ODER Target Phase auf Phase 2/3/4 anpassen.
       - Fuer D-15-Fehler: Operational-Finding hat `Severity:` statt `Class:` - korrigieren.
       - Fuer VOL-Fehler: Findings in Deferred verschieben (zu viele) oder pruefen ob falsch konsolidiert (zu wenige).

    5. **Erst wenn `bash validation.sh && echo GREEN` "GREEN" ausgibt** -> Task ist abgeschlossen.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-security-audit-priorisierung/validation.sh &amp;&amp; test -x .planning/phases/01-security-audit-priorisierung/validation.sh &amp;&amp; bash .planning/phases/01-security-audit-priorisierung/validation.sh &amp;&amp; echo VALIDATION_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/01-security-audit-priorisierung/validation.sh` exit 0.
    - `test -x .planning/phases/01-security-audit-priorisierung/validation.sh` exit 0 (executable bit gesetzt).
    - Shebang korrekt: `head -1 validation.sh | grep -q '^#!/bin/bash'`.
    - Dir-Resolve korrekt: `rg -q 'DIR="\$\(cd "\$\(dirname "\$0"\)/\.\./\.\./\.\." && pwd\)"' validation.sh`.
    - 13 Check-Aufrufe vorhanden: `test $(rg -c '^check "' validation.sh) -ge 13` (es koennen auch mehr sein wenn SC2 pro Feld einen Call hat).
    - Script terminiert mit Exit 0 gegen das finale Audit-Doc: `bash validation.sh && echo GREEN` produziert "GREEN" im Output.
    - Kein `set -e` im Script: `! rg -q '^set -e' validation.sh` (invert; wir wollen FAIL-Accumulator, nicht Early-Exit).
  </acceptance_criteria>
  <done>
    `validation.sh` existiert, executable, enthaelt alle 13 Checks aus 01-VALIDATION.md, exit 0 gegen das finale Audit.
  </done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md SEC-01-Status auf Done flippen + STATE.md aktualisieren</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (aktueller Stand - SEC-01-Zeile suchen, Footer-Zeile pruefen).
    - .planning/STATE.md (aktueller Stand - Frontmatter + Current Position + Performance Metrics + Accumulated Context + Session Continuity pruefen).
    - .planning/phases/01-security-audit-priorisierung/01-PATTERNS.md §"Pattern I" (REQUIREMENTS-Edit) und §"Pattern J/K" (STATE-Edit).
  </read_first>
  <action>
    **A) REQUIREMENTS.md editieren:**

    1. Zeile finden: `| SEC-01 | Phase 1 | Pending |` (normalerweise Zeile 80).
    2. Edit: `Pending` -> `Done`. Nach dem Edit soll da stehen: `| SEC-01 | Phase 1 | Done |`.
    3. KEINE anderen Zeilen beruehren. KEINE Coverage-Zeilen (96-99) aendern - SEC-01 bleibt gemappt; nur Status wechselt.
    4. Footer (letzte Zeile) aktualisieren:
       - Vorher: `*Last updated: 2026-04-16 after roadmap mapping*`
       - Nachher: `*Last updated: <heutiges ISO-Datum> after Phase 1 audit completion*`

    **B) STATE.md editieren:**

    1. Aktuelles Datum ermitteln: `TODAY=$(date -u +"%Y-%m-%d")` und `NOW_ISO=$(date -u +"%Y-%m-%dT%H:%M:%SZ")`.

    2. YAML-Frontmatter (Zeilen 1-15):
       - `stopped_at: Phase 1 context gathered` -> `stopped_at: Phase 1 audit completed`
       - `last_updated: "2026-04-16T16:20:13.304Z"` -> `last_updated: "<NOW_ISO>"`
       - `last_activity: 2026-04-16 - Roadmap erstellt (8 Phasen, 15/15 v1-Requirements gemappt)` -> `last_activity: <TODAY> - Phase 1 abgeschlossen: Security-Audit veroeffentlicht (.planning/research/01-security-audit.md)`
       - `progress.completed_phases: 0` -> `progress.completed_phases: 1`
       - `progress.total_plans: 0` -> `progress.total_plans: 5`
       - `progress.completed_plans: 0` -> `progress.completed_plans: 5`
       - `progress.percent: 0` -> `progress.percent: 12`

    3. §Current Position (Zeilen ~26-33):
       - `Phase: 1 of 8 (Security Audit & Priorisierung)` -> `Phase: 2 of 8 (Apps-Plugin-Sandbox)`
       - `Plan: - of - in current phase` -> `Plan: - of - in current phase` (bleibt, weil Phase 2 noch nicht geplant)
       - `Status: Ready to plan` -> `Status: Audit delivered; ready for Phase 2 planning`
       - `Last activity: 2026-04-16 - Roadmap erstellt ...` -> `Last activity: <TODAY> - Phase 1 abgeschlossen: Security-Audit veroeffentlicht`
       - `Progress: [░░░░░░░░░░] 0%` -> `Progress: [█░░░░░░░░░] 12%`

    4. §Performance Metrics: By-Phase-Tabelle erweitern:
       - Bestehende Zeile `| - | - | - | - |` entweder ersetzen oder erste Daten-Zeile hinzufuegen: `| 1 | 5 | - | - |` (Total und Avg leer lassen wenn keine Timer-Daten vorhanden).
       - `Total plans completed: 0` -> `Total plans completed: 5`.

    5. §Accumulated Context > Decisions (Zeilen ~58-65): Neuer Bullet am Ende (bestehende Decisions erhalten):
       - `- <TODAY>: Phase 1 abgeschlossen - Security-Audit mit <N> Findings (davon <C> Critical, <H> High) mapped an Phase 2/3/4; Git-Tag phase-1-audit-baseline gesetzt`

       Die Counts aus Task 1 Exec-Summary uebernehmen.

    6. §Session Continuity (Zeilen ~83-87):
       - `Last session: 2026-04-16T16:20:13.302Z` -> `Last session: <NOW_ISO>`
       - `Stopped at: Phase 1 context gathered` -> `Stopped at: Phase 1 audit completed`
       - `Resume file: .planning/phases/01-security-audit-priorisierung/01-CONTEXT.md` -> `Resume file: .planning/phases/02-apps-plugin-sandbox/02-CONTEXT.md` (oder `-` falls der Phase-2-Dir noch nicht existiert; Wave 5 traegt nur die Zielerwartung ein).

    KEIN git-Commit in dieser Task - Commit passiert in Task 4 (Bundle).
  </action>
  <verify>
    <automated>rg -q '^\| SEC-01 \| Phase 1 \| Done \|' .planning/REQUIREMENTS.md &amp;&amp; rg -q '^completed_phases: 1$' .planning/STATE.md &amp;&amp; rg -q '^  percent: 12$' .planning/STATE.md &amp;&amp; rg -q 'Phase 1 abgeschlossen' .planning/STATE.md &amp;&amp; ! rg -q '^\| SEC-01 \| Phase 1 \| Pending \|' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md SEC-01-Zeile ist Done: `rg -q '^\| SEC-01 \| Phase 1 \| Done \|' REQUIREMENTS.md` exit 0.
    - REQUIREMENTS.md SEC-01-Zeile ist NICHT mehr Pending: `! rg -q '^\| SEC-01 \| Phase 1 \| Pending \|' REQUIREMENTS.md` (invert).
    - REQUIREMENTS.md Footer aktualisiert: `rg -q 'Last updated.*after Phase 1 audit completion' REQUIREMENTS.md`.
    - Andere SEC-Reqs noch Pending: `test $(rg -c '^\| SEC-0[2-7] \| Phase [234] \| Pending \|' REQUIREMENTS.md) -eq 6`.
    - STATE.md completed_phases auf 1: `rg -q '^  completed_phases: 1$' STATE.md`.
    - STATE.md percent auf 12: `rg -q '^  percent: 12$' STATE.md`.
    - STATE.md Phase-1-Abschluss-Notiz: `rg -q 'Phase 1 abgeschlossen' STATE.md`.
    - STATE.md hat aktualisiertes last_updated: `! rg -q 'last_updated: "2026-04-16T16:20:13\.304Z"' STATE.md` (invert - alter Wert ersetzt).
    - STATE.md Progress-Bar gefuellt: `rg -q 'Progress: \[█░░░░░░░░░\] 12%' STATE.md`.
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md zeigt SEC-01 als Done, keine andere Zeile geaendert, Footer aktualisiert. STATE.md spiegelt Phase-1-Abschluss (Frontmatter, Current Position, Performance Metrics, Decisions, Session Continuity). Keine Commits - Task 4 buendelt.
  </done>
</task>

<task type="auto">
  <name>Task 4: validation.sh final run, git commit + git tag phase-1-audit-baseline</name>
  <files>no new files; commits existing</files>
  <read_first>
    - Alle Files aus Wave 1-5 (Audit-Doc, validation.sh, REQUIREMENTS.md, STATE.md, kopf-review.md, scratch/grep-results.md, alle 5 PLAN-Files).
    - .planning/config.json (`commit_docs: true` bestaetigt - wir committen).
    - .planning/phases/01-security-audit-priorisierung/01-RESEARCH.md §"Common Pitfalls" §Pitfall 3 (Staleness) - Tag-Setzung ist die Mitigation.
  </read_first>
  <action>
    1. **Final validation-Run** als letzter Check vor Commit:

       ```bash
       cd /home/andreas/workspace/guildora/platform
       bash .planning/phases/01-security-audit-priorisierung/validation.sh
       RC=$?
       if [ $RC -ne 0 ]; then
         echo "FAIL: validation.sh exit $RC - NICHT committen. Audit nachbessern."
         exit 1
       fi
       ```

    2. **Git-Commit** der Phase-1-Abschluss-Artefakte:

       Die committenden Dateien:
       - `.planning/research/01-security-audit.md` (neu)
       - `.planning/phases/01-security-audit-priorisierung/kopf-review.md` (neu)
       - `.planning/phases/01-security-audit-priorisierung/scratch/grep-results.md` (neu; alternativ: vor Commit loeschen wenn Wave 5 entscheidet, scratch-Content nicht zu behalten - siehe unten)
       - `.planning/phases/01-security-audit-priorisierung/scratch/.gitkeep` (neu)
       - `.planning/phases/01-security-audit-priorisierung/validation.sh` (neu)
       - `.planning/REQUIREMENTS.md` (modified - SEC-01 Done)
       - `.planning/STATE.md` (modified - Phase-1-Abschluss)
       - `.planning/phases/01-security-audit-priorisierung/01-01-PLAN.md` bis `01-05-PLAN.md` (neu; noch nicht committed aus Plan-Phase - falls schon committed, automatisch skipped)
       - `.planning/phases/01-security-audit-priorisierung/01-01-SUMMARY.md` bis `01-05-SUMMARY.md` (neu; falls vorhanden)

       **Scratch-Entscheidung:** Per 01-RESEARCH.md §"Output-Handling" Zeile 446 sollte `scratch/grep-results.md` am Phase-Ende geloescht werden (nur das finale Audit-Doc lebt lang). ABER fuer Audit-Nachvollziehbarkeit ist das Scratch-File wertvoller Kontext. Entscheidungs-Regel hier:
       - Wenn Wave 4 alle wichtigen grep-Treffer ins Audit uebernommen hat, scratch/grep-results.md loeschen VOR Commit: `rm scratch/grep-results.md`.
       - scratch/.gitkeep committen (damit scratch/ fuer Wave-4-Re-Runs wieder nutzbar ist).
       - Alternativ: scratch/grep-results.md behalten als "Audit-Appendix fuer Reproduzierbarkeit". Default-Empfehlung dieses Plans: **behalten** (Nachvollziehbarkeit > Minimal-Tree).

       Commit via gsd-tools:

       ```bash
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(01): complete Phase 1 security audit with 13-check validation" --files \
         .planning/research/01-security-audit.md \
         .planning/phases/01-security-audit-priorisierung/kopf-review.md \
         .planning/phases/01-security-audit-priorisierung/scratch/grep-results.md \
         .planning/phases/01-security-audit-priorisierung/scratch/.gitkeep \
         .planning/phases/01-security-audit-priorisierung/validation.sh \
         .planning/REQUIREMENTS.md \
         .planning/STATE.md \
         .planning/phases/01-security-audit-priorisierung/01-01-PLAN.md \
         .planning/phases/01-security-audit-priorisierung/01-02-PLAN.md \
         .planning/phases/01-security-audit-priorisierung/01-03-PLAN.md \
         .planning/phases/01-security-audit-priorisierung/01-04-PLAN.md \
         .planning/phases/01-security-audit-priorisierung/01-05-PLAN.md
       ```

       Wenn einzelne Files schon in frueheren Commits sind (z.B. weil plan-phase sie committed hat), Meldung "nothing to commit for file X" ignorieren.

    3. **Git-Tag setzen:**

       ```bash
       cd /home/andreas/workspace/guildora/platform
       git tag -a phase-1-audit-baseline -m "Phase 1 Security Audit Baseline - consolidated audit at .planning/research/01-security-audit.md. Use as checkout point for Phase 2/3/4 research."
       ```

       **Kein `git push --tags`** - Tag bleibt lokal, damit `platform/`-Repo nicht versehentlich öffentlich wird vor Phase-2-Mitigation (T-01-22).

    4. **Verify:**

       ```bash
       # Tag existiert und zeigt auf den commit mit dem Audit:
       git tag --list phase-1-audit-baseline
       git log phase-1-audit-baseline -1 --oneline
       # Audit-Doc Header-SHA matcht den Tagged-Commit:
       AUDIT_SHA=$(rg -o '[0-9a-f]{40}' .planning/research/01-security-audit.md | head -1)
       TAG_SHA=$(git rev-parse phase-1-audit-baseline)
       test "$AUDIT_SHA" = "$TAG_SHA" || echo "WARN: Audit-Header-SHA ($AUDIT_SHA) != Tag-SHA ($TAG_SHA) - check header update in Task 1"
       ```

       (Die SHA-Gleichheit ist wuenschenswert aber nicht hart erforderlich - der Tag wurde NACH Task 1 gesetzt, der Header-SHA wurde zu Beginn von Task 1 erfasst. Wenn zwischen Task-1-Execution und Task-4-Execution kein weiterer Commit erfolgte, sind sie gleich. Sonst zeigt der Header auf den Commit VOR Finalisierung - das ist der "Codebase-Stand-beim-Audit"-Sinn und daher korrekt.)
  </action>
  <verify>
    <automated>bash .planning/phases/01-security-audit-priorisierung/validation.sh &amp;&amp; git -C /home/andreas/workspace/guildora/platform tag --list phase-1-audit-baseline | grep -q phase-1-audit-baseline &amp;&amp; git -C /home/andreas/workspace/guildora/platform log -1 --oneline | grep -q 'Phase 1 security audit'</automated>
  </verify>
  <acceptance_criteria>
    - validation.sh exit 0: `bash validation.sh` exit 0.
    - git-Tag phase-1-audit-baseline existiert: `git tag --list phase-1-audit-baseline` produziert "phase-1-audit-baseline".
    - Letzter Commit ist der Phase-1-Completion-Commit: `git log -1 --pretty=%s | grep -q 'Phase 1 security audit'`.
    - Commit enthaelt die Phase-1-Files: `git show --name-only HEAD | rg -q '01-security-audit.md'`.
    - Kein Tag-Push nach Remote: `git config --get remote.origin.url` kann existieren, aber `git push --tags` wurde NICHT ausgefuehrt (operational; keine direkte Assertion).
  </acceptance_criteria>
  <done>
    validation.sh gruen, Commit gesetzt, Tag `phase-1-audit-baseline` lokal gesetzt, Remote nicht angefasst. Phase 1 ist formal abgeschlossen; Phase 2 kann mit `/gsd-plan-phase 02` starten.
  </done>
</task>

</tasks>

<verification>
End-of-Phase-Check:
- Audit-Doc ist vollstaendig (Header-SHA, Exec-Summary, 15-25 Findings, Operational, Deferred, Traceability mit 6 SEC-Reqs).
- validation.sh laeuft gruen (alle 13 Checks PASS).
- REQUIREMENTS.md SEC-01 ist Done; andere SEC-Reqs sind Pending.
- STATE.md reflektiert Phase-1-Abschluss (completed_phases=1, percent=12).
- Git-Tag phase-1-audit-baseline zeigt auf den Completion-Commit.

Alle 13 Nyquist-Checks aus 01-VALIDATION.md sind durch einen oder mehrere Plan-Tasks abgedeckt:
- 1-01-SC1 (Audit-Datum) -> Plan 01 Task 2 + Plan 05 Task 1 (Aktualisierung)
- 1-01-SC2a-e (5 Pflicht-Felder pro Finding) -> Plan 04 Task 1
- 1-01-SC3a (Severity-Order) -> Plan 01 Task 2 + Plan 04 Task 1
- 1-01-SC3b (Kein C/H auf v2) -> Plan 04 Task 1
- 1-01-SC4 (Deferred + Warum nicht jetzt) -> Plan 04 Task 2
- 1-01-D13 (Kopf-Review protokolliert) -> Plan 03 Task 2
- 1-01-D15 (OP Class: Operational) -> Plan 04 Task 2
- 1-01-TRACE (SEC-02..07 gedeckt) -> Plan 05 Task 1
- 1-01-VOL (15-25 F-Findings) -> Plan 04 Task 1
- validation.sh-Skript selbst -> Plan 05 Task 2
- SEC-01 Done-Flip -> Plan 05 Task 3
- STATE-Update -> Plan 05 Task 3
- Tag + Commit -> Plan 05 Task 4
</verification>

<success_criteria>
- Exec-Summary in §1 ist mit echten Zahlen gefuellt.
- Traceability-Tabelle §10 hat alle 6 SEC-Reqs mit Finding-ID-Listen.
- Commit-SHA im Audit-Header ist ein echter git-SHA (nicht Platzhalter).
- validation.sh existiert, ist executable, laeuft exit 0.
- REQUIREMENTS.md SEC-01 ist Done; Footer aktualisiert.
- STATE.md zeigt Phase-1-Abschluss in Frontmatter + Body.
- Git-Tag phase-1-audit-baseline ist lokal gesetzt.
- Roadmap Phase-1-Success-Criteria #1-#4 sind alle erfuellt (Audit vollstaendig, Meta-Felder pro Finding, Severity-sortiert mit Phase-Zuordnung, Deferred-Abschnitt mit Begruendung).
</success_criteria>

<output>
Nach Abschluss: Erstelle `.planning/phases/01-security-audit-priorisierung/01-05-SUMMARY.md` mit:
- Finding-Counts (Final): Total, Critical, High, Medium, Low, Operational, Deferred.
- Phase-Aufteilung (Phase 2 / Phase 3 / Phase 4 / v2 / Deferred Counts).
- SEC-Requirement-Coverage: welche 6 SEC-Reqs sind durch welche Finding-IDs gedeckt.
- validation.sh Ergebnis (alle 13 Checks PASS).
- Git-Tag: `phase-1-audit-baseline` -> commit `<SHA>`.
- Next Step: `/gsd-plan-phase 02` fuer Apps-Plugin-Sandbox.
</output>
