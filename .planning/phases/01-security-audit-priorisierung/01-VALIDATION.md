---
phase: 1
slug: security-audit-priorisierung
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-16
---

# Phase 1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> **Phase 1 is a documentation phase — there is no application test framework.**
> Validation is structural verification of the audit document at
> `.planning/research/01-security-audit.md` via grep/ripgrep checks.
> All automated checks live in `validation.sh` (generated by Wave 5 of PLAN.md).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Bash + ripgrep (structural document validation) |
| **Config file** | none — validation commands live in `validation.sh` |
| **Quick run command** | `bash .planning/phases/01-security-audit-priorisierung/validation.sh` |
| **Full suite command** | same — audit is one-shot, no sub-suites |
| **Estimated runtime** | <5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `rg -c '^### \[F-' .planning/research/01-security-audit.md` (finding-count spot-check during Wave 4 incremental writes)
- **After every plan wave:** Run `bash .planning/phases/01-security-audit-priorisierung/validation.sh`
- **Before `/gsd-verify-work`:** Full suite must be green; `SEC-01` row in `REQUIREMENTS.md` traceability table set to `Done`; Andi ACK on Kopf-Review protocol
- **Max feedback latency:** <5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 1-01-SC1 | 01 | 5 | SEC-01.SC1 | — | Audit doc carries `**Audit-Datum:**` line (written + dated) | grep | `rg -q '^\*\*Audit-Datum:\*\*' .planning/research/01-security-audit.md` | ❌ W0 | ⬜ pending |
| 1-01-SC2a | 01 | 4 | SEC-01.SC2 | — | Every finding has `Severity` metadata | grep | `test $(rg -c '^### \[F-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Severity:\*\*' .planning/research/01-security-audit.md)` | ❌ W0 | ⬜ pending |
| 1-01-SC2b | 01 | 4 | SEC-01.SC2 | — | Every finding has `Datei-Pfad(e)` metadata | grep | `test $(rg -c '^### \[F-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Datei-Pfad\(e\):\*\*' .planning/research/01-security-audit.md)` | ❌ W0 | ⬜ pending |
| 1-01-SC2c | 01 | 4 | SEC-01.SC2 | — | Every finding has `Fix-Ansatz` metadata | grep | `test $(rg -c '^### \[F-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Fix-Ansatz:\*\*' .planning/research/01-security-audit.md)` | ❌ W0 | ⬜ pending |
| 1-01-SC2d | 01 | 4 | SEC-01.SC2 (D-06) | — | Every finding has `Current Mitigation` (mandatory, even "keine") | grep | `test $(rg -c '^### \[F-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Current Mitigation:\*\*' .planning/research/01-security-audit.md)` | ❌ W0 | ⬜ pending |
| 1-01-SC2e | 01 | 4 | SEC-01.SC2 (D-06) | — | Every finding has `Target Phase` metadata | grep | `test $(rg -c '^### \[F-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Target Phase:\*\*' .planning/research/01-security-audit.md)` | ❌ W0 | ⬜ pending |
| 1-01-SC3a | 01 | 4 | SEC-01.SC3 | — | Findings sorted: Critical → High → Medium → Low | grep + line-order | `validation.sh` verifies header order `## Findings — Critical` precedes `## Findings — High` precedes `## Findings — Medium` precedes `## Findings — Low` | ❌ W0 | ⬜ pending |
| 1-01-SC3b | 01 | 4 | SEC-01.SC3 | — | No Critical/High finding has Target Phase `v2` or `out-of-scope` | grep (invert) | script: read each `### [F-…]` block where Severity matches `Critical\|High`, fail if any `Target Phase:` is `v2\|out-of-scope` | ❌ W0 | ⬜ pending |
| 1-01-SC4 | 01 | 5 | SEC-01.SC4 | — | Deferred section exists and every deferred item has `Warum nicht jetzt` justification | grep | `rg -q '^## .* Deferred' .planning/research/01-security-audit.md && test $(rg -c '^### \[D-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Warum nicht jetzt:\*\*' .planning/research/01-security-audit.md)` | ❌ W0 | ⬜ pending |
| 1-01-D13 | 01 | 3 | SEC-01 (D-13) | — | Kopf-Review protocol captured (either separate file OR as appendix in audit) | file | `test -f .planning/phases/01-security-audit-priorisierung/kopf-review.md \|\| rg -q '^## .* Appendix.*Kopf-Review' .planning/research/01-security-audit.md` | ❌ W0 | ⬜ pending |
| 1-01-D15 | 01 | 4 | SEC-01 (D-15) | — | Operational findings tagged with `Class: Operational` | grep | `rg -q '^### \[OP-' .planning/research/01-security-audit.md && test $(rg -c '^### \[OP-' .planning/research/01-security-audit.md) -eq $(rg -A2 '^### \[OP-' .planning/research/01-security-audit.md \| grep -c '^- \*\*Class:\*\* Operational')` | ❌ W0 | ⬜ pending |
| 1-01-TRACE | 01 | 5 | SEC-02…SEC-07 coverage | — | Traceability table covers SEC-02 through SEC-07 | grep | `for req in SEC-02 SEC-03 SEC-04 SEC-05 SEC-06 SEC-07; do rg -q "^\\\| $req " .planning/research/01-security-audit.md \|\| exit 1; done` | ❌ W0 | ⬜ pending |
| 1-01-VOL | 01 | 4 | D-16 volume budget | — | Audit contains 15–25 findings total (F- blocks) | count | `N=$(rg -c '^### \[F-' .planning/research/01-security-audit.md); test $N -ge 15 -a $N -le 25` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `.planning/phases/01-security-audit-priorisierung/validation.sh` — bash script executing all 13 checks above (generated in Wave 5 as a planned task)
- [ ] `.planning/research/01-security-audit.md` — the audit document itself (exists only after Wave 5)
- [ ] `.planning/phases/01-security-audit-priorisierung/kopf-review.md` — temporary Wave 3 protocol file (planner decides in Wave 5 whether to keep as separate artifact or fold into audit as Appendix A)
- [ ] ripgrep (`rg`) available in the container — assumption A1 from RESEARCH.md; planner task must verify with `command -v rg` before first use

*No test framework install needed — Phase 1 produces only documentation.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Kopf-Review content quality (Andi's undocumented concerns actually surfaced) | SEC-01.SC1 + D-13 | Content cannot be grep-verified for depth/honesty | Andi re-reads the Kopf-Review protocol and confirms that every concern he carries in his head is represented |
| Severity classification accuracy | SEC-01.SC2 + D-05 | Severity is a human judgment call against D-05 criteria | Andi reviews each finding's Severity against the D-05 criteria block and confirms no misclassification |
| "Fail Loud, Never Fake" adherence | PROJECT.md | Cannot grep for absence of softening language | Andi reviews every `Current Mitigation` field for weasel wording ("might", "usually", "normally") and hardens any found |
| Finding → Phase mapping correctness | SEC-01.SC3 | Phase assignment requires reasoning about scope of Phase 2/3/4 | Andi reviews `Target Phase` for each Critical/High and confirms it matches the target phase's stated scope in ROADMAP.md |

---

## Validation Sign-Off

- [ ] All tasks in PLAN.md with documentation outputs have a grep-verifiable `<automated>` check or Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (validation.sh, audit doc, kopf-review doc, ripgrep availability)
- [ ] No watch-mode flags (none apply — Phase 1 has no runtime)
- [ ] Feedback latency < 5s for `validation.sh`
- [ ] `nyquist_compliant: true` set in frontmatter once validation.sh exists and all 13 checks pass

**Approval:** pending
