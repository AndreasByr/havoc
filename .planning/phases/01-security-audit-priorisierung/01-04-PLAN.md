---
phase: 01-security-audit-priorisierung
plan: 04
type: execute
wave: 4
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - .planning/research/01-security-audit.md
autonomous: true
requirements:
  - SEC-01
tags:
  - security
  - audit
  - consolidation
must_haves:
  truths:
    - "Alle Findings aus drei Quellen (CONCERNS.md Inventar, scratch/grep-results.md, kopf-review.md) sind zu einem konsolidierten Finding-Set in .planning/research/01-security-audit.md zusammengefuehrt."
    - "Anzahl der F-Findings liegt im D-16-Zielbereich 15-25 - bei Ueberschreitung werden schwaechste Lows in den Deferred-Block verschoben."
    - "Jedes F-Finding hat alle 5 Pflicht-Meta-Felder: Severity, Datei-Pfad(e), Current Mitigation, Fix-Ansatz, Target Phase (D-06)."
    - "Jedes Critical/High-Finding zeigt auf Phase 2, 3 oder 4 - KEIN Critical/High auf v2 oder out-of-scope (Roadmap SC3)."
    - "Operational-Findings haben Class: Operational statt Severity: ... (D-15)."
    - "Deferred-Findings haben Warum nicht jetzt-Begruendung (D-08)."
    - "Kein Fix-Ansatz ueberschreitet 2 Saetze, keine Library-Namen in Fix-Ansatz (Pitfall 2)."
    - "Current Mitigation: keine steht immer mit Begruendung (Pitfall 5)."
    - "Operational-vs-Security-Dichotomie-Regel respektiert: Angreifer-Pfad beschreibbar -> Security; nur Last -> Operational (Pitfall 4)."
  artifacts:
    - path: ".planning/research/01-security-audit.md"
      provides: "Finding-Blocks in §§4-9 ausgefuellt: Critical/High/Medium/Low Findings + Operational + Deferred"
      contains: "### [F-NN] pro Finding, ### [OP-NN] pro Operational, ### [D-NN] pro Deferred"
      min_lines: 250
  key_links:
    - from: "01-security-audit.md §§4-7"
      to: "scratch/grep-results.md + kopf-review.md + CONCERNS.md"
      via: "jeder Finding-Block synthetisiert aus mindestens einer Quelle; Discovered-By-Feld nennt Quelle(n)"
      pattern: "Discovered-By"
    - from: "01-security-audit.md §8 Operational"
      to: "CONCERNS.md §Performance Bottlenecks + §Tech Debt"
      via: "OC-01 (In-Memory-RL), OC-02 (Thundering Herd), OC-03 (Migration-Fixups) uebernommen"
      pattern: "^### \\[OP-"
---

<objective>
Konsolidiere die drei Input-Quellen aus Wave 1-3 (Skelett, grep-results, kopf-review) zu einem vollstaendigen Finding-Set in `.planning/research/01-security-audit.md` §§4-9. Wave 4 ist die **intellektuelle Kernwave** der Phase: hier wird bewertet, klassifiziert, severity-assigned, und phase-zugeordnet.

Keine Exec-Summary und keine Traceability-Tabelle-Befuellung in dieser Wave - das ist Wave 5, nachdem die Findings finalisiert sind.

Purpose: Roadmap Phase-1 Success Criteria #2 verlangt "Jedes Finding ist mit Severity, Datei-Pfad und Fix-Ansatz versehen"; #3 verlangt "Findings sind nach Severity sortiert und die Phasen 2-4 lassen sich eindeutig auf Gruppen von Findings abbilden". Diese Wave produziert das Kernmaterial.

Output: Alle Finding-Blocks (F-/OP-/D-) als Markdown-Bloecke im Audit-Dokument, sortiert nach Severity innerhalb der §§4-9.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-audit-priorisierung/01-CONTEXT.md
@.planning/phases/01-security-audit-priorisierung/01-RESEARCH.md
@.planning/phases/01-security-audit-priorisierung/01-PATTERNS.md
@.planning/phases/01-security-audit-priorisierung/01-VALIDATION.md
@.planning/codebase/CONCERNS.md
@.planning/research/01-security-audit.md
@.planning/phases/01-security-audit-priorisierung/scratch/grep-results.md
@.planning/phases/01-security-audit-priorisierung/kopf-review.md

<interfaces>
Finding-Block-Template (aus 01-PATTERNS.md §Pattern C, Zeilen ~90-130) - MUSS fuer ALLE F-Findings identisch sein:

```
### [F-NN] <Titel in Deutsch, Substantiv-zentriert>

- **Severity:** Critical | High | Medium | Low
- **Area:** Apps-Plugin | Auth/Session | Supply-Chain | Uploads | ... (kurze Bereichs-Kategorie)
- **Datei-Pfad(e):** `platform/apps/...ts:line` (optional mit Zeilen-Referenz)
- **Current Mitigation:** <Konkrete Beschreibung; bei "keine" -> "keine - <ein Satz warum>">
- **Fix-Ansatz:** <1-2 Saetze, keine Library-Namen, keine Implementierungs-Spec>
- **Target Phase:** Phase 2 | Phase 3 | Phase 4 | v2 | out-of-scope
- **Discovered-By:** CONCERNS.md §... | grep-Scan P-N | Kopf-Review §<Bereich>.N (optional; nur wenn es das Verstaendnis foerdert)
```

Operational-Block-Template (aus 01-PATTERNS.md §Pattern D) - `Class:` statt `Severity:`:

```
### [OP-NN] <Titel>

- **Class:** Operational (nicht Security-Severity)
- **Area:** <Bereich>
- **Datei-Pfad(e):** `platform/...`
- **Current Mitigation:** <Beschreibung>
- **Fix-Ansatz:** <kurz>
- **Target Phase:** v2 (<v2-Requirement-ID falls vorhanden, z.B. INFRA-01>)
```

Deferred-Block-Template (aus 01-PATTERNS.md §Pattern E) - `Warum nicht jetzt:` Pflicht:

```
### [D-NN] <Titel>

- **Severity:** Low | Medium (Defense-in-Depth / Prozess)
- **Area:** <Bereich>
- **Datei-Pfad(e):** <path> | - (Prozess-Luecke, nicht Code)
- **Current Mitigation:** <Beschreibung>
- **Warum nicht jetzt:** <konkrete Begruendung, die in 6 Monaten noch nachvollziehbar ist>
- **Target Phase:** Phase 4 | v2 | out-of-scope
```

Candidate-Finding-Inventory aus 01-RESEARCH.md (Zeilen ~663-709) - Rohmaterial-Liste die in Wave 4 konsolidiert wird:

Security-Kandidaten (17 Eintraege):
- CF-01 Plugin-Sandbox (Critical, Phase 2 SEC-02) - 2 Execution-Sites
- CF-02 Timing-Safe Token Hub (High, Phase 3 SEC-03)
- CF-03 Docker Default-Credentials (Medium, Phase 4 SEC-06)
- CF-04 Session-Middleware nicht deny-by-default (High, Phase 3 SEC-04)
- CF-05 MCP-Token Placeholder (Medium-or-Low, Phase 4 SEC-06)
- CF-06 Dev-Login-Bypass (Medium, Phase 3 SEC-05)
- CF-07 CSRF-Skip (Low-or-Medium, Phase 3 SEC-05)
- CF-08 .env.example DATABASE_URL Default (Low, Phase 4 SEC-06)
- CF-09 Sideload ohne Integrity-Check (Medium, Phase 2 SEC-02-ext)
- CF-10 createAppDb Scope-Leakage (Medium-or-Low, Phase 2 SEC-02)
- CF-11 installed_apps.code_bundle Admin-Manipulation (Medium, Phase 2 SEC-02)
- CF-12 Application-Tokens Verifikationspfad (needs-review, Phase 3 SEC-03)
- CF-13 pnpm audit nie gelaufen (Low process, Phase 4 SEC-07) -> Deferred-Block
- CF-14 pnpm.overrides ungeprueft (Low, Phase 4 SEC-07) -> Phase-4 oder Deferred
- CF-15 Upload-Path-Handling (Low, Phase 3 SEC-05 grenzwertig)
- CF-16 Rate-Limit zu grob (Low, v2, Security-oder-Operational je Pitfall-4)
- CF-17 NUXT_SESSION_COOKIE_SECURE Opt-out (Low, Phase 3 SEC-05)

Operational-Kandidaten (3 Eintraege):
- OC-01 In-Memory Rate-Limit (v2 INFRA-01)
- OC-02 App-Registry Thundering Herd (v2 INFRA-02)
- OC-03 DB-Migration-Fixups (v2 DEBT-01)

Deferred-Kandidaten (5 Eintraege):
- DF-01 pnpm audit -> Phase 4 / SEC-07
- DF-02 ESLint-Security-Plugins -> Phase 5 / CI-02
- DF-03 CVSS-Scoring
- DF-04 Runtime-/Infra-Audit
- DF-05 Audit fuer marketplace/guildai/voice-rooms/app-template

Zusaetzlich aus Wave 3 (Kopf-Review): neue CF-IDs (CF-18+), falls Andi Themen aufgedeckt hat, die in CONCERNS.md und grep-Scans nicht drin waren. Wave-3-Summary enthaelt die neue ID-Liste.

5 Pitfalls aus 01-RESEARCH.md §Common Pitfalls - MUESSEN in dieser Wave respektiert werden:
- Pitfall 1 (Fatigue): 3-Bucket-Protokoll wurde in Wave 3 schon angewandt; Wave 4 haertet nach, nicht mehr als 25 F-Findings.
- Pitfall 2 (Fix-Ansatz Scope-Creep): ≤2 Saetze, keine Library-Namen, keine Implementation-Details.
- Pitfall 3 (Staleness): wird in Wave 5 durch Commit-SHA mitigiert.
- Pitfall 4 (Operational-vs-Security-Blur): Dichotomie-Regel - Angreifer-Pfad beschreibbar -> Security; nur Last -> Operational.
- Pitfall 5 (keine-ohne-Begruendung): Current Mitigation: keine - <Satz>.

Severity-Sortierung innerhalb von §4-§7:
- §4 Findings - Critical (alle [F-] mit Severity: Critical)
- §5 Findings - High (alle [F-] mit Severity: High)
- §6 Findings - Medium (alle [F-] mit Severity: Medium)
- §7 Findings - Low (alle [F-] mit Severity: Low)
- Innerhalb jedes §-Blocks: nach Bereich gruppiert (Apps-Plugin zuerst, dann Auth/Session, dann Supply-Chain, etc.), per D-03.

Finding-ID-Regel: **neu, sequentiell, kontinuierlich** - [F-01], [F-02], ... ueber alle Severity-Blocks hinweg (nicht neu startend pro §). Die CF-IDs aus dem Inventar sind NUR Kandidaten-IDs und tauchen im finalen Audit NICHT auf - die finale ID ist F-NN. Das Discovered-By-Feld kann den CF-Bezug nennen, wenn hilfreich.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Input-Quellen -> Audit-Doc | Drei unabhaengige Quellen (CONCERNS.md, grep-results, kopf-review) werden verschmolzen. Claude muss Widersprueche auffangen - z.B. wenn Kopf-Review sagt "kein Problem" und grep einen Treffer zeigt. |
| Audit-Doc -> Phase 2/3/4-Research | Phase 2/3/4 vertrauen der Severity-/Phase-Zuordnung. Fehlbewertung verzerrt Priorisierung in Folge-Phasen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | T (Tampering) | Severity-Inflation (alle Findings werden Critical) ODER Severity-Deflation (alle werden Medium) | mitigate | D-05-Kriterien-Block ist seit Wave 1 im Audit; Wave 4 prueft jedes Finding ATOMAR gegen die Kriterien. Wave 5 Kopf-Review von Andi fungiert als zweiter Check. |
| T-01-16 | I (Information Disclosure) | Audit enthaelt Finding-Details (Datei:Zeile), die Angreifer mit Repo-Zugriff ausnutzen koennten | accept | `platform/`-Repo ist privat; Phase-Branch wird nicht gepusht bis Mitigations in Phase 2/3/4 greifen. Konsistent mit Wave-1-Doc-Policy. |
| T-01-17 | S (Spoofing - Fake Coverage) | Finding mit vagen Quellen ohne konkrete Datei:Zeile | mitigate | Pattern C erzwingt `Datei-Pfad(e):`-Feld; validation.sh-Check 1-01-SC2b (in Wave 5) prueft pro-Finding-Count. Prozess-Luecken-Findings bekommen `-` als Path mit Begruendung. |
| T-01-18 | R (Repudiation) | Fix-Ansatz wird zur Architektur-Spec, bindet Phase 2/3/4 an falsche Loesung | mitigate | Pitfall 2 als Haerte-Regel: ≤2 Saetze, keine Libraries, keine Implementation. Bei Verstoss: Fix-Ansatz kuerzen, Implementation-Details raus. |
| T-01-19 | T (Blur) | Operational-Finding wird als Security klassifiziert -> Phase 3 priorisiert es faelschlich | mitigate | Pitfall 4 Dichotomie-Regel: Angreifer-Pfad beschreibbar -> Security; nur Last -> Operational. Wave 4 Task 1 wendet diese Regel pro Kandidat an. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Konsolidieren aller Security-Findings (F-NN Bloecke) in §§4-7 der Audit-Doc</name>
  <files>.planning/research/01-security-audit.md</files>
  <read_first>
    - .planning/research/01-security-audit.md (aktueller Zustand aus Wave 1 - Skelett mit leeren §§4-9).
    - .planning/codebase/CONCERNS.md §Security Considerations (4 Findings als Rohmaterial fuer CF-01..CF-04).
    - .planning/phases/01-security-audit-priorisierung/scratch/grep-results.md (alle 5 Pattern-Scans).
    - .planning/phases/01-security-audit-priorisierung/kopf-review.md (Andis wörtliche Antworten + Bucket-Zuordnungen).
    - .planning/phases/01-security-audit-priorisierung/01-RESEARCH.md §"Candidate Finding Inventory" (Zeilen ~663-709) und §"Proposed Finding -> Phase Mapping" (Zeilen ~711-746).
    - .planning/phases/01-security-audit-priorisierung/01-PATTERNS.md §"Pattern C - Finding-Block" (Zeilen 90-130) - Template und Regeln.
    - .planning/phases/01-security-audit-priorisierung/01-RESEARCH.md §"Common Pitfalls" (Zeilen ~750-798) - 5 Pitfalls, insbesondere Pitfall 2 (≤2 Saetze) und Pitfall 5 (keine-Begruendung).
  </read_first>
  <action>
    Konsolidiere die 17 Security-Kandidaten (CF-01..CF-17) aus 01-RESEARCH.md plus die in Wave 3 neu entstandenen CF-18+ zu einem finalen F-NN-Finding-Set und schreibe sie in §§4-7 des Audit-Docs.

    Konsolidierungs-Schritte:

    1. **Input-Merge:** Lade alle drei Quellen:
       - CONCERNS.md §Security Considerations (4 Findings: No-Sandboxing, Non-Timing-Safe, Default-DB-Creds, Session-Middleware).
       - scratch/grep-results.md (Raw-Treffer P-1 bis P-5 + Known-Positive-Status).
       - kopf-review.md (27 Andis-Antworten + Bucket-Checkboxes - nur Eintraege mit `[x] Wird Finding` sind Kandidaten).

    2. **Merge-Logik:**
       - Wenn CONCERNS.md-Finding + grep-Treffer + Kopf-Review-Antwort alle auf dasselbe Thema zeigen -> EIN Finding, Discovered-By listet alle drei Quellen.
       - Wenn nur grep-Treffer ohne Kopf-Review/CONCERNS.md-Bestaetigung -> Finding NUR wenn Claude mit Read-Tool den Kontext verifiziert hat (nicht auto-als-Finding).
       - Wenn nur Kopf-Review-Antwort ohne grep/CONCERNS.md-Korrespondenz -> Finding mit Discovered-By `Kopf-Review §<Bereich>.N`.

    3. **Severity-Finalisierung** (D-05 streng anwenden, Pitfall 1 gegen Inflation):
       - Jedes Kandidaten-Severity-Guess aus 01-RESEARCH.md ist NUR Vorschlag. Claude bewertet neu gegen D-05-Kriterien (im Audit-Doc §2 wörtlich), unter Beruecksichtigung der `Current Mitigation`.
       - Kandidaten mit Severity-Guess "needs-review" (CF-12) oder "Medium-or-Low" (CF-05, CF-10): finale Severity basierend auf Kopf-Review-Antwort plus Read-Tool-Check.
       - Kandidaten mit Severity-Guess Low UND keinem konkreten Fix-Ansatz -> Streich-Kandidat (Pitfall 1). Entweder ganz raus ODER in §9 Deferred mit Begruendung.

    4. **Operational-vs-Security-Dichotomie** (Pitfall 4):
       - Fuer CF-16 (Rate-Limit zu grob): Angreifer-Pfad beschreibbar? "Angreifer macht Brute-Force auf /api/auth/* und der Rate-Limit ist zu grob um das zu stoppen" -> ja, beschreibbar -> Security Low.
       - Andere Kandidaten analog durchgehen; bei reinem Last-Szenario -> OP-NN in §8.

    5. **Fix-Ansatz-Trim** (Pitfall 2):
       - Max 2 Saetze pro Fix-Ansatz.
       - Keine Library-Namen (z.B. NICHT "Use isolated-vm", stattdessen "Echte Sandbox-Isolation einfuehren (Tech-Auswahl: Phase 2)").
       - Keine Code-Snippets, keine Bench-Werte.
       - Beispiele aus 01-RESEARCH.md Pitfall 2:
         * "Token-Vergleich auf `crypto.timingSafeEqual` umstellen (Pattern aus `internal-sync-server.ts` wiederverwenden)."
         * "Session-Middleware erweitern: Routen nicht in Public-Allowlist werden geblockt (401) statt `userSession=null` zu setzen."

    6. **Current Mitigation Formulierung** (Pitfall 5):
       - Keine Ein-Wort-Mitigation.
       - "keine" nur mit angehaengtem Satz: "keine - Endpoint ist oeffentlich und fuehrt die Unsafe-Operation direkt aus." oder "keine - im Kopf-Review geprueft, es gibt keine indirekte Schutzschicht."

    7. **Finding-ID-Vergabe:** Starte bei F-01, vergib sequentiell ohne Luecken. Reihenfolge:
       - §4 Critical: alle F-... mit Severity Critical, innerhalb sortiert nach Bereich (Apps-Plugin zuerst).
       - §5 High: alle F-... mit Severity High, nach Bereich (Auth/Session zuerst).
       - §6 Medium: alle F-... mit Severity Medium, nach Bereich.
       - §7 Low: alle F-... mit Severity Low, nach Bereich.
       - Die CF-Kandidaten-IDs verschwinden - nur im `Discovered-By:`-Feld als Referenz moeglich (z.B. `Discovered-By: CONCERNS.md §"Non-Timing-Safe Token Comparison" (CF-02) + grep P-1`).

    8. **Volumen-Check (D-16):** Nach Konsolidierung Count durchfuehren:
       - `rg -c '^### \[F-' .planning/research/01-security-audit.md`
       - Ziel: 15-25.
       - Wenn > 25: die schwaechsten Lows (ohne konkreten Fix-Ansatz, Defense-in-Depth-only) in §9 Deferred verschieben mit `Warum nicht jetzt: D-16 Volumen-Budget; Risiko akzeptabel weil <Grund>.`
       - Wenn < 15: entweder sind Kandidaten faelschlich konsolidiert worden (zwei CF-IDs in ein Finding) oder die Kopf-Review war mager - dokumentieren, nicht kuenstlich fuellen.

    9. **Target-Phase-Zuordnung (Roadmap SC3):**
       - Jedes Critical + High MUSS auf Phase 2, 3, oder 4 zeigen. NICHT v2, NICHT out-of-scope.
       - Medium und Low koennen auf Phase 2-4, v2 oder out-of-scope zeigen.
       - Mapping-Vorschlag aus 01-RESEARCH.md §"Proposed Finding -> Phase Mapping" als Richtschnur; Claude finalisiert mit Blick auf Severity-Finalisierung.

    10. **Schreibvorgang:**
        - Bearbeite die Datei mit Edit- oder MultiEdit-Tool.
        - Die Blockquote-Platzhalter unter `## 4. Findings - Critical`, `## 5. Findings - High`, `## 6. Findings - Medium`, `## 7. Findings - Low` werden durch die tatsaechlichen `### [F-NN] ...`-Bloecke ERSETZT. Keine doppelten Platzhalter.
        - Jeder `### [F-NN]`-Block enthaelt ALLE 5 Pflicht-Meta-Bullets in der Reihenfolge: Severity, Area, Datei-Pfad(e), Current Mitigation, Fix-Ansatz, Target Phase (+ optional Discovered-By).
        - Zwischen Findings: Leerzeile + `---`? NEIN (einfacher Leer-Trenner, kein Horizontal Rule noetig innerhalb einer §).
        - Reihenfolge der Pflicht-Felder muss identisch sein (validation.sh zaehlt pro Feld).

    Sprache: Fliesstext Deutsch, Feldnamen Englisch (`Severity`, `Current Mitigation`, `Target Phase`, `Fix-Ansatz`, `Datei-Pfad(e)`, `Area`, `Discovered-By`) - konsistent mit 01-PATTERNS.md §Sprach-Konvention.
  </action>
  <verify>
    <automated>N=$(rg -c '^### \[F-' .planning/research/01-security-audit.md); test $N -ge 15 -a $N -le 25 &amp;&amp; test $(rg -c '^### \[F-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Severity:\*\*' .planning/research/01-security-audit.md) &amp;&amp; test $(rg -c '^### \[F-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Datei-Pfad\(e\):\*\*' .planning/research/01-security-audit.md) &amp;&amp; test $(rg -c '^### \[F-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Current Mitigation:\*\*' .planning/research/01-security-audit.md) &amp;&amp; test $(rg -c '^### \[F-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Fix-Ansatz:\*\*' .planning/research/01-security-audit.md) &amp;&amp; test $(rg -c '^### \[F-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Target Phase:\*\*' .planning/research/01-security-audit.md)</automated>
  </verify>
  <acceptance_criteria>
    - Volumen: 15-25 F-Findings. `N=$(rg -c '^### \[F-' .planning/research/01-security-audit.md); test $N -ge 15 -a $N -le 25`.
    - Alle 5 Pflicht-Meta-Felder pro Finding: Zaehl-Check zwischen `^### \[F-` und `^- \*\*Severity:\*\*`, `^- \*\*Datei-Pfad\(e\):\*\*`, `^- \*\*Current Mitigation:\*\*`, `^- \*\*Fix-Ansatz:\*\*`, `^- \*\*Target Phase:\*\*` MUSS pro Feld gleich sein wie F-Count.
    - Sequentielle F-IDs ohne Luecken: Die IDs `[F-01]`, `[F-02]`, ..., `[F-NN]` sind alle present. Script: `for i in $(seq -f "%02g" 1 $N); do rg -q "^### \[F-$i\]" .planning/research/01-security-audit.md || echo "MISSING F-$i"; done` produziert KEINE MISSING-Zeile.
    - Kein Critical/High auf v2/out-of-scope: Fuer jeden `### [F-...]`-Block mit `- **Severity:** Critical` oder `- **Severity:** High` darf `- **Target Phase:**` NICHT mit `v2` oder `out-of-scope` beginnen. Bash-Check:
      ```
      awk '/^### \[F-/{capture=1; sev=""; tp=""} capture && /^- \*\*Severity:\*\* (Critical|High)/{sev=$3} capture && /^- \*\*Target Phase:\*\*/{tp=$3; if(sev!="" && (tp=="v2" || tp=="out-of-scope")) print "FAIL:" $0}' .planning/research/01-security-audit.md
      ```
      Kein Output = PASS.
    - Severity-Sortierung: Header-Reihenfolge `## 4. Findings - Critical` < `## 5. Findings - High` < `## 6. Findings - Medium` < `## 7. Findings - Low` nach Zeile (cmp via line-number): `test $(rg -n '^## 4\. Findings' .planning/research/01-security-audit.md | cut -d: -f1) -lt $(rg -n '^## 5\. Findings' .planning/research/01-security-audit.md | cut -d: -f1)` und analog fuer 5<6, 6<7.
    - Fix-Ansatz ≤ 2 Saetze: Fuer jeden `- **Fix-Ansatz:**`-Bullet Quick-Check: kein Bullet-Text enthaelt `isolated-vm`, `worker_threads`, `vm2`, `isolated_vm` (Library-Namen, Pitfall 2). `! rg -q 'Fix-Ansatz.*isolated-vm|Fix-Ansatz.*worker_threads' .planning/research/01-security-audit.md` (invert - keine Treffer).
    - Current Mitigation keine-Begruendung: jeder Finding mit `Current Mitigation: keine` hat den Dash+Begruendung: `! rg -q '^- \*\*Current Mitigation:\*\* keine$' .planning/research/01-security-audit.md` (invert - kein Finding endet die Zeile mit nur "keine" ohne weiteren Text). Wave-4-Researcher darf `keine - <Grund>` oder `keine (<Grund>)` schreiben.
    - Mindest-Zeilen: `test $(wc -l < .planning/research/01-security-audit.md) -ge 250` (Skelett war ~90 Zeilen, Findings addieren mind. 160).
  </acceptance_criteria>
  <done>
    Audit-Doc §§4-7 sind mit F-NN-Findings gefuellt. Volumen im 15-25-Korridor. Alle 5 Pflicht-Meta-Felder pro Finding vorhanden. Severity-Sortierung korrekt. Kein Critical/High auf v2/out-of-scope. Fix-Ansatz respektiert Pitfall 2, Current Mitigation respektiert Pitfall 5.
  </done>
</task>

<task type="auto">
  <name>Task 2: Operational-Findings (§8) und Deferred-Findings (§9) eintragen</name>
  <files>.planning/research/01-security-audit.md</files>
  <read_first>
    - .planning/research/01-security-audit.md (aktueller Zustand nach Task 1).
    - .planning/codebase/CONCERNS.md §Performance Bottlenecks und §Tech Debt (Input fuer OC-01, OC-02, OC-03).
    - .planning/phases/01-security-audit-priorisierung/01-RESEARCH.md §"Candidate Finding Inventory" §Operational-Kandidaten + §Deferred-Kandidaten (Zeilen ~691-708).
    - .planning/phases/01-security-audit-priorisierung/01-PATTERNS.md §"Pattern D - Operational-Block" und §"Pattern E - Deferred-Block" (Zeilen ~135-170).
    - .planning/phases/01-security-audit-priorisierung/01-CONTEXT.md §D-08 (Deferred-Eigenabschnitt) und §D-15 (Operational-Separation).
  </read_first>
  <action>
    Fuelle §8 Operational Findings und §9 Deferred / Accepted Risks im Audit-Doc.

    **§8 Operational Findings:**

    Basis-Kandidaten aus 01-RESEARCH.md (uebernehmen wenn Pitfall-4-Dichotomie-Regel bestaetigt, dass sie wirklich Operational sind, nicht Security):

    - **OP-01 In-Memory Rate-Limit (nicht horizontal skalierbar)**
      - Class: Operational
      - Area: Rate-Limiting
      - Datei-Pfad(e): `platform/apps/hub/server/utils/rate-limit.ts:13` (Map-Store), `platform/apps/hub/server/middleware/01-rate-limit.ts:7`
      - Current Mitigation: Solo-Deployment (ein Hub-Prozess) - Rate-Limit ist effektiv, weil es pro Prozess korrekt zaehlt.
      - Fix-Ansatz: Redis-backed Store (bereits als v2 `INFRA-01` geplant).
      - Target Phase: v2 (INFRA-01)

    - **OP-02 App-Registry Thundering Herd auf DB**
      - Class: Operational
      - Area: App-Plugin-Loader
      - Datei-Pfad(e): `platform/apps/hub/server/plugins/app-loader.ts:67`
      - Current Mitigation: 15-Sekunden-Cache (`cacheUntil`) - reduziert DB-Load, aber Cache-Invalidate wirkt nicht als Mutex.
      - Fix-Ansatz: Mutex/Lock fuer Refresh ODER Event-basierte Invalidation (bei App-Install/Update).
      - Target Phase: v2 (INFRA-02)

    - **OP-03 DB-Migration-Fixups laufen bei jedem Start**
      - Class: Operational
      - Area: DB-Migrations
      - Datei-Pfad(e): `platform/packages/shared/src/db/run-migrations.ts:101-444` (~200 Zeilen idempotente SQL-Fixups)
      - Current Mitigation: `IF NOT EXISTS`-Guards machen Fixups idempotent - keine falschen Schema-Aenderungen, nur Latenz.
      - Fix-Ansatz: Fixups in Drizzle-Baseline-Migration konsolidieren; Inline-SQL entfernen.
      - Target Phase: v2 (DEBT-01)

    Falls Wave 4 Task 1 weitere Operational-Kandidaten gefunden hat (z.B. aus Kopf-Review), diese als OP-04+ anhaengen.

    **§9 Deferred / Accepted Risks:**

    Basis-Kandidaten (DF-01..DF-05 aus 01-RESEARCH.md) - jede braucht `Warum nicht jetzt:` Pflicht-Feld (D-08):

    - **D-01 Fehlender `pnpm audit`-Lauf**
      - Severity: Low (Defense-in-Depth, Prozess-Luecke)
      - Area: Supply-Chain
      - Datei-Pfad(e): - (Prozess-Luecke, nicht Code)
      - Current Mitigation: 12 `pnpm.overrides` in `platform/package.json` decken bekannte CVE-Patches ab (serialize-javascript, undici, node-forge, h3, srvx, postcss, ...).
      - Warum nicht jetzt: `pnpm audit`-Durchlauf + Finding-Aufloesung ist als eigener Phase-4-Arbeitsblock (SEC-07) geplant. Ein Audit-Lauf HIER wuerde Arbeit duplizieren und Phase 1 aus dem Volumen-Budget (15-25 Findings) schieben.
      - Target Phase: Phase 4 (SEC-07)

    - **D-02 ESLint-Security-Plugins nicht installiert**
      - Severity: Low (Defense-in-Depth)
      - Area: CI / Tooling
      - Datei-Pfad(e): - (Prozess-Luecke)
      - Current Mitigation: Manuelle grep/ripgrep-Scans (Phase 1 Wave 2) decken die wichtigsten Pattern-Klassen ab.
      - Warum nicht jetzt: ESLint-Integration ist CI-Arbeit; CI wird erst in Phase 5 stabilisiert (Lint ist heute non-blocking). Plugin-Install ohne stabile Lint-Pipeline bringt keinen Nutzen.
      - Target Phase: Phase 5 (CI-02)

    - **D-03 Kein CVSS-Scoring / externes Review-Format**
      - Severity: Low (Prozess)
      - Area: Audit-Prozess
      - Datei-Pfad(e): - (Meta-Entscheidung)
      - Current Mitigation: Qualitative C/H/M/L-Kriterien (D-05) sind fuer Solo-Projekt ausreichend.
      - Warum nicht jetzt: Solo-Projekt ohne externen Reviewer - CVSS-Vektoren waeren Overhead ohne Mehrwert. Wenn spaeter externer Reviewer reinkommt, laesst sich C/H/M/L nachtraeglich auf CVSS hochziehen.
      - Target Phase: out-of-scope (v2 falls externes Review kommt)

    - **D-04 Runtime-/Infra-Audit (Docker-Daemon, Cloudflare-Tunnel, Host-OS)**
      - Severity: Medium (vermutet, nicht geprueft)
      - Area: Infrastruktur
      - Datei-Pfad(e): - (Out-of-platform)
      - Current Mitigation: Container laeuft als unprivileged User; Cloudflare-Tunnel-Policy wird von Cloudflare gemanaged.
      - Warum nicht jetzt: Out-of-Scope dieses Stabilisierungs-Projekts (D-14). Koennte eigene "Ops-Hardening"-Milestone werden.
      - Target Phase: out-of-scope

    - **D-05 Audit fuer marketplace/guildai/voice-rooms/app-template**
      - Severity: unbekannt (nicht geprueft)
      - Area: Fremd-Repos
      - Datei-Pfad(e): - (Out-of-platform)
      - Current Mitigation: Getrennte Repos, getrennte Lifecycles; marketplace hat eigene Auth und eigenes Review-Prozess.
      - Warum nicht jetzt: D-14 - dieses Projekt ist `platform/`-only. Wenn dort Audits noetig werden, bekommen sie ihre eigene Milestone.
      - Target Phase: out-of-scope

    Falls Wave 4 Task 1 Findings in den Deferred-Block verschoben hat (wegen Volumen-Budget oder fehlendem Fix-Ansatz), diese als D-06+ anhaengen. Jedes MUSS `Warum nicht jetzt:` haben.

    Falls H.3 in der Kopf-Review ein Gap aufgedeckt hat, das in keinem SEC-02..07 landet (per 01-RESEARCH.md §Open Questions #3), dieses als neuen D-NN anhaengen mit `Target Phase: v2` und der Notiz "wurde in Phase 1 entdeckt, faellt nicht in SEC-02..SEC-07, wird in naechster Milestone als neues Requirement aufgenommen".

    **Schreibvorgang:**
    - Edit-Tool auf `.planning/research/01-security-audit.md`.
    - Blockquote-Platzhalter unter `## 8. Operational Findings` durch OP-NN-Bloecke ersetzen.
    - Blockquote-Platzhalter unter `## 9. Deferred / Accepted Risks` durch D-NN-Bloecke ersetzen.
    - Der erklaerende Blockquote-Text ueber den Findings (`> Diese Items sind Stabilitaets-/Performance-relevant...`) bleibt als Doc-Anker; die Findings werden UNTER dem Blockquote eingefuegt.

    WICHTIG: OP-Bloecke haben `Class: Operational` (nicht `Severity`). D-Bloecke haben BEIDES: `Severity:` (fuer Kontext wieviel waere es ohne Deferred-Entscheidung) UND `Warum nicht jetzt:` (Pflicht per D-08). Beides ist in den Templates oben bereits korrekt.
  </action>
  <verify>
    <automated>test $(rg -c '^### \[OP-' .planning/research/01-security-audit.md) -ge 3 &amp;&amp; test $(rg -c '^### \[D-' .planning/research/01-security-audit.md) -ge 5 &amp;&amp; test $(rg -c '^### \[OP-' .planning/research/01-security-audit.md) -eq $(rg -A2 '^### \[OP-' .planning/research/01-security-audit.md | grep -c '^- \*\*Class:\*\* Operational') &amp;&amp; test $(rg -c '^### \[D-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Warum nicht jetzt:\*\*' .planning/research/01-security-audit.md)</automated>
  </verify>
  <acceptance_criteria>
    - Mindestens 3 Operational-Findings: `test $(rg -c '^### \[OP-' .planning/research/01-security-audit.md) -ge 3`.
    - Mindestens 5 Deferred-Findings: `test $(rg -c '^### \[D-' .planning/research/01-security-audit.md) -ge 5`.
    - Jedes Operational hat `Class: Operational`: `test $(rg -c '^### \[OP-' .planning/research/01-security-audit.md) -eq $(rg -A2 '^### \[OP-' .planning/research/01-security-audit.md | grep -c '^- \*\*Class:\*\* Operational')`.
    - KEIN Operational hat `Severity:` (D-15 Dichotomie): `! rg -A2 '^### \[OP-' .planning/research/01-security-audit.md | grep -q '^- \*\*Severity:\*\*'` (invert).
    - Jedes Deferred-Finding hat `Warum nicht jetzt:`: `test $(rg -c '^### \[D-' .planning/research/01-security-audit.md) -eq $(rg -c '^- \*\*Warum nicht jetzt:\*\*' .planning/research/01-security-audit.md)`.
    - Deferred-Findings haben `Target Phase:`: `test $(rg -c '^### \[D-' .planning/research/01-security-audit.md) -eq $(rg -B6 '^- \*\*Warum nicht jetzt:\*\*' .planning/research/01-security-audit.md | grep -c '^- \*\*Target Phase:\*\*')` (approximativ; Haupt-Check ist das Feld present).
    - Sequentielle OP- und D-IDs ohne Luecken: `for i in $(seq -f "%02g" 1 $(rg -c '^### \[OP-' audit)); do rg -q "^### \[OP-$i\]" audit || echo MISSING; done` leer; analog fuer D-NN.
  </acceptance_criteria>
  <done>
    §8 enthaelt mind. 3 OP-Bloecke mit Class: Operational. §9 enthaelt mind. 5 D-Bloecke mit Severity + Warum nicht jetzt + Target Phase. Operational- und Deferred-Dichotomie (D-08 + D-15) respektiert.
  </done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks muss gelten:
- §§4-7 (Findings by Severity) enthalten 15-25 F-Findings.
- §8 enthaelt ≥3 OP-Findings mit `Class: Operational`.
- §9 enthaelt ≥5 D-Findings mit `Warum nicht jetzt:`.
- Alle 5 Pflicht-Meta-Felder pro F-Finding present (validation.sh-Checks 1-01-SC2a..e).
- Severity-Sortierung korrekt (Critical < High < Medium < Low nach Zeile).
- Kein Critical/High auf v2/out-of-scope.
- Pitfalls 1, 2, 4, 5 respektiert.

Korrespondenz zu 01-VALIDATION.md:
- Check 1-01-SC2a..e: erfuellt durch Pflicht-Meta-Felder in Task 1.
- Check 1-01-SC3a (Severity-Sortierung): erfuellt durch Sortier-Reihenfolge in Task 1.
- Check 1-01-SC3b (kein Critical/High auf v2): erfuellt durch Mapping-Regel in Task 1.
- Check 1-01-D15 (OP-Class: Operational): erfuellt durch Task 2.
- Check 1-01-SC4 (Deferred + Warum nicht jetzt): erfuellt durch Task 2.
- Check 1-01-VOL (15-25 F): erfuellt durch Task 1.
</verification>

<success_criteria>
- Audit-Doc ist inhaltlich befuellt (nicht mehr nur Skelett).
- Alle drei Input-Quellen (CONCERNS.md, grep-results, kopf-review) sind synthetisiert.
- Pitfalls respektiert (Pitfall 2: ≤2 Saetze Fix-Ansatz, Pitfall 5: keine-Begruendung).
- 5 D-06-Pflicht-Meta-Felder pro F-Finding.
- D-15 Operational-Trennung vollzogen.
- D-08 Deferred-Eigenabschnitt mit Warum-nicht-jetzt.
- Volumen 15-25 (D-16).
</success_criteria>

<output>
Nach Abschluss: Erstelle `.planning/phases/01-security-audit-priorisierung/01-04-SUMMARY.md` mit:
- F-Finding-Count (Critical/High/Medium/Low).
- OP-Finding-Count.
- D-Finding-Count.
- Liste neuer CF-IDs, die aus Wave 3 zusaetzlich reinkamen.
- Bucket-Reshuffle-Notizen: welche Kandidaten wurden gestrichen / in Deferred verschoben / als Operational umklassifiziert.
- Status: "Wave 5 (Plan 05) kann finalisieren: Exec-Summary schreiben, Traceability-Tabelle fuellen, validation.sh generieren und SEC-01 auf Done flippen."
</output>
