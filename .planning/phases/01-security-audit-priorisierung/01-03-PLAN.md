---
phase: 01-security-audit-priorisierung
plan: 03
type: execute
wave: 3
depends_on:
  - 01
files_modified:
  - .planning/phases/01-security-audit-priorisierung/kopf-review.md
autonomous: false
waits_for_user: true
requirements:
  - SEC-01
tags:
  - security
  - audit
  - human-in-loop
must_haves:
  truths:
    - "Alle 8 Kopf-Review-Bereiche (A-H) aus 01-RESEARCH.md sind als strukturierte Q&A-Sektionen in kopf-review.md erfasst."
    - "Alle 27 Fragen sind woertlich aus 01-RESEARCH.md uebernommen, keine Paraphrasierung."
    - "Andis Antworten sind woertlich protokolliert (Blockquote), nicht umformuliert - 'Fail Loud, Never Fake'."
    - "Jede Frage hat eine Claude-Bucket-Zuordnung (Finding / Deferred / geloescht) als Anti-Fatigue-Schutz (Pitfall 1)."
    - "Severity-Kriterien (D-05) werden VOR der Session an Andi wiederholt, damit er beim Antworten vorfiltert."
  artifacts:
    - path: ".planning/phases/01-security-audit-priorisierung/kopf-review.md"
      provides: "Protokoll der strukturierten Andi-Kopf-Review-Session als Input fuer Wave 4 Finding-Consolidation"
      contains: "8 Bereiche A-H, 27 Fragen, 27 Bucket-Zuordnungen"
      min_lines: 80
  key_links:
    - from: "kopf-review.md"
      to: "01-RESEARCH.md §Kopf-Review Questionnaire"
      via: "woertliche Uebernahme aller 8 Bereiche und 27 Fragen"
      pattern: "^## Bereich [A-H]"
    - from: "kopf-review.md Claude-Bucket-Zuordnung"
      to: "01-RESEARCH.md §Common Pitfalls §Pitfall 1"
      via: "3-Bucket-System verhindert Finding-Inflation"
      pattern: "Claude-Bucket-Zuordnung"
---

<objective>
Fuehre die strukturierte Kopf-Review-Session mit Andi durch (D-13), um die bisher undokumentierten Security-Reviews aus seinem Kopf als schriftliche Antworten in `.planning/phases/01-security-audit-priorisierung/kopf-review.md` festzuhalten. Der Fragenkatalog ist bereits vollstaendig in 01-RESEARCH.md §"Kopf-Review Questionnaire" ausgearbeitet - diese Wave nutzt ihn verbatim.

Diese Wave ist HUMAN-IN-LOOP - Claude kann die Fragen stellen und Antworten protokollieren, aber Andi MUSS antworten. `waits_for_user: true`.

Purpose: Roadmap Phase-1 Success Criteria #1 verlangt "Andis bisher nur im-Kopf existierende Security-Reviews sind schriftlich verfuegbar". Ohne diese Wave geht Wissen verloren, das in keiner anderen Quelle (CONCERNS.md, grep-Scans) zu finden ist - z.B. Bauchweh-Hypothesen, halb-entdeckte Muster, noch-nicht-verifizierte Sorgen.

Output: Ein Markdown-Protokoll-File im Phase-Dir mit 8 Bereichen (A-H), jede Frage woertlich + Andis woertliche Antwort + Claude-Bucket-Zuordnung pro Frage.

Laeuft parallel zu Wave 2 (grep-Scans) - keine Datenabhaengigkeit, unterschiedliche Output-Files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-audit-priorisierung/01-CONTEXT.md
@.planning/phases/01-security-audit-priorisierung/01-RESEARCH.md
@.planning/phases/01-security-audit-priorisierung/01-PATTERNS.md
@.planning/phases/01-security-audit-priorisierung/01-VALIDATION.md
@.planning/PROJECT.md

<interfaces>
Woertlich uebernommene Fragenliste aus 01-RESEARCH.md §"Kopf-Review Questionnaire" (Zeilen 458-511).
Task 1 setzt das Skelett; Task 2 (Checkpoint) sammelt Andis Antworten.

Vorab-Session-Prompt, den Claude an Andi als erste Nachricht vor Task 2 ausgibt (D-05-Refresher gegen Severity-Drift):

"""
Andi, vor der Kopf-Review-Session: bitte die Severity-Kriterien aktiv im Kopf halten, waehrend du antwortest - hilft dir, schon beim Antworten vorzufiltern:

- Critical: Unauthenticated Remote-Code-Execution, Zugriff auf alle User-Daten/Secrets durch externen Angreifer ohne Nutzerbeteiligung.
- High: Privilege-Escalation fuer authenticated User, Einzel-User-Data-Leak, Timing-/Info-Leak mit direktem Angriffsweg.
- Medium: Schwaechen mit Zusatzvoraussetzungen (innerer Netzzugriff, kompromittierte App), Haertungs-Luecken ohne direkten Exploit.
- Low: Defense-in-Depth, Best-Practice-Abweichungen ohne bekannten Exploit-Pfad.
- Operational (nicht Security): Performance/DoS/Stabilitaet - extra klassifiziert.

Severity = Restrisiko NACH heutiger Mitigation, nicht Raw-Max.

Ich stelle dir jetzt die 8 Bereiche (A-H, insgesamt 27 Fragen). Antworten bitte stichpunktartig; ich protokolliere woertlich. Wenn eine Frage "passt nicht / hab ich nicht im Kopf" ist, sag das direkt - wird als "geloescht mit Begruendung" vermerkt.
"""

Skelett fuer `kopf-review.md` - siehe Vorlage-Referenz in 01-PATTERNS.md §Pattern G (Zeilen 200-244). Task 1 uebertraegt dieses Skelett vollstaendig mit allen 27 Fragen woertlich aus 01-RESEARCH.md §Kopf-Review Questionnaire.

Struktur pro Frage (muss bei ALLEN 27 identisch sein):

```
### X.N - <Frage woertlich aus 01-RESEARCH.md>

> **Andis Antwort:**
>
> _Task 2 fuellt._

**Claude-Bucket-Zuordnung:**
- [ ] Wird Finding (Kandidat-ID: CF-...)
- [ ] Wird Deferred mit Begruendung: ...
- [ ] Wird geloescht mit Begruendung: ...
```

Sonderhinweise:
- A.1 bekommt Zusatznotiz: "A.1 ist Input fuer Phase-2-Research (Sandbox-Tech-Wahl), KEIN Phase-1-Finding - per 01-RESEARCH.md Bereich-A-Note."
- H.3 bekommt Zusatznotiz: "Falls H.3 ein Gap in SEC-02..07 aufdeckt -> per 01-RESEARCH.md §Open Questions #3 wird es Deferred mit Phase v2 und Notiz 'wurde in Phase 1 entdeckt, faellt nicht in SEC-02..SEC-07, wird in naechster Milestone als neues Requirement aufgenommen'. Nicht als neues SEC-XX in Phase 1."

Session-Abschluss-Sektion am Ende mit Platzhaltern:
- Dauer: _Task 2 fuellt._
- Gesamt-Fragen beantwortet: 27 (4+4+5+3+3+3+2+3)
- Bucket-Aufteilung: _Task 2 fuellt (X Finding-Kandidaten, Y Deferred, Z geloescht)._
- Offene Punkte fuer Wave 4: _Task 2 fuellt - Fragen, bei denen Andi "muss ich noch pruefen" gesagt hat._

Frage-Nummerierung (gesamt 27):
- Bereich A: A.1, A.2, A.3, A.4 (4 Fragen)
- Bereich B: B.1, B.2, B.3, B.4 (4 Fragen)
- Bereich C: C.1, C.2, C.3, C.4, C.5 (5 Fragen)
- Bereich D: D.1, D.2, D.3 (3 Fragen)
- Bereich E: E.1, E.2, E.3 (3 Fragen)
- Bereich F: F.1, F.2, F.3 (3 Fragen)
- Bereich G: G.1, G.2 (2 Fragen)
- Bereich H: H.1, H.2, H.3 (3 Fragen)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Andi (user) -> Claude protocol | User-Antworten werden woertlich protokolliert. Claude darf NICHT paraphrasieren. |
| Protocol-file -> private repo | `kopf-review.md` enthaelt potentiell sensitive Bauchweh-Details. Bleibt im privaten Repo; Phase-Branch nicht pushen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | T (Tampering) | Andis Antwort wird unbewusst paraphrasiert / geglaettet durch Claude -> Verlust von Signal | mitigate | Protokoll-Regel: woertlich als Blockquote, keine Umformulierung. |
| T-01-11 | I (Information Disclosure) | Kopf-Review-Antworten leak Finding-Details, bevor Mitigation greift | mitigate | Datei bleibt im private `platform/`-Repo. Phase-Branch nicht oeffentlich pushen. |
| T-01-12 | S (Spoofing - Fake Coverage) | Claude bearbeitet ohne Andi alle Fragen mit "Andi hat nichts zusaetzliches" -> scheinbar abgedeckt, tatsaechlich nicht | mitigate | `waits_for_user: true` - Wave kann nicht ohne User-Input abgeschlossen werden. Task 2 ist `type="checkpoint:human-action"`. |
| T-01-13 | D (Fatigue-basierte Quality-Loss) | Andi ist nach Frage 20 erschoepft, antwortet kurz oder mit "nichts" ohne tatsaechlich nachzudenken | mitigate | 3-Bucket-System pro Frage zwingt zu expliziter Klassifizierung; `geloescht` verlangt Begruendung. |
| T-01-14 | R (Repudiation) | Protokoll-Aenderungen unnachvollziehbar | mitigate | Einmal geschrieben -> git commit (Wave 5 committed zusammen mit Audit). Session-Datum in Header. Blockquote-Format (klar abgegrenzt). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Skelett fuer kopf-review.md erstellen mit allen 8 Bereichen und 27 Fragen woertlich</name>
  <files>.planning/phases/01-security-audit-priorisierung/kopf-review.md</files>
  <read_first>
    - .planning/phases/01-security-audit-priorisierung/01-RESEARCH.md §"Kopf-Review Questionnaire" (Zeilen 448-511) - vollstaendiger Fragenkatalog mit allen 8 Bereichen.
    - .planning/phases/01-security-audit-priorisierung/01-PATTERNS.md §"Pattern G - Kopf-Review-Struktur" (Zeilen ~200-244) - Heading-Konvention und Bucket-Zuordnung-Stil.
    - .planning/phases/01-security-audit-priorisierung/01-CONTEXT.md §D-13 - Kopf-Review-Extraktion-Entscheidung.
  </read_first>
  <action>
    Erstelle `.planning/phases/01-security-audit-priorisierung/kopf-review.md` mit den folgenden Inhalten. Schreibe das Skelett mit dem Write-Tool.

    Struktur:

    1. Header-Metadaten:
       - Title: `# Kopf-Review Protokoll - Phase 1 Security Audit`
       - `**Session-Datum:** YYYY-MM-DD (Task 2 fuellt)`
       - `**Teilnehmende:** Andi + Claude (via Alice)`
       - `**Bezug:** .planning/phases/01-security-audit-priorisierung/01-RESEARCH.md §Kopf-Review Questionnaire (Zeilen 448-511)`
       - `**Schutzmechanismen:**` mit 3 Bullets:
         - Severity-Kriterien (D-05) wurden zu Session-Beginn wiederholt (Anti-Drift).
         - Jede Antwort landet in einem von 3 Buckets: wird Finding / wird Deferred / wird geloescht (Anti-Fatigue, Pitfall 1).
         - Andis Antworten werden WOERTLICH protokolliert (Blockquote) - keine Paraphrasierung (Fail-Loud).

    2. 8 Bereiche als `## Bereich X - <Name>`-Heading in dieser exakten Reihenfolge:
       - `## Bereich A - Apps-Plugin-System`
       - `## Bereich B - Internal-Auth (Hub <-> Bot, MCP, Application-Tokens)`
       - `## Bereich C - Session & CSRF`
       - `## Bereich D - Supply-Chain & Secrets`
       - `## Bereich E - Upload / Media / File-Serving`
       - `## Bereich F - Apps-Pipeline & Sideload`
       - `## Bereich G - Audit- / Logging-Pfad`
       - `## Bereich H - Offene Kategorie (Catch-All)`

    3. Insgesamt 27 Fragen - woertlich aus 01-RESEARCH.md §Kopf-Review Questionnaire. Jede Frage ist ein `### X.N - <Fragetext>` Heading. Nummerierung:
       - A: A.1, A.2, A.3, A.4 (Sandbox-Mechanik, Apps-Code-Aufrufe, h3-Helpers-Whitelist, createAppDb-Scope)
       - B: B.1, B.2, B.3, B.4 (zusaetzliche Token-Vergleiche, APPLICATION_TOKEN_SECRET, MCP-Token-Prod-Status, BOT_INTERNAL_TOKEN-Placeholder)
       - C: C.1, C.2, C.3, C.4, C.5 (03-session-silent-fallback, 02-csrf-skip, Session-Rotation, Cookie-Flags, Dev-Login-Bypass)
       - D: D.1, D.2, D.3 (pnpm.overrides-Kenntnis, .env-Fallback-Defaults, weitere hartcoded docker-compose-Werte)
       - E: E.1, E.2, E.3 (Upload-ueber-MIME-hinaus, media.ts/S3-User-Pfade, media/uploads/-Serve-Auth)
       - F: F.1, F.2, F.3 (app-sideload TOCTOU, esbuild-Obfuscation, installed_apps.code_bundle-Manipulation)
       - G: G.1, G.2 (Security-Events-Audit-Log, PII im Log)
       - H: H.1, H.2, H.3 (bisher nicht genanntes Finding, Angreifer-Traum, Core-Value-Bauchweh ausserhalb SEC-02..07)

    4. Fragetext woertlich aus 01-RESEARCH.md uebernehmen. Beispiel fuer A.1: "Welche konkrete Sandbox-Mechanik schwebt dir vor (isolated-vm / Worker Threads / Docker-per-App)? Wo vermutest du den Haken?"

    5. Unter JEDER Frage: (identisches Schema fuer alle 27 Fragen)

       ```
       > **Andis Antwort:**
       >
       > _Task 2 fuellt._

       **Claude-Bucket-Zuordnung:**
       - [ ] Wird Finding (Kandidat-ID: CF-...)
       - [ ] Wird Deferred mit Begruendung: ...
       - [ ] Wird geloescht mit Begruendung: ...
       ```

    6. Sonderhinweise unter spezifischen Fragen:
       - Nach den Bucket-Bullets von A.1: `- Notiz: A.1 ist Input fuer Phase-2-Research (Sandbox-Tech-Wahl), KEIN Phase-1-Finding - per 01-RESEARCH.md Bereich-A-Note.`
       - Nach den Bucket-Bullets von H.3: `- Notiz: Falls H.3 ein Gap in SEC-02..07 aufdeckt -> per 01-RESEARCH.md §Open Questions #3 wird es Deferred mit Phase v2 und Notiz 'wurde in Phase 1 entdeckt, faellt nicht in SEC-02..SEC-07, wird in naechster Milestone als neues Requirement aufgenommen'. Nicht als neues SEC-XX in Phase 1.`

    7. Session-Abschluss-Sektion:

       ```
       ---

       ## Session-Abschluss

       - **Dauer:** _Task 2 fuellt._
       - **Gesamt-Fragen beantwortet:** 27 (4+4+5+3+3+3+2+3)
       - **Bucket-Aufteilung:** _Task 2 fuellt (X Finding-Kandidaten, Y Deferred, Z geloescht)._
       - **Offene Punkte fuer Wave 4:** _Task 2 fuellt - Fragen, bei denen Andi "muss ich noch pruefen" gesagt hat._
       ```

    Separator `---` zwischen Bereichen.

    Sprache: Deutsch. Feldnamen wie `Claude-Bucket-Zuordnung` bleiben Deutsch.

    Kein YAML-Frontmatter.

    WICHTIG: Die Fragen MUESSEN woertlich aus 01-RESEARCH.md uebernommen werden. Keine Umformulierungen.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-security-audit-priorisierung/kopf-review.md &amp;&amp; test $(rg -c '^## Bereich [A-H] ' .planning/phases/01-security-audit-priorisierung/kopf-review.md) -eq 8 &amp;&amp; test $(rg -c '^### [A-H]\.[0-9]+ ' .planning/phases/01-security-audit-priorisierung/kopf-review.md) -eq 27 &amp;&amp; test $(rg -c '^\*\*Claude-Bucket-Zuordnung:\*\*' .planning/phases/01-security-audit-priorisierung/kopf-review.md) -eq 27</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/01-security-audit-priorisierung/kopf-review.md` exit 0.
    - Genau 8 `## Bereich`-Headings: `test $(rg -c '^## Bereich [A-H] ' kopf-review.md) -eq 8`.
    - Genau 27 Fragen als `### X.N`-Headings: `test $(rg -c '^### [A-H]\.[0-9]+ ' kopf-review.md) -eq 27`.
    - Genau 27 Bucket-Zuordnungen: `test $(rg -c '^\*\*Claude-Bucket-Zuordnung:\*\*' kopf-review.md) -eq 27`.
    - Jede Bucket-Zuordnung hat 3 Checkbox-Bullets: `test $(rg -c '^- \[ \] Wird (Finding|Deferred|geloescht)' kopf-review.md) -eq 81` (27*3).
    - 8 Bereichs-Headings enthalten die erwarteten Namen: `rg -q '^## Bereich A - Apps-Plugin' kopf-review.md && rg -q '^## Bereich B - Internal-Auth' kopf-review.md && rg -q '^## Bereich C - Session' kopf-review.md && rg -q '^## Bereich D - Supply-Chain' kopf-review.md && rg -q '^## Bereich E - Upload' kopf-review.md && rg -q '^## Bereich F - Apps-Pipeline' kopf-review.md && rg -q '^## Bereich G - Audit' kopf-review.md && rg -q '^## Bereich H - Offene Kategorie' kopf-review.md`.
    - Sonderhinweise present: `rg -q 'A.1 ist Input fuer Phase-2-Research' kopf-review.md && rg -q 'H.3.*Open Questions #3' kopf-review.md`.
    - Session-Abschluss-Sektion present: `rg -q '^## Session-Abschluss' kopf-review.md`.
    - Gesamt-Fragen-Notiz: `rg -q 'Gesamt-Fragen beantwortet.*27' kopf-review.md`.
    - Mindest-Zeilenanzahl: `test $(wc -l < kopf-review.md) -ge 80`.
  </acceptance_criteria>
  <done>
    `kopf-review.md` enthaelt das vollstaendige Skelett mit 8 Bereichen, 27 nummerierten Fragen (A.1-H.3), 27 Bucket-Zuordnungen (je 3 Checkbox-Bullets), Sonderhinweise fuer A.1 und H.3, Session-Abschluss-Sektion. Keine Antworten gefuellt (Task 2 Job). Datei ist bereit fuer Checkpoint-Session mit Andi.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Checkpoint - Kopf-Review-Session mit Andi durchfuehren und Antworten woertlich protokollieren</name>
  <files>.planning/phases/01-security-audit-priorisierung/kopf-review.md</files>
  <what-built>
    Task 1 hat das vollstaendige Kopf-Review-Skelett mit 27 leeren Fragen in `.planning/phases/01-security-audit-priorisierung/kopf-review.md` angelegt. Diese Task fuehrt die Session mit Andi durch - Claude stellt die Fragen, Andi antwortet, Claude protokolliert WOERTLICH.

    Dies ist eine echte Human-in-the-Loop-Interaktion - es gibt keinen CLI/API-Ersatz. Andis bisher-nur-im-Kopf-Reviews existieren nur in seinem Kopf; nur er kann sie aussprechen.
  </what-built>
  <action>
    CHECKPOINT-TASK - MENSCH-IM-LOOP. Claude stellt Fragen; Andi antwortet; Claude protokolliert woertlich in kopf-review.md. Vollstaendige Anleitung steht unter `<how-to-verify>`. Keine autonome Ausfuehrung ohne Andi.

    Ablauf:
    1. Session-Preambel an Andi schicken (Severity-Kriterien-Refresh + Ablauf-Erklaerung) - Wortlaut siehe how-to-verify.
    2. Fuer jede der 27 Fragen (A.1..H.3): Frage woertlich stellen, Antwort abwarten, WOERTLICH in den Blockquote der jeweiligen Frage einfuegen (Edit-Tool auf kopf-review.md), Bucket-Checkbox setzen (Finding / Deferred / geloescht) mit Begruendung.
    3. Nach der letzten Frage Session-Abschluss-Block fuellen (Dauer, Bucket-Aufteilung, offene Punkte).
    4. Auf resume-signal von Andi warten (approved / revise / pause).

    Keine Paraphrasierung, keine Platzhalter am Session-Ende.
  </action>
  <how-to-verify>
    Vor der Session - Session-Preambel an Andi senden (aus 01-PATTERNS.md + interfaces-Block):

    "Andi, vor der Kopf-Review-Session: bitte die Severity-Kriterien aktiv im Kopf halten, waehrend du antwortest:
    - Critical: Unauthenticated RCE, voller Daten-/Secret-Zugriff durch externen Angreifer ohne Nutzerbeteiligung.
    - High: Privilege-Escalation fuer authenticated User, Einzel-User-Data-Leak, Timing-/Info-Leak mit direktem Angriffsweg.
    - Medium: Schwaechen mit Zusatzvoraussetzungen, Haertungs-Luecken ohne direkten Exploit.
    - Low: Defense-in-Depth, Best-Practice-Abweichungen ohne bekannten Exploit-Pfad.
    - Operational (nicht Security): Performance/DoS/Stabilitaet.
    Severity = Restrisiko NACH heutiger Mitigation, nicht Raw-Max.
    27 Fragen in 8 Bereichen (A-H). Antworten bitte stichpunktartig; ich protokolliere woertlich. 'Passt nicht / hab ich nicht im Kopf' ist eine gueltige Antwort - wird als 'geloescht mit Begruendung' vermerkt."

    Session-Ablauf (fuer jede der 27 Fragen):
    1. Frage woertlich aus kopf-review.md vorlesen (z.B. "A.1 - Welche konkrete Sandbox-Mechanik schwebt dir vor ...?").
    2. Andis Antwort abwarten.
    3. Antwort-Text WOERTLICH in den Blockquote-Block unter der jeweiligen Frage einfuegen (Edit-Tool auf kopf-review.md; den Platzhalter "_Task 2 fuellt._" durch die protokollierte Antwort ersetzen). Keine Paraphrasierung. Wenn Andi sagt "weiss ich nicht mehr" - das ist die Antwort, woertlich uebernehmen.
    4. Claude entscheidet JETZT (pro Frage), welche Bucket-Checkbox gesetzt wird:
       - Antwort ein konkretes, noch nicht in CONCERNS.md / grep-results.md stehendes Risiko -> Checkbox "Wird Finding" setzen und neue sequentielle Kandidat-ID (CF-XX) eintragen.
       - Antwort bestaetigt bekanntes v2/Deferred-Thema -> Checkbox "Wird Deferred mit Begruendung" setzen und kurze Begruendung hinter den Doppelpunkt schreiben.
       - Antwort "nichts"/"weiss nicht"/"passt nicht" -> Checkbox "Wird geloescht mit Begruendung" setzen mit Text "Andi hat kein Finding dazu im Kopf.".
       - Antwort impliziert mehrere Findings -> mehrere CF-IDs anlegen und alle checken.
    5. Nach jeder Frage kurz zurueckmelden: "Protokoliert. Frage N von 27, weiter mit X.M+1?"

    Session-Ende: Nach H.3 den Session-Abschluss-Block ausfuellen (Dauer, Bucket-Aufteilung, offene Punkte).

    Qualitaets-Check (Claude selbst vor Freigabe):
    - Jede der 27 Fragen hat einen nicht-leeren Antwort-Blockquote (nicht mehr "_Task 2 fuellt._").
    - Jede der 27 Fragen hat genau eine angekreuzte "[x] Wird ..."-Bucket-Zuordnung.
    - Antworten sind Blockquote-formatiert (Zeilen beginnen mit "> ").
    - Andi-Zitate nicht paraphrasiert.
    - Session-Abschluss-Sektion hat keine Platzhalter mehr.

    Andi bestaetigt:
    - Antworten sind woertlich (nicht geschoent).
    - Bucket-Zuordnungen sind sinnvoll.
    - Falls Andi revidiert: Edit im File, neuer Bestaetigungs-Cycle.
  </how-to-verify>
  <resume-signal>
    Andi antwortet mit einer dieser Optionen im Chat:
    - `kopf-review approved` -> Wave 3 abgeschlossen, Wave 4 (Plan 04) kann starten.
    - `revise: <konkrete Aenderung>` -> Claude editiert kopf-review.md entsprechend, zeigt Diff, neuer Bestaetigungs-Cycle.
    - `pause` -> Partial-Protokoll bleibt, Session wird spaeter fortgesetzt; bis dahin Wave 4 geblockt.
  </resume-signal>
  <verify>
    <automated>! rg -q 'Task 2 fuellt' .planning/phases/01-security-audit-priorisierung/kopf-review.md &amp;&amp; test $(rg -c '^> \*\*Andis Antwort' .planning/phases/01-security-audit-priorisierung/kopf-review.md) -eq 27 &amp;&amp; test $(rg -c '^- \[x\] Wird ' .planning/phases/01-security-audit-priorisierung/kopf-review.md) -ge 27</automated>
  </verify>
  <acceptance_criteria>
    - Keine `_Task 2 fuellt._`-Platzhalter mehr in `kopf-review.md`: `! rg -q 'Task 2 fuellt' kopf-review.md` (invert check - muss exit 1, d.h. keine Treffer).
    - Genau 27 ausgefuellte Andis-Antwort-Blockquotes: `test $(rg -c '^> \*\*Andis Antwort' kopf-review.md) -eq 27`.
    - Mindestens 27 aktivierte Bucket-Checkboxes: `test $(rg -c '^- \[x\] Wird ' kopf-review.md) -ge 27` (einige Fragen koennen mehrere Findings ergeben -> >27 moeglich).
    - Session-Abschluss-Sektion ausgefuellt (keine `_Task 2 fuellt._` mehr darin).
    - Andi hat explizit `kopf-review approved` geantwortet (Nachweis: User-Message im Chat-Transcript).
  </acceptance_criteria>
  <done>
    Andi hat `kopf-review approved` geantwortet. kopf-review.md enthaelt 27 woertliche Andis-Antworten, 27+ aktivierte Bucket-Checkboxes, Session-Abschluss-Sektion ausgefuellt, keine _Task 2 fuellt._-Platzhalter. Wave 4 kann starten.
  </done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:
- `kopf-review.md` enthaelt 27 woertliche Andis-Antworten.
- Jede Frage hat eine Bucket-Zuordnung mit Begruendung.
- Session-Abschluss-Sektion ist ausgefuellt mit Bucket-Aufteilung und offenen Punkten.

Korrespondenz zu 01-VALIDATION.md: Check 1-01-D13 ("Kopf-Review protokolliert") wird durch `test -f kopf-review.md` in validation.sh (Wave 5) erfuellt. Dieser Plan liefert den Content.
</verification>

<success_criteria>
- Andis bisher-im-Kopf Security-Reviews sind schriftlich - Roadmap Phase-1 SC1 Input geliefert.
- Keine Frage wurde uebersprungen (alle 27 beantwortet).
- Keine Antwort wurde paraphrasiert (Fail-Loud respektiert).
- Bucket-Zuordnung pro Frage verhindert Finding-Fatigue und liefert Wave 4 direkt verwertbare Kandidaten-IDs.
</success_criteria>

<output>
Nach Abschluss: Erstelle `.planning/phases/01-security-audit-priorisierung/01-03-SUMMARY.md` mit:
- Session-Dauer.
- Bucket-Aufteilung (X Finding, Y Deferred, Z geloescht).
- Liste der neuen CF-IDs, die in Wave 3 entstanden sind (z.B. CF-18, CF-19 falls Andi neue Findings brachte).
- Offene Punkte, die in Wave 4 Read-Tool-Verifikation brauchen.
- Pfad: `.planning/phases/01-security-audit-priorisierung/kopf-review.md`.
- Status: "Wave 4 kann konsolidieren, sobald auch Wave 2 fertig ist (parallel)."
</output>
