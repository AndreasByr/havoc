---
phase: 01-security-audit-priorisierung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/01-security-audit.md
  - .planning/phases/01-security-audit-priorisierung/scratch/.gitkeep
autonomous: true
requirements:
  - SEC-01
tags:
  - security
  - audit
  - documentation
must_haves:
  truths:
    - "Das Audit-Doc-Skelett existiert unter .planning/research/01-security-audit.md mit allen 10 Abschnitten (Exec-Summary, Severity-Kriterien, Scope, Findings Critical/High/Medium/Low, Operational, Deferred, Traceability)."
    - "Der Severity-Kriterien-Block (§2) enthält die D-05-Definitionen wörtlich als objektiver Anker gegen Severity-Drift."
    - "Die SEC-02…SEC-07-Traceability-Tabelle (§10) ist als leeres Skelett mit allen 6 Zeilen bereits da, damit Wave 5 nur Finding-IDs einträgt."
    - "Audit-Datum-Header + commit-SHA-Platzhalter sind gesetzt, damit Stale-Audit-Drift (Pitfall 3) verhindert wird."
    - "ripgrep (rg) ist im Container verfügbar (Wave-0-Sanity, blockiert Wave 2+)."
    - "Scratch-Working-Dir für Wave-2- und Wave-3-Arbeitsdateien existiert."
  artifacts:
    - path: ".planning/research/01-security-audit.md"
      provides: "Audit-Dokument-Skelett mit 10 numerierten §-Abschnitten"
      contains: "## 1. Executive Summary, ## 2. Severity-Kriterien, ## 3. Scope & Methodik, ## 4. Findings — Critical, ## 5. Findings — High, ## 6. Findings — Medium, ## 7. Findings — Low, ## 8. Operational Findings, ## 9. Deferred / Accepted Risks, ## 10. SEC-Requirement Traceability"
      min_lines: 90
    - path: ".planning/phases/01-security-audit-priorisierung/scratch/.gitkeep"
      provides: "Arbeits-Directory für Wave-2-grep-results und Wave-3-kopf-review temporäre Outputs"
  key_links:
    - from: ".planning/research/01-security-audit.md §2"
      to: "01-CONTEXT.md D-05"
      via: "Severity-Kriterien-Block wörtlich aus D-05 übernommen"
      pattern: "Unauthenticated.*Remote-Code-Execution|Privilege-Escalation"
    - from: ".planning/research/01-security-audit.md §3"
      to: "01-CONTEXT.md D-14"
      via: "Out-of-Scope-Liste: marketplace/guildai/voice-rooms/app-template/Workspace-Root + Runtime/Infra"
      pattern: "marketplace.*guildai.*voice-rooms"
    - from: ".planning/research/01-security-audit.md §10"
      to: ".planning/REQUIREMENTS.md SEC-02…SEC-07"
      via: "Traceability-Tabelle mit 6 Zeilen (SEC-02..SEC-07) — Abgedeckt-Spalte leer, wird in Wave 5 gefüllt"
      pattern: "^\\| SEC-0[2-7] "
---

<objective>
Erzeuge das leere, vollständig strukturierte Skelett für `.planning/research/01-security-audit.md` in **Deutsch** (per PROJECT.md-Sprach-Regel), das die 10 numerierten §-Abschnitte gemäß D-03-Severity-Gruppierung enthält, den Severity-Kriterien-Block aus D-05 wörtlich, die Scope-/Out-of-Scope-Markierung aus D-14, und die SEC-02…SEC-07-Traceability-Tabelle aus D-06 vorbereitet. Zusätzlich Wave-0-Sanity-Check: `ripgrep` im Container verfügbar, und Arbeits-Subdirectory für Wave 2/3 angelegt.

Purpose: Ohne dieses Skelett hängt Wave 4 (Finding-Consolidation) an der Doc-Struktur-Entscheidung — das wäre Context-Verschwendung. Wave 2 und Wave 3 können parallel loslaufen, SOBALD (a) `rg` bestätigt ist und (b) das Skelett die Ziel-Struktur vorgibt, gegen die ihre Outputs gematcht werden.

Output: Ein leeres aber strukturell komplettes Audit-Doc + scratch-Dir + bestätigte `rg`-Verfügbarkeit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-security-audit-priorisierung/01-CONTEXT.md
@.planning/phases/01-security-audit-priorisierung/01-RESEARCH.md
@.planning/phases/01-security-audit-priorisierung/01-PATTERNS.md
@.planning/phases/01-security-audit-priorisierung/01-VALIDATION.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Zielstruktur für das Audit-Dokument (aus 01-RESEARCH.md §Audit Document Skeleton + 01-PATTERNS.md Pattern A/B). -->
<!-- Executor kopiert dieses Gerüst 1:1 in die Write-Operation. -->

Markdown-Skelett (copy-paste-ready):

```markdown
# Security Audit — Guildora Platform

**Audit-Datum:** 2026-04-16 (ursprüngliche Erstellung); letzte Aktualisierung: YYYY-MM-DD
**Codebase-Stand:** git-rev `<fill-in-wave-5>` (aus `git rev-parse HEAD` zur Audit-Schlusszeit)
**Scope:** `platform/`-Stack (ohne marketplace/, guildai/, voice-rooms/, app-template/)
**Methode:** Konsolidierung aus `.planning/codebase/CONCERNS.md` (2026-04-15) + strukturierte Kopf-Review-Session mit Andi + gezielte ripgrep-Scans für 5 Pattern-Klassen. KEIN frisches Full-Audit.
**Zielgruppe:** Phase-2/3/4-Builder (Claude-Agents) + Andi im Planning-Modus.

---

## 1. Executive Summary

> Wird in Wave 5 (Plan 05) ausgefüllt, sobald Finding-Anzahl pro Severity/Phase feststeht.
>
> Erwartete Form (Platzhalter):
> - **Gesamt-Findings:** N (davon X Critical, Y High, Z Medium, W Low + V Operational + U Deferred)
> - **Phase-Aufteilung:** Phase 2 = N, Phase 3 = N, Phase 4 = N, v2 / Deferred = N
> - **Sofort-Handlungsempfehlung:** {1 Satz}
> - **Nicht abgedeckt (out-of-scope):** Infrastructure (Docker daemon, Cloudflare), Fremd-Repos.

## 2. Severity-Kriterien

**Critical:** Unauthenticated Remote-Code-Execution, Zugriff auf alle User-Daten oder Secrets durch externen Angreifer ohne Nutzerbeteiligung.
**High:** Privilege-Escalation für authenticated User, Data-Leak einzelner User, Timing-/Info-Leaks mit direktem Angriffsweg.
**Medium:** Schwächen, die zusätzliche Voraussetzungen brauchen (innerer Netzzugriff, kompromittierte Apps), oder Härtungs-Lücken ohne direkten Exploit.
**Low:** Defense-in-Depth, Best-Practice-Abweichungen ohne bekannten Exploit-Pfad.
**Operational (nicht Security):** Performance/DoS/Stabilität — wird getrennt geführt, damit Phase-Priorisierung nicht verzerrt wird (D-15).

Severity bewertet **Restrisiko** nach heutiger Mitigation (`Current Mitigation`-Feld), nicht das theoretische Maximum (D-09).

## 3. Scope & Methodik

**In-Scope:**
- Sämtlicher Code in `platform/apps/*` und `platform/packages/*`
- `platform/docker-compose.yml`, `platform/docker-compose.override.yml`, `platform/.env.example`
- `platform/package.json` (inkl. `pnpm.overrides`)

**Explizit Out-of-Scope** (D-14):
- Code in `marketplace/`, `guildai/`, `voice-rooms/`, `app-template/`, Workspace-Root
- Runtime/Infrastruktur: Docker-Daemon-Config, Cloudflare-Tunnel-Setup, Host-OS-Härtung, Caddy-Config, Netzwerk-Topologie
- `pnpm audit`-Finding-Auflösung (gehört zu Phase 4/SEC-07)
- Einführung neuer Tooling-Layer (ESLint-Security-Plugins etc.) — Phase 5

**Methodik:**
1. Bestehende Findings aus `.planning/codebase/CONCERNS.md` (2026-04-15) übernommen + Pflicht-Metadaten ergänzt.
2. Andis bisher-nur-im-Kopf-Reviews via strukturierter Fragenkatalog (Bereiche A–H) schriftlich protokolliert.
3. 5 ripgrep-Pattern-Scans (Token-Vergleiche, Code-Execution, Hardcoded Secrets, ungeschützte API-Routen, unsafe HTML) durchgeführt; Treffer einzeln bewertet.

---

## 4. Findings — Critical

> Wird in Wave 4 (Plan 04) mit Finding-Blocks gefüllt. Jedes Finding folgt dem Template:
> `### [F-NN] <Titel>` + Bullets: **Severity**, **Area**, **Datei-Pfad(e)**, **Current Mitigation**, **Fix-Ansatz**, **Target Phase**, **Discovered-By** (optional).

## 5. Findings — High

> Wird in Wave 4 gefüllt.

## 6. Findings — Medium

> Wird in Wave 4 gefüllt.

## 7. Findings — Low

> Wird in Wave 4 gefüllt.

## 8. Operational Findings (nicht Security-Severity)

> Diese Items sind Stabilitäts-/Performance-relevant, nicht Security-Exploit-relevant.
> Sie stehen hier, damit Phase-2/3/4-Planning sie nicht fälschlich als Security-Blocker priorisiert.
>
> Jedes Item: `### [OP-NN] <Titel>` + Bullets mit `- **Class:** Operational` (NICHT `Severity:`).

## 9. Deferred / Accepted Risks

> Findings, die bewusst NICHT in dieser Milestone adressiert werden — mit Begründung.
> Jedes Item: `### [D-NN] <Titel>` + Pflicht-Bullet `- **Warum nicht jetzt:** <Begründung>`.

---

## 10. SEC-Requirement Traceability

> Gegenprobe: jedes Critical/High-Finding zeigt auf SEC-02…SEC-07. SEC-01 ist das Meta-Requirement (Audit existiert) und wird in dieser Tabelle NICHT aufgeführt (per 01-RESEARCH.md §Open Questions #2).

| SEC-Req | Beschreibung | Abgedeckt durch Finding(s) |
|---------|--------------|----------------------------|
| SEC-02  | Apps-Plugin Sandbox (CPU/Memory/Timeout + Whitelisted APIs) | _Wave 5 füllt_ |
| SEC-03  | timingSafeEqual für interne Tokens | _Wave 5 füllt_ |
| SEC-04  | Session-Middleware deny-by-default | _Wave 5 füllt_ |
| SEC-05  | OAuth/Cookie/CSRF-Review | _Wave 5 füllt_ |
| SEC-06  | Docker-Compose env-basiert (keine hartcoded DB-Credentials) | _Wave 5 füllt_ |
| SEC-07  | pnpm.overrides-Review + pnpm audit | _Wave 5 füllt_ |

**Unabgedeckt / offen:** _Wave 5 füllt (Liste der SEC-Requirements ohne Finding ODER leer, wenn alle gedeckt)._

---

*Audit erstellt: 2026-04-16 (Phase 1 dieses Stabilisierungs-Projekts)*
```

Verification rules that WILL be enforced in Wave 4+5 (know now, don't drift):
- Heading-Order: `## 4. Findings — Critical` → `## 5. Findings — High` → `## 6. Findings — Medium` → `## 7. Findings — Low` → `## 8. Operational Findings` → `## 9. Deferred / Accepted Risks` → `## 10. SEC-Requirement Traceability` (validation.sh prüft die numerische Reihenfolge).
- Meta-Feld-Stil: `- **Label:** Value` (Bullet-Dash, Space, zwei Sterne, Label, Doppelpunkt, Leerzeichen, Wert). KEIN YAML-Frontmatter (D-02).
- Sprache: Fließtext Deutsch, Feldnamen Englisch (`Severity`, `Target Phase`, `Current Mitigation`, `Fix-Ansatz`, `Datei-Pfad(e)`, `Area`, `Discovered-By`).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner → Filesystem | Schreibt neue Markdown-Datei in `.planning/research/` (new dir). Keine externen Trust-Boundaries in dieser Wave — doc-only. |
| Audit-Repo → Public Push | `platform/`-Repo ist privat; Phase-Branch darf NICHT gepusht werden, bis Phase 2 die Mitigations umsetzt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information Disclosure) | Audit-Doc exposure vor Fix-Delivery | mitigate | `.planning/research/01-security-audit.md` bleibt im privaten Repo; Phase-Branch nicht öffentlich pushen (Sub-Task am Wave-Ende als Reminder-Komment). |
| T-01-02 | T (Tampering) | Severity-Drift (ohne objektiven Anker wird C/H/M/L Bauchgefühl) | mitigate | Severity-Kriterien-Block (§2) wörtlich aus D-05 als Anker; validation.sh erzwingt später dass jedes Finding ein `Severity:`- oder `Class:`-Feld trägt. |
| T-01-03 | R (Repudiation) | Stale-Audit — Phase 2 referenziert Finding, Code-Stelle hat sich unter dem Finding bewegt | mitigate | `**Codebase-Stand:** git-rev `<fill-in>`` Header-Zeile wird in Wave 5 mit aktuellem SHA belegt; Wave 5 setzt git-Tag `phase-1-audit-baseline`. |
| T-01-04 | S (Spoofing) | Fake-Severity (Finding weichgespült) | accept-with-control | Nicht in Wave 1 mitigierbar (kein Content da). Erzwungen in Wave 4 durch „Fail Loud"-Regel + Wave-5-Andi-Review. Hier nur: Skelett enthält keine Weichspül-Phrasen. |
| T-01-05 | E (Elevation of Privilege) | — | n/a | Wave 1 schreibt nur `.planning/`-Dateien; keine Code- oder Infra-Berührung. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Sanity-Check — ripgrep verfügbar und scratch-Dir anlegen</name>
  <files>.planning/phases/01-security-audit-priorisierung/scratch/.gitkeep</files>
  <read_first>
    - .planning/phases/01-security-audit-priorisierung/01-CONTEXT.md (D-10, D-11, D-12 — Scan-Tooling-Entscheidungen)
    - .planning/phases/01-security-audit-priorisierung/01-RESEARCH.md §"Environment Availability" (Zeilen ~805-820) — Pre-flight-Check definiert
    - .planning/phases/01-security-audit-priorisierung/01-VALIDATION.md §"Wave 0 Requirements" — ripgrep-Availability-Anforderung
  </read_first>
  <action>
    Führe die beiden folgenden Commands IN DIESER REIHENFOLGE aus (Bash-Tool, beide Absolutpfade):

    ```bash
    # 1) ripgrep-Verfügbarkeit prüfen. Exit 0 = OK, Exit ≠ 0 = FAIL → Task abbrechen mit Klartext-Fehler.
    command -v rg >/dev/null 2>&1 && rg --version | head -1 || { echo "FAIL: ripgrep missing — cannot proceed with Wave 2 grep scans"; exit 1; }

    # 2) Aktuellen commit-SHA fürs Audit-Header-Feld erfassen (Info-only; Wave 5 nutzt das endgültig).
    cd /home/andreas/workspace/guildora/platform && git rev-parse HEAD

    # 3) scratch-Dir für Wave-2-grep-Results und Wave-3-kopf-review-Arbeitsfiles anlegen.
    mkdir -p /home/andreas/workspace/guildora/platform/.planning/phases/01-security-audit-priorisierung/scratch
    touch /home/andreas/workspace/guildora/platform/.planning/phases/01-security-audit-priorisierung/scratch/.gitkeep
    ```

    Begründung der `.gitkeep`: Wave 2 + Wave 3 schreiben temporäre Arbeitsdateien (`grep-results.md`, `kopf-review.md`). Der `scratch/`-Ordner muss existieren, bevor Wave 2 und Wave 3 parallel starten. `.gitkeep` stellt sicher, dass Git den leeren Ordner trackt — die temporären Ergebnis-Files werden später per `.gitignore`-Regel in Wave 5 aufgeräumt ODER als Audit-Appendix beibehalten (Wave 5 entscheidet per 01-RESEARCH.md §Open Questions #1).

    KEINE weiteren Scans in dieser Task. KEINE Audit-Doc-Erstellung hier — das ist Task 2.
  </action>
  <verify>
    <automated>command -v rg &amp;&amp; test -d /home/andreas/workspace/guildora/platform/.planning/phases/01-security-audit-priorisierung/scratch &amp;&amp; test -f /home/andreas/workspace/guildora/platform/.planning/phases/01-security-audit-priorisierung/scratch/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - `command -v rg` exit 0 (ripgrep verfügbar; sonst Wave 2 blockiert).
    - `test -d .planning/phases/01-security-audit-priorisierung/scratch` exit 0.
    - `test -f .planning/phases/01-security-audit-priorisierung/scratch/.gitkeep` exit 0.
    - `git rev-parse HEAD` produziert einen 40-char hex string (für späteres Header-Filling in Wave 5).
  </acceptance_criteria>
  <done>
    `rg --version` liefert erste Zeile; `.planning/phases/01-security-audit-priorisierung/scratch/.gitkeep` existiert und ist ein leeres File. Der SHA wird im Task-Output im Terminal angezeigt (informational).
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit-Doc-Skelett schreiben — 10 §-Abschnitte, Severity-Kriterien aus D-05, Traceability-Tabelle leer</name>
  <files>.planning/research/01-security-audit.md</files>
  <read_first>
    - .planning/phases/01-security-audit-priorisierung/01-CONTEXT.md §Implementation Decisions (D-01 bis D-16 vollständig — besonders D-03 Severity-Gruppierung, D-05 Severity-Kriterien wörtlich, D-08 Deferred-Section, D-14 Out-of-Scope-Liste, D-15 Operational-Separation)
    - .planning/phases/01-security-audit-priorisierung/01-RESEARCH.md §"Audit Document Skeleton" (Zeilen ~515-653) — vollständiges Copy-Paste-Skelett
    - .planning/phases/01-security-audit-priorisierung/01-PATTERNS.md §"Pattern A — Doc-Header" und §"Pattern B — Abschnitts-Hierarchie" und §"Pattern F — Traceability-Tabelle" (Zeilen ~30-200)
    - .planning/codebase/CONCERNS.md Zeilen 1-3 (Header-Stil-Vorbild `**Analysis Date:**`)
  </read_first>
  <action>
    Erstelle die Datei `.planning/research/01-security-audit.md` mit **exakt** dem Markdown aus dem `<interfaces>`-Block oben (innerhalb der ```markdown ... ```-Fences). Copy-paste one-shot; keine eigenen Umformulierungen, keine weggelassenen Abschnitte.

    Verpflichtende Details beim Schreiben:

    1. **Datei-Pfad:** `/home/andreas/workspace/guildora/platform/.planning/research/01-security-audit.md` (Directory `.planning/research/` muss ggf. von Write-Tool implizit angelegt werden — `.planning/` existiert bereits, `research/` ist neu).

    2. **Header-Metadaten** (Zeile 3-7):
       - `**Audit-Datum:** 2026-04-16 (ursprüngliche Erstellung); letzte Aktualisierung: YYYY-MM-DD` — Platzhalter-Datum bleibt `YYYY-MM-DD`, Wave 5 ersetzt.
       - `**Codebase-Stand:** git-rev `<fill-in-wave-5>`` — Platzhalter, Wave 5 füllt mit echtem SHA.
       - `**Scope:**`, `**Methode:**`, `**Zielgruppe:**` wörtlich wie im Interfaces-Block.

    3. **§2 Severity-Kriterien-Block:** MUSS die D-05-Wortlaute wörtlich enthalten — 4 fett-gedruckte Begriffe (`**Critical:**`, `**High:**`, `**Medium:**`, `**Low:**`) plus `**Operational (nicht Security):**` plus den Restrisiko-Hinweis aus D-09. Keine Paraphrasierung.

    4. **§3 Scope & Methodik:** Out-of-Scope-Liste aus D-14 wörtlich (`marketplace/`, `guildai/`, `voice-rooms/`, `app-template/`, Workspace-Root + Runtime/Infrastruktur-Aufzählung).

    5. **§§4-7 (Critical/High/Medium/Low):** LEER mit Blockquote-Hinweis `> Wird in Wave 4 (Plan 04) mit Finding-Blocks gefüllt. Jedes Finding folgt dem Template: \`### [F-NN] <Titel>\` + Bullets ...`.

    6. **§8 Operational Findings:** Blockquote-Hinweis mit `- **Class:** Operational` (explizit, damit validation.sh später anschlägt, falls jemand `Severity:` statt `Class:` nutzt).

    7. **§9 Deferred / Accepted Risks:** Blockquote-Hinweis mit Pflicht-Bullet `- **Warum nicht jetzt:**`.

    8. **§10 Traceability-Tabelle:** 3-spaltige Markdown-Tabelle mit Header `| SEC-Req | Beschreibung | Abgedeckt durch Finding(s) |` und genau 6 Datenzeilen (SEC-02, SEC-03, SEC-04, SEC-05, SEC-06, SEC-07) mit der jeweiligen Beschreibung (aus Interfaces-Block) und `_Wave 5 füllt_` als Platzhalter. SEC-01 erscheint NICHT (per 01-RESEARCH.md §Open Questions #2).

    9. **Footer (letzte Zeile):** `*Audit erstellt: 2026-04-16 (Phase 1 dieses Stabilisierungs-Projekts)*`

    WICHTIG — Sprache: Fließtext Deutsch, Feldnamen/Headings teilweise Englisch (Severity, Target Phase, Current Mitigation) — siehe 01-PATTERNS.md §"Sprach-Konvention". Nicht übersetzen.

    WICHTIG — Kein Frontmatter: Dieses File nutzt KEIN YAML-Frontmatter (D-02). Datei startet mit `# Security Audit — Guildora Platform` als erste nicht-leere Zeile.
  </action>
  <verify>
    <automated>test -f .planning/research/01-security-audit.md &amp;&amp; rg -q '^\*\*Audit-Datum:\*\*' .planning/research/01-security-audit.md &amp;&amp; rg -q '^## 2\. Severity-Kriterien' .planning/research/01-security-audit.md &amp;&amp; rg -q '^## 9\. Deferred / Accepted Risks' .planning/research/01-security-audit.md &amp;&amp; rg -q '^## 10\. SEC-Requirement Traceability' .planning/research/01-security-audit.md &amp;&amp; for req in SEC-02 SEC-03 SEC-04 SEC-05 SEC-06 SEC-07; do rg -q "^\| $req " .planning/research/01-security-audit.md || { echo "MISSING: $req"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/research/01-security-audit.md` exit 0.
    - Datei enthält Header-Zeile passend zu Regex `^\*\*Audit-Datum:\*\*` (D-06 SC1-Check).
    - Datei enthält alle 10 `##`-Section-Header in numerischer Reihenfolge 1 bis 10: `rg -n '^## [0-9]+\. ' .planning/research/01-security-audit.md | head -10` liefert genau 10 Zeilen, beginnend mit `1.`, endend mit `10.`.
    - `rg -q '^## 2\. Severity-Kriterien' .planning/research/01-security-audit.md` → exit 0.
    - `rg -q '^## 4\. Findings — Critical' .planning/research/01-security-audit.md` → exit 0 (identischer Heading-Text wird von Wave 4 erwartet).
    - `rg -q '^## 8\. Operational Findings' .planning/research/01-security-audit.md` → exit 0.
    - `rg -q '^## 9\. Deferred / Accepted Risks' .planning/research/01-security-audit.md` → exit 0.
    - `rg -q '^## 10\. SEC-Requirement Traceability' .planning/research/01-security-audit.md` → exit 0.
    - Traceability-Tabelle enthält alle 6 SEC-Requirement-Zeilen: `for req in SEC-02 SEC-03 SEC-04 SEC-05 SEC-06 SEC-07; do rg -q "^\| $req " .planning/research/01-security-audit.md || exit 1; done`.
    - SEC-01 erscheint NICHT in der Traceability-Tabelle: `! rg -q '^\| SEC-01 ' .planning/research/01-security-audit.md` (invert check — muss failen, d.h. Zeile darf nicht existieren).
    - Datei enthält KEIN YAML-Frontmatter: `head -1 .planning/research/01-security-audit.md | rg -q '^# Security Audit'` (erste Zeile ist H1, nicht `---`).
    - Severity-Kriterien-Block enthält alle 4 wörtlichen D-05-Begriffe: `rg -q 'Unauthenticated.*Remote-Code-Execution' .planning/research/01-security-audit.md && rg -q 'Privilege-Escalation' .planning/research/01-security-audit.md && rg -q 'Defense-in-Depth' .planning/research/01-security-audit.md`.
    - Minimum-Zeilenanzahl: `test $(wc -l < .planning/research/01-security-audit.md) -ge 90` (Skelett ist ~90-110 Zeilen; deutlich weniger würde auf ausgelassene Abschnitte hindeuten).
  </acceptance_criteria>
  <done>
    `.planning/research/01-security-audit.md` existiert, alle 10 §-Abschnitte vorhanden in korrekter Reihenfolge, Severity-Kriterien-Block wörtlich aus D-05, Traceability-Tabelle mit 6 Zeilen (SEC-02..SEC-07) als leere Platzhalter, kein YAML-Frontmatter, erste Zeile ist H1-Heading. Datei ist bereit für Wave 4 zum Befüllen der Finding-Sections und für Wave 5 zum Befüllen von Exec-Summary + Traceability.
  </done>
</task>

</tasks>

<verification>
Alle Tasks haben automatisierte `<verify>`-Kommandos. Nach Abschluss beider Tasks MUSS gelten:
- `rg --version | head -1` erfolgreich (ripgrep verfügbar).
- `.planning/phases/01-security-audit-priorisierung/scratch/.gitkeep` existiert.
- `.planning/research/01-security-audit.md` existiert mit allen 10 numerierten §-Abschnitten.
- 6 SEC-Requirement-Zeilen in der Traceability-Tabelle (SEC-02..SEC-07, nicht SEC-01).
- Severity-Kriterien-Block wörtlich aus D-05.

Diese Plan-Artefakte sind Input für validation.sh-Checks SC1 (Audit-Datum-Header), SC3a (Heading-Reihenfolge), TRACE (SEC-02..07 gedeckt) — siehe 01-VALIDATION.md Tasks 1-01-SC1, 1-01-SC3a, 1-01-TRACE.
</verification>

<success_criteria>
- Audit-Doc-Skelett ist leer, aber strukturell vollständig — Wave 4 kann ohne Struktur-Fragen Findings einfügen.
- ripgrep-Verfügbarkeit ist bestätigt — Wave 2 kann parallel zu Wave 3 laufen.
- scratch/-Dir ist angelegt — Wave 2 kann nach `scratch/grep-results.md` schreiben, Wave 3 nach `scratch/kopf-review.md` (oder direkt ins Phase-Dir, je nach finaler Entscheidung in Wave 5).
- Kein Finding-Content in dieser Wave. Kein Code-Fix. Reines Doc-Gerüst.
</success_criteria>

<output>
Nach Abschluss: Erstelle `.planning/phases/01-security-audit-priorisierung/01-01-SUMMARY.md` mit:
- Was wurde erzeugt (Skelett-Umfang in Zeilen, Verzeichnis-Struktur).
- SHA-Output von `git rev-parse HEAD` (wird in Wave 5 für Header-Füllung benutzt).
- Ergebnis des `rg --version`-Checks (Version-String).
- Blockierte/nicht-blockierte Downstream-Waves: "Wave 2 und Wave 3 können jetzt parallel starten."
</output>
